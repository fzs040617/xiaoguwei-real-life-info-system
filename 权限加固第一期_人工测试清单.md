# 权限加固第一期_人工测试清单

> 目标：验证“后端接口真正权限校验（第一期）”是否可用。  
> 原则：只做非破坏性测试，不删除、不清空、不导入、不改密码、不改数据库结构。

---

## 1. 测试前准备

1. 先关闭旧后端窗口，避免仍在运行旧代码。
2. 在项目根目录双击：

```text
打开后端.bat
```

3. 打开接口文档：

```text
http://127.0.0.1:8000/docs
```

4. 确认后端正常启动，没有红色报错。
5. 备份项目或确认已有可用备份。
6. 测试期间不要执行真实删除、真实清空、真实导入。

---

## 2. 公开功能测试

这些功能不应因为权限加固而坏掉：

1. 未登录打开 `02_前端页面/index.html`。
2. 首页公开模块能正常显示和跳转。
3. 打开公开列表：
   - `submit.html`
   - `map.html`
   - `route.html`
   - 可查看公开线索、真实库、地图点、路线列表。
4. 打开详情页：
   - `clue-detail.html?id=已有线索ID`
   - `item-detail.html?id=已有真实库ID`
   - `map-detail.html?id=已有地图点ID`
   - `route-detail.html?id=已有路线ID`
5. 普通用户或未登录用户提交线索：
   - 只提交测试线索时，请使用明显测试标题。
   - 不要提交会污染正式数据的大量内容。
6. 普通用户提交反馈：
   - 对线索、真实库、地图点、路线提交反馈应可用。

成功标志：

```text
公开页面能打开。
公开列表能加载。
详情页能打开。
提交线索和提交反馈不要求管理员 token。
```

---

## 3. 管理员页面测试

登录管理员账号后，逐个打开：

```text
admin.html
manage.html
crawler.html
backup.html
feedback-admin.html
map.html
route.html
history.html
```

检查：

1. 页面能正常加载。
2. 管理员入口不被误拦截。
3. 列表数据能读取。
4. 按钮区域没有明显报错。
5. 浏览器 Console 没有新增脚本语法错误。

注意：

```text
不要点击真实删除。
不要真实导入备份。
不要真实清空历史。
不要真实修改用户密码。
```

---

## 4. 管理员低风险操作测试

只做低风险或 0 影响测试：

1. 备份导出：
   - 打开 `backup.html`。
   - 登录管理员。
   - 确认备份预览能加载。
   - 如需点击导出，只导出 JSON 文件，不导入。

2. 更新历史未来日期 0 影响测试：
   - 打开 `history.html`。
   - 登录管理员。
   - 开始日期：`2099-01-01`
   - 结束日期：`2099-01-02`
   - 系统密码：`xgw2026`
   - 确认文字：`清空历史`
   - 预期：返回或提示 `已清空 0 条更新历史`。

3. 数据管理加载：
   - 打开 `manage.html`。
   - 确认线索和真实库列表能加载。
   - 不点击删除、归档、恢复、编辑保存。

4. 审核中心加载：
   - 打开 `admin.html`。
   - 确认待审核线索列表能加载。
   - 不执行审核通过、删除。

5. 采集管理加载：
   - 打开 `crawler.html`。
   - 确认采集目标列表能加载。
   - 不新增真实采集目标，不删除目标。
   - 不点击真实“立即执行采集”，除非确认采集目标和结果不会污染数据。

6. 反馈中心加载：
   - 打开 `feedback-admin.html`。
   - 确认反馈列表能加载。
   - 不删除反馈。

---

## 5. 普通用户权限测试

1. 退出管理员。
2. 登录普通用户。
3. 打开以下页面：

```text
admin.html
manage.html
crawler.html
backup.html
feedback-admin.html
dashboard.html
history.html
```

预期：

```text
普通用户应被权限守卫拦截，或看不到管理功能。
普通用户不应看到危险操作区。
普通用户不能执行管理接口。
```

同时确认普通用户仍可：

```text
打开首页。
查看公开列表。
查看详情页。
提交线索。
提交反馈。
编辑自己的资料。
退出登录。
```

---

## 6. bad token 测试

如果浏览器 F12 不方便，可以使用 CMD / PowerShell。

### 6.1 运行安全测试 bat

在项目根目录双击：

```text
权限加固_安全测试命令.bat
```

该脚本只使用 `bad` token 和未来日期，不会删除数据。

### 6.2 手动 PowerShell 命令

#### GET /backup/export?token=bad

```powershell
Invoke-WebRequest -Uri "http://127.0.0.1:8000/backup/export?token=bad" -Method GET -UseBasicParsing
```

预期：

```text
401 或 403。
不应返回备份 JSON。
```

#### DELETE /update-history/clear-range-admin，bad token + 未来日期

```powershell
$body = @{
  token = "bad"
  system_password = "xgw2026"
  confirm_text = "清空历史"
  start_date = "2099-01-01"
  end_date = "2099-01-02"
} | ConvertTo-Json

Invoke-WebRequest `
  -Uri "http://127.0.0.1:8000/update-history/clear-range-admin" `
  -Method DELETE `
  -ContentType "application/json; charset=utf-8" `
  -Body $body `
  -UseBasicParsing
```

预期：

```text
401 或 403。
不应清空任何历史。
```

#### DELETE /feedbacks/1?token=bad

```powershell
Invoke-WebRequest -Uri "http://127.0.0.1:8000/feedbacks/1?token=bad" -Method DELETE -UseBasicParsing
```

预期：

```text
优先应返回 401 或 403，因为 token 无效。
如果返回 404，说明后端先检查了数据是否存在再检查权限；这不是理想顺序，需要回报。
本次期望是无效 token 不应执行删除。
```

---

## 7. 失败时收集什么

请把以下信息发回来：

1. 后端 CMD 完整报错。
2. 页面名称，例如 `backup.html`、`manage.html`。
3. 当前登录角色：未登录 / 普通用户 / 管理员。
4. 请求 URL。
5. 请求方法：GET / POST / PATCH / DELETE。
6. 状态码。
7. 响应内容。
8. 浏览器 Console 报错。
9. Network 里失败请求的 request payload 或 query string。
10. 你点击了哪个按钮、输入了什么安全测试数据。

---

## 8. 绝对不要做的测试

```text
不要真实删除线索。
不要真实删除真实库。
不要真实删除地图点。
不要真实删除路线。
不要真实删除反馈。
不要真实清空更新历史。
不要真实导入备份。
不要覆盖数据库。
不要修改用户密码。
不要删除用户。
不要做批量删除。
```

