# 项目交接与续接总文档

> 适用对象：ChatGPT、Codex、其他后续接手开发者。  
> 项目：小谷围岛广州大学城真实生活信息共建系统。  
> 规则：任何新对话、新账号、新 Codex 任务开始前，都必须先读本文件，再读当前项目代码。不要依赖旧聊天记忆，以本文件和当前代码为准继续开发。

---

## 一、给 ChatGPT / Codex 的开场说明

这是“小谷围岛广州大学城真实生活信息共建系统”的续接文档，也是后续开发的唯一交接依据。

无论是切换 ChatGPT 账号、开启新对话、启动新的 Codex 任务，还是交给其他开发者继续，都必须先阅读本文件。不要依赖旧聊天记录、旧账号记忆或口头印象。实际开发时，以本文件、原续接文档和当前项目代码共同为准；如果文档和代码不一致，先读代码确认现状，再把确认结果补回文档。

后续每次完成修改，都要在本文档末尾追加“本次修改记录”，写清楚修改文件、修改内容、接口变化、测试步骤和当前测试状态。

---

## 二、项目基本信息

1. 项目名称：小谷围岛广州大学城真实生活信息共建系统。
2. 项目根目录：`C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统`
3. 后端目录：`C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务`
4. 前端目录：`C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\02_前端页面`
5. 后端启动命令：

```bat
cd /d "C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务"
py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
```

也可以在项目根目录双击或运行：

```bat
打开后端.bat
```

6. 前端首页路径：

```text
C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\02_前端页面\index.html
```

7. 接口文档地址：

```text
http://127.0.0.1:8000/docs
```

8. 当前 Python 依赖情况：
   - 当前没有在项目中扫描到 `requirements.txt`、`pyproject.toml`、`Pipfile` 或 `poetry.lock`。
   - 从代码导入可以看到至少使用：`fastapi`、`uvicorn`、`pydantic`、`sqlalchemy`、`requests`、`beautifulsoup4`。
   - 后端本地数据库为 SQLite，文件：`01_后端服务\xiaoguwei.db`。

---

## 三、开发原则

1. 不要重构整个项目。
2. 不要删除已有功能。
3. 每次只做一个明确功能包。
4. 修改前必须先阅读本续接文档、原续接文档和现有代码。
5. 优先做最小范围修改，避免无关改动。
6. 优先复用已有脚本、已有接口、已有样式和已有权限逻辑。
7. 如果需要新增全局 JS，必须加入 `02_前端页面\global-loader.js`，并更新 `GLOBAL_SCRIPT_VERSION`。
8. 如果统一修改所有 HTML 的 `global-loader.js?v=xx`，优先使用已有脚本 `02_前端页面\tools\cleanup-global-scripts.py`。
9. 修改完成后必须写清楚修改文件、修改内容和测试步骤。
10. 修改完成后必须更新本交接文档。
11. 管理类、危险类操作不能只靠前端隐藏，后端也必须校验权限。
12. 如果发现文档和代码不一致，不要直接猜；先检查代码，确认后更新文档说明。

---

## 四、当前系统结构

### 4.1 后端主要文件

后端目录：`01_后端服务`

```text
app.py              FastAPI 主程序，包含大部分接口、权限校验、备份导入导出、更新历史、用户系统等。
models.py           SQLAlchemy 数据模型。
database.py         SQLite 数据库连接配置，当前使用 sqlite:///./xiaoguwei.db。
crawler_service.py  公开网页采集相关服务。
scheduler.py        定时任务启动和调度。
xiaoguwei.db        本地 SQLite 数据库文件。
.venv               本地 Python 虚拟环境目录。
```

### 4.2 前端主要文件

前端目录：`02_前端页面`

```text
index.html                 首页。
submit.html                提交线索。
admin.html                 审核中心。
manage.html                数据管理。
dashboard.html             数据看板。
crawler.html               采集管理。
feedback-admin.html        反馈中心。
map.html                   地图中心。
route.html                 路线中心。
history.html               更新历史中心。
backup.html                备份导出/导入。
clue-detail.html           线索详情页。
item-detail.html           真实库详情页。
map-detail.html            地图点详情页。
route-detail.html          路线详情页。
style.css                  全站主要样式。
global-loader.js           全局脚本加载器。
admin-permission-guard.js  管理员权限守卫。
user-widget.js             用户入口旧版脚本。
user-widget-v2.js          用户入口新版脚本。
history-danger-zone.js     更新历史危险操作区。
history.js                 更新历史列表基础逻辑。
history-filter-v2.js       更新历史筛选逻辑。
history-auto.js            自动记录更新历史。
history-badge.js           卡片/列表历史标记。
history-inline.js          详情页内嵌历史。
backup.js                  备份导出与预览。
backup-import.js           备份导入逻辑。
backup-import-layout.js    备份导入布局增强。
dashboard-v2.js            综合数据看板 V2。
feedback-history-link.js   反馈提交后联动更新历史。
generic-smart-fill.js      通用智能识别填表。
route-smart-fill.js        路线智能识别填表。
map-smart-fill.js          地图点智能识别填表。
```

### 4.3 当前数据库模型

当前 `models.py` 中主要模型：

```text
Clue           线索表，对应 clues。
VerifiedItem   真实库表，对应 verified_items。
CrawlTarget    采集目标表，对应 crawl_targets。
Feedback       反馈表，对应 feedbacks。
MapPoint       地图点表，对应 map_points。
RoutePlan      路线表，对应 route_plans。
UpdateHistory  更新历史表，对应 update_histories。
AuthUser       用户表，对应 auth_users。
```

重要字段概览：

```text
UpdateHistory:
id, target_type, target_id, action, title, detail, operator, created_at

AuthUser:
id, account, nickname, password_salt, password_hash, role, avatar_base64, session_token, created_at, last_login_at
```

注意：续接旧文档中有时写“routes”，但当前模型表名是 `route_plans`。备份导出字段里使用 `routes` 作为 JSON 键名，对应数据库模型 `RoutePlan` / 表 `route_plans`。

### 4.4 当前主要接口

基础与状态：

```text
GET  /
GET  /health
GET  /db/status
GET  /scheduler/status
POST /scheduler/test-run
```

线索与真实库：

```text
POST   /clues
GET    /clues
GET    /clues/{clue_id}
PATCH  /clues/{clue_id}
PATCH  /clues/{clue_id}/status
POST   /clues/{clue_id}/archive
POST   /clues/{clue_id}/restore
DELETE /clues/{clue_id}

POST   /verified-items
GET    /verified-items
GET    /verified-items/{item_id}
PATCH  /verified-items/{item_id}
POST   /verified-items/{item_id}/archive
POST   /verified-items/{item_id}/restore
DELETE /verified-items/{item_id}

POST   /admin/clues/{clue_id}/approve
GET    /search
```

采集：

```text
POST   /crawler/targets
GET    /crawler/targets
PATCH  /crawler/targets/{target_id}
POST   /crawler/targets/{target_id}/toggle
DELETE /crawler/targets/{target_id}
POST   /crawler/public-url
```

反馈：

```text
POST   /feedbacks
POST   /feedbacks-v2
GET    /feedbacks
GET    /feedbacks/stats
DELETE /feedbacks/{feedback_id}
```

地图点：

```text
POST   /map-points
GET    /map-points
GET    /map-points/{point_id}
PATCH  /map-points/{point_id}
POST   /map-points/{point_id}/archive
POST   /map-points/{point_id}/restore
DELETE /map-points/{point_id}
```

路线：

```text
POST   /routes
GET    /routes
GET    /routes/{route_id}
PATCH  /routes/{route_id}
POST   /routes/{route_id}/archive
POST   /routes/{route_id}/restore
DELETE /routes/{route_id}
```

更新历史：

```text
POST   /update-history
GET    /update-history
DELETE /update-history/clear-all
DELETE /update-history/clear-all-admin
DELETE /update-history/clear-all-admin-v2
DELETE /update-history/clear-range-admin
```

备份：

```text
GET  /backup/export
POST /backup/import
GET  /backup/import-code
POST /backup/import-v2
POST /backup/import-v3
```

用户：

```text
POST  /auth/register
POST  /auth/login
POST  /auth/me
PATCH /auth/profile
POST  /auth/logout
POST  /auth/register-v2
PATCH /auth/profile-v2
```

### 4.5 全局加载器机制

当前所有主要 HTML 页面末尾统一引用：

```html
<script src="global-loader.js?v=52"></script>
```

`global-loader.js` 当前版本：

```javascript
const GLOBAL_SCRIPT_VERSION = "52";
```

当前全局加载脚本：

```text
page-scope-cleanup.js
user-widget.js
user-widget-v2.js
modal-shortcuts.js
password-eye.js
user-message-clear.js
user-avatar-click-upload.js
danger-message-clear.js
admin-permission-guard.js
```

以后新增全局功能时：

1. 新建独立 JS 文件。
2. 加入 `global-loader.js` 的 `globalScripts`。
3. 更新 `GLOBAL_SCRIPT_VERSION`。
4. 如需统一 HTML 引用版本，在 `02_前端页面` 目录运行 `py tools\cleanup-global-scripts.py`。

### 4.6 用户系统和管理员权限机制

用户表：`auth_users`。

角色：

```text
admin  管理员
user   普通用户
```

用户密码不是明文保存，而是 `password_salt` + `password_hash`。当前 hash 逻辑在 `app.py` 中。

登录后前端把 token 保存在本地，常见键名：

```text
xgw_user_token
```

管理员权限：

1. 前端通过 `admin-permission-guard.js` 隐藏或拦截管理入口。
2. 危险接口应通过后端 `require_admin_user(db, token)` 校验 token 对应用户是否为管理员。
3. 危险操作还要校验系统密码 `xgw2026`。

### 4.7 当前危险操作规则

危险操作至少包括：

```text
清空更新历史
按时间区间清空更新历史
备份导入
未来所有批量删除、批量覆盖、权限修改、用户禁用等管理操作
```

危险操作必须满足：

1. 已登录管理员账号。
2. 输入系统密码：`xgw2026`。
3. 输入确认文字：`清空历史` 或对应功能规定的确认文字。
4. 后端必须校验管理员身份，不能只靠前端隐藏按钮。
5. 前端应明确提示“只删除哪些数据、不删除哪些正式数据”。

---

## 五、已完成功能

当前系统已完成或基本完成以下功能：

1. 首页：入口导航、模块跳转、用户入口。
2. 提交线索：用户提交线索，支持智能识别填表。
3. 审核中心：管理员审核线索、归入真实库、归档/恢复等。
4. 数据管理：线索和真实库信息管理。
5. 数据看板：综合统计线索、真实库、地图点、路线、反馈、历史、采集目标等。
6. 采集管理：维护采集目标，支持公开网页采集相关功能。
7. 反馈中心：管理线索、真实库、地图点、路线的反馈。
8. 地图中心：新增、搜索、管理地图点，支持跳转高德/百度地图。
9. 路线中心：新增、搜索、管理路线，可选地图点加入路线草稿。
10. 更新历史中心：记录新增、编辑、删除、归档、恢复、审核、反馈等操作。
11. 备份导出/导入：支持导出 JSON，导入时要求管理员、系统密码和验证码。
12. 注册登录系统：支持注册、登录、退出、编辑资料、头像上传、角色显示。
13. 管理员权限守卫：普通用户不能进入或使用管理功能，管理员可正常操作。
14. 全局脚本加载器：统一加载用户组件、权限守卫、快捷键、密码眼睛等全局脚本。
15. 智能识别填表：规则版智能识别，把用户输入填入原表单，由用户再校正提交。
16. 反馈联动更新历史：反馈提交成功后写入“收到反馈”历史并刷新详情页。
17. 详情页编辑：线索、真实库、地图点、路线详情页支持编辑并记录历史。
18. 错误提示自动清除：登录、修改密码、危险操作等错误提示会在重新输入后清除。
19. 弹窗快捷键：支持 Enter、Y、N 等快捷操作，并避免输入框里误触发。
20. 密码眼睛：密码输入框支持显示/隐藏密码。

---

## 六、重要规则和密码

1. 系统密码：`xgw2026`。
2. 普通用户可以提交、查看、反馈、参与共建。
3. 管理员可以审核、采集、管理数据、导入备份、执行危险操作。
4. 危险操作必须满足：管理员登录 + 系统密码 + 确认文字。
5. 后端权限校验不能只靠前端隐藏，必须在接口里验证 token 和角色。
6. 账号和密码规则：只允许英文字母、数字和常用英文标点；不允许中文、空格、特殊不可见字符。
7. 注册管理员账号时，需要选择管理员角色并输入系统密码 `xgw2026`。
8. 修改密码必须输入原正确密码，新密码通常需要输入两遍。
9. 以后新增管理类接口时，后端也要校验管理员身份。
10. 系统密码不是用户账号密码，不应用用户密码替代系统密码。

---

## 七、当前待做清单

保留并整理之前的待做事项：

1. 按时间区间选择性清空历史。
2. 后端接口真正权限校验。
3. 可选绑定邮箱。
4. 管理员用户管理页。
5. 正式数据库迁移。
6. AI 智能识别升级。
7. 系统设置页。

补充说明：

当前代码中已经能看到 `/update-history/clear-range-admin` 接口和 `history-danger-zone.js` 中的区间清空前端区域，但本次没有做功能开发，也没有完整测试。下一次如果正式开发“按时间区间选择性清空历史”，应先核对这部分现有代码是否能正常运行、中文提示是否有编码问题、前后端请求体是否一致、管理员权限是否确实生效。

---

## 八、本次接下来要做的功能

当前优先继续做：

```text
按时间区间选择性清空更新历史
```

功能要求：

1. 只删除 `update_histories` 表中的记录。
2. 不删除 `clues`、`verified_items`、`map_points`、`route_plans`、`feedbacks`、`crawl_targets`、`auth_users` 等正式数据。
3. 管理员可以选择开始日期和结束日期。
4. 必须输入系统密码 `xgw2026`。
5. 必须输入确认文字：`清空历史`。
6. 普通用户不应该看到或不能使用这个危险操作。
7. 后端接口也必须校验管理员身份。
8. 删除完成后返回删除数量。
9. 删除完成后前端刷新历史列表或刷新页面。
10. 日期格式建议使用 `YYYY-MM-DD`，按 `created_at` 字段筛选。

现有代码线索：

```text
后端已有疑似接口：DELETE /update-history/clear-range-admin
前端已有疑似脚本：02_前端页面\history-danger-zone.js
页面引用位置：02_前端页面\history.html
```

下一次正式开发时不要从零重写，先检查这些现有实现，再按最小范围修复、完善和测试。

---

## 九、每次修改完成后的记录模板

以后每次完成任务，都按下面模板追加到本文档末尾。

```markdown
## YYYY-MM-DD 本次修改记录

### 本次完成

写清楚完成了什么功能。

### 修改文件

逐个列出文件路径和修改内容。

### 新增接口

列出接口路径、请求方式、请求体、返回值、权限要求。

### 前端变化

说明新增了哪些按钮、表单、交互、提示。

### 后端变化

说明新增了哪些模型、接口、校验、数据库操作。

### 测试步骤

1. 如何启动后端。
2. 打开哪个页面。
3. 登录什么角色。
4. 输入什么测试数据。
5. 点击什么按钮。
6. 预期结果是什么。
7. 失败时应该把什么报错发回来。

### 当前测试状态

已测试通过 / 未测试 / 部分通过 / 有遗留问题。

### 下次继续建议

写清楚下一次应该从哪里继续。
```

---

## 十、给未来新 ChatGPT / Codex 的第一句话

可以复制下面这段话给新对话、新账号或新的 Codex 任务：

```text
请先阅读项目根目录里的《项目交接与续接总文档.md》或《小谷围岛广州大学城真实生活信息共建系统_续接文档.md》。这是项目的唯一交接依据。请不要依赖旧聊天记忆。阅读后先总结当前系统状态，再根据我提出的新需求做最小范围修改。
```

---

## 2026-06-06 本次修改记录

### 本次完成

整理并新建 ChatGPT 与 Codex 通用的唯一交接文档，保留原续接文档不覆盖，并在原续接文档顶部追加优先阅读提示。

### 修改文件

```text
项目交接与续接总文档.md
新增总交接文档，整理项目基本信息、结构、接口、权限规则、已完成功能、待做清单、下一步功能要求和后续记录模板。

小谷围岛广州大学城真实生活信息共建系统_续接文档.md
仅在文档开头追加提示：以后优先阅读《项目交接与续接总文档.md》。
```

### 新增接口

无。本次只整理文档，没有新增或修改接口。

### 前端变化

无。本次没有修改前端功能代码。

### 后端变化

无。本次没有修改后端功能代码。

### 测试步骤

1. 打开项目根目录。
2. 确认存在 `项目交接与续接总文档.md`。
3. 打开原续接文档 `小谷围岛广州大学城真实生活信息共建系统_续接文档.md`。
4. 确认原文档开头有“以后优先阅读《项目交接与续接总文档.md》”提示。
5. 确认没有启动后端、没有修改功能代码。

### 当前测试状态

未测试功能；已完成文档整理。

### 下次继续建议

下一次正式开发“按时间区间选择性清空更新历史”时，先核对现有 `/update-history/clear-range-admin` 和 `history-danger-zone.js`，再做最小范围修复和测试。

## 2026-06-06 本次修改记录

### 本次完成

修复项目根目录已有的 `打开后端.bat`。该脚本用于 Windows 双击启动：先打开前端首页 `02_前端页面\index.html`，再进入后端目录启动 FastAPI 服务。

### 修改文件

```text
打开后端.bat
修复为纯英文 echo、相对路径定位、无完整中文绝对路径的启动脚本；使用 %~dp0 作为项目根目录，自动查找 01_* 下的 app.py 和 02_* 下的 index.html。

项目交接与续接总文档.md
追加本次启动 bat 修复记录、启动步骤、失败排查信息和下次继续建议。
```

### 新增接口

无。本次只修复 Windows 启动脚本，没有修改后端接口。

### 前端变化

无。本次没有修改前端业务文件。bat 只会在启动时打开前端首页 `index.html`。

### 后端变化

无。本次没有修改 `app.py`、`models.py`、`database.py` 或其他后端业务代码。bat 启动命令为：

```bat
py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
```

如果 `py` 命令失败，会继续尝试：

```bat
python -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
```

### 启动步骤

1. 在项目根目录双击 `打开后端.bat`。
2. 脚本先以自身所在目录 `%~dp0` 作为项目根目录。
3. 脚本查找 `01_*` 目录下的 `app.py`，作为后端目录。
4. 脚本查找 `02_*` 目录下的 `index.html`，找到则自动打开前端首页。
5. 脚本进入后端目录，优先用 `py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000` 启动。
6. 如果 `py` 启动失败，脚本自动尝试 `python -m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
7. 后端启动后可打开 `http://127.0.0.1:8000/docs` 检查接口文档。

### 测试步骤

1. 确认 `打开后端.bat` 位于项目根目录。
2. 双击 `打开后端.bat`。
3. 观察是否自动打开 `02_前端页面\index.html`。
4. 观察命令行窗口是否显示 `Uvicorn running on http://127.0.0.1:8000` 或类似启动成功信息。
5. 浏览器打开 `http://127.0.0.1:8000/docs`，确认能看到 FastAPI 接口文档。

### 启动失败时应查看的报错

1. 如果提示 `Cannot find backend app.py`，检查 bat 是否在项目根目录，以及 `01_后端服务\app.py` 是否存在。
2. 如果提示 `Cannot find frontend index.html`，检查 `02_前端页面\index.html` 是否存在；该 warning 不会中断后端启动。
3. 如果 `py` 和 `python` 都失败，查看是否安装 Python、是否安装 `uvicorn` 和 `fastapi`。
4. 如果出现 `ModuleNotFoundError`，把缺少的模块名发回来。
5. 如果出现 `Address already in use` 或端口占用，说明 `127.0.0.1:8000` 已被其他进程占用。
6. 如果出现中文乱码或类似 `'锘緻echo' 不是内部或外部命令`，说明 bat 编码或文件开头仍有异常，需要把命令行完整报错发回来。

### 当前测试状态

已完成文件检查和脚本语法检查；未在本次任务中长期启动后端服务，避免留下运行中的服务进程。

### 下次继续建议

下次继续功能开发前，先双击 `打开后端.bat` 验证后端可启动，再打开 `http://127.0.0.1:8000/docs` 和前端首页确认基础运行状态。后续功能仍建议按总文档待办，从“按时间区间选择性清空更新历史”或“后端接口真正权限校验”中选择一个做最小范围开发。

## 2026-06-06 本次修改记录

### 本次完成

完成“按时间区间选择性清空更新历史”功能包的核对和修复。复用现有 `/update-history/clear-range-admin` 接口和 `history-danger-zone.js` 危险操作区，没有重复新增一套功能。

该功能允许管理员在更新历史中心选择开始日期和结束日期，只删除 `update_histories` 表中 `created_at` 位于该日期范围内的记录，不删除 `clues`、`verified_items`、`map_points`、`route_plans`、`feedbacks`、`crawl_targets`、`auth_users` 等正式数据。

### 修改文件

```text
01_后端服务/app.py
修复 DELETE /update-history/clear-range-admin：请求字段提供默认值以便返回更明确错误；强制 start_date/end_date 使用 YYYY-MM-DD；将开始日期转换为当天 00:00:00，将结束日期转换为当天 23:59:59.999999；继续复用 require_admin_user 和 verify_system_password。

02_前端页面/history-danger-zone.js
完善“按时间区间清空更新历史”交互：日期、系统密码、确认文字输入变化时清除提示；前端空日期、日期倒置、空密码、确认文字错误给出明确提示；后端错误优先显示 detail；成功后提示“已清空 X 条更新历史”，清空系统密码和确认文字，并刷新历史列表。

02_前端页面/history.html
将 history-danger-zone.js 引用版本从 v=47 更新为 v=48，避免浏览器继续使用旧缓存。
```

### 新增/修复接口

修复现有接口，没有新增新路径：

```text
DELETE /update-history/clear-range-admin
```

请求体：

```json
{
  "token": "登录后的用户 token",
  "system_password": "xgw2026",
  "confirm_text": "清空历史",
  "start_date": "YYYY-MM-DD",
  "end_date": "YYYY-MM-DD"
}
```

权限和校验：

```text
必须有有效 token。
token 对应用户必须存在且 role 为 admin。
system_password 必须等于 xgw2026。
confirm_text 必须等于 清空历史。
start_date/end_date 必须非空且格式为 YYYY-MM-DD。
开始日期不能晚于结束日期。
```

返回：

```json
{
  "message": "指定时间区间内的更新历史已清空",
  "deleted_count": 0,
  "operator": "管理员账号",
  "start_date": "YYYY-MM-DD",
  "end_date": "YYYY-MM-DD"
}
```

### 前端变化

更新历史中心的管理员危险操作区已有“按时间区间清空更新历史”区域，本次补齐交互：

1. 开始日期 `input type="date"`。
2. 结束日期 `input type="date"`。
3. 系统密码 `input type="password"`。
4. 确认文字输入框，提示必须输入：`清空历史`。
5. 按钮：`清空所选区间历史`。
6. 结果提示区域显示明确错误或成功信息。
7. 提交前进行两次 `confirm`。
8. 成功后清空系统密码和确认文字，并刷新历史列表。

普通用户不应看到该区域；若普通用户绕过前端直接请求接口，后端也会拒绝。

### 后端变化

没有新增数据库表、没有修改模型、没有修改正式数据表。后端只修复区间清空接口：

1. 使用 `require_admin_user(db, token)` 校验管理员。
2. 使用 `verify_system_password(system_password)` 校验系统密码。
3. 校验确认文字必须为 `清空历史`。
4. 严格校验日期格式为 `YYYY-MM-DD`。
5. `start_date` 按当天 `00:00:00` 处理。
6. `end_date` 按当天 `23:59:59.999999` 处理。
7. 只对 `UpdateHistory` / `update_histories` 执行删除。
8. 返回 `deleted_count`。

### 测试步骤

1. 启动后端：在项目根目录双击 `打开后端.bat`，或进入 `01_后端服务` 后运行 `py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
2. 打开前端页面：`02_前端页面/history.html`。
3. 管理员测试：登录管理员账号，进入更新历史中心，确认能看到“管理员危险操作”和“按时间区间清空更新历史”。
4. 正常区间测试：选择有历史记录的开始日期和结束日期，输入系统密码 `xgw2026`，确认文字输入 `清空历史`，点击“清空所选区间历史”，两次确认后应提示“已清空 X 条更新历史”，历史列表刷新。
5. 普通用户测试：退出管理员，登录普通用户，打开 `history.html`，不应看到危险操作区；若用接口工具直接请求 `/update-history/clear-range-admin`，应返回非管理员拒绝。
6. 输错密码测试：管理员登录后输入错误系统密码，应提示“系统密码错误，禁止执行危险操作”。
7. 确认文字错误测试：确认文字不等于 `清空历史`，前端应提示“确认文字不正确，请输入：清空历史”。
8. 日期为空测试：不选开始日期或结束日期，应提示“请选择开始日期和结束日期”。
9. 日期范围错误测试：开始日期晚于结束日期，应提示“开始日期不能晚于结束日期”。
10. 日期格式错误测试：如果绕过前端直接传非 `YYYY-MM-DD` 格式，应返回“日期格式错误，请使用 YYYY-MM-DD”。

### 当前测试状态

已测试通过。

代码检查：

```text
C:\Users\86138\miniconda3\python.exe -m py_compile app.py
通过。

Get-Content -Raw history-danger-zone.js | node --check -
通过。
```

用户已完成手动功能测试，结果如下：

1. 后端可以正常启动。
2. `/docs` 中能看到 `DELETE /update-history/clear-range-admin`。
3. 管理员使用未来日期 `2099-01-01` 到 `2099-01-02` 测试，成功返回“已清空 0 条更新历史”。
4. 密码错误、确认文字错误、日期倒置等错误提示正常。
5. 普通用户权限测试正常。
6. 没有发现正式数据被误删。

### 下次继续建议

下次如果该功能测试通过，可继续做“后端接口真正权限校验”，优先核对审核、采集、数据管理等管理类接口是否仍只依赖前端隐藏。继续保持最小范围修改，不要重构项目。

## 2026-06-06 本次修改记录

### 本次功能名称

后端接口真正权限校验（第一期）

### 本次完成

完成第一期后端权限加固。重点保护管理类、危险类、修改类接口，复用现有 `AuthUser.session_token`、`role == "admin"`、`get_user_by_token`、`require_admin_user`、`verify_system_password` 等机制。

本期不改数据库结构，不删除功能，不做真实删除、真实清空、真实导入测试。公开浏览、提交线索、提交反馈、登录注册、用户资料编辑继续保持可用。

### 只读审计分类

公开接口，普通用户或未登录用户可以使用：

```text
GET  /
GET  /health
GET  /db/status
GET  /scheduler/status
GET  /clues
GET  /clues/{clue_id}
POST /clues
GET  /verified-items
GET  /verified-items/{item_id}
GET  /search
GET  /feedbacks
GET  /feedbacks/stats
POST /feedbacks
POST /feedbacks-v2
GET  /map-points
GET  /map-points/{point_id}
GET  /routes
GET  /routes/{route_id}
POST /update-history
GET  /update-history
POST /auth/register
POST /auth/register-v2
POST /auth/login
POST /auth/me
POST /auth/logout
```

登录用户接口，普通用户登录后可以使用：

```text
PATCH /auth/profile
PATCH /auth/profile-v2
```

管理员接口，本期已加后端 admin 校验：

```text
POST   /scheduler/test-run
POST   /crawler/targets
PATCH  /crawler/targets/{target_id}
POST   /crawler/targets/{target_id}/toggle
DELETE /crawler/targets/{target_id}
POST   /crawler/public-url
POST   /verified-items
POST   /admin/clues/{clue_id}/approve
PATCH  /clues/{clue_id}/status
PATCH  /clues/{clue_id}
DELETE /clues/{clue_id}
POST   /clues/{clue_id}/archive
POST   /clues/{clue_id}/restore
PATCH  /verified-items/{item_id}
DELETE /verified-items/{item_id}
POST   /verified-items/{item_id}/archive
POST   /verified-items/{item_id}/restore
DELETE /feedbacks/{feedback_id}
POST   /map-points
PATCH  /map-points/{point_id}
POST   /map-points/{point_id}/archive
POST   /map-points/{point_id}/restore
DELETE /map-points/{point_id}
POST   /routes
PATCH  /routes/{route_id}
POST   /routes/{route_id}/archive
POST   /routes/{route_id}/restore
DELETE /routes/{route_id}
PATCH  /detail-edit/clues/{clue_id}
PATCH  /detail-edit/verified-items/{item_id}
DELETE /update-history/clear-all
DELETE /update-history/clear-all-admin
DELETE /update-history/clear-all-admin-v2
DELETE /update-history/clear-range-admin
GET    /backup/export
POST   /backup/import
GET    /backup/import-code
POST   /backup/import-v2
POST   /backup/import-v3
```

### 修改文件

```text
01_后端服务/app.py
新增 AdminTokenRequest 和 require_admin_token 小 helper；复用 require_admin_user 校验管理员。为第一期高风险接口补 token 字段或 token query，并在接口入口调用管理员校验。优化未登录/非管理员错误提示。

02_前端页面/app.js
审核中心、采集管理相关写请求补 token，包括审核通过、线索状态修改、删除线索/真实库、采集目标新增/切换/删除、手动触发采集。

02_前端页面/manage.js
数据管理页编辑、归档、恢复、删除线索和真实库时补 token。

02_前端页面/map.js
地图中心新增、编辑、归档、恢复、删除地图点时补 token。

02_前端页面/route.js
路线中心新增、编辑、归档、恢复、删除路线时补 token。

02_前端页面/detail.js
线索/真实库详情页审核、状态修改、归档、恢复、删除时补 token。

02_前端页面/map-route-detail.js
地图点/路线详情页归档、恢复、删除时补 token。

02_前端页面/map-route-detail-edit.js
地图点/路线详情页保存编辑时补 token。

02_前端页面/clue-item-detail-edit.js
线索/真实库详情页保存编辑时补 token。

02_前端页面/backup.js
备份导出请求补 token。

02_前端页面/backup-import.js
备份导入验证码请求补 token；导入 v3 已有 token，继续保留。

02_前端页面/feedback-admin-v2.js
反馈中心 V2 删除反馈请求补 token，避免加载时序导致旧请求绕过。

02_前端页面/feedback-admin-final.js
反馈中心最终版删除反馈请求补 token。

02_前端页面/feedback.js
线索/真实库详情页删除反馈请求补 token。

02_前端页面/map-route-feedback.js
地图点/路线详情页删除反馈请求补 token。

02_前端页面/submit-action-fix.js
该脚本会覆盖采集目标、地图点、路线创建函数，本次补 token，避免覆盖其他脚本中的 token 修改。

多个 HTML 页面
更新直接引用脚本的版本号，避免浏览器缓存旧 JS。
```

未修改：

```text
01_后端服务/models.py
01_后端服务/database.py
02_前端页面/global-loader.js
```

### 新增或复用的权限 helper

复用：

```text
get_user_by_token(db, token)
require_admin_user(db, token)
verify_system_password(system_password)
AuthUser.session_token
AuthUser.role == "admin"
```

新增小 helper：

```python
class AdminTokenRequest(BaseModel):
    token: str = ""

def require_admin_token(db: Session, token: Optional[str]):
    return require_admin_user(db, token or "")
```

错误提示：

```text
未登录或 token 无效：请先登录 / 请先登录，登录状态无效或已过期
非管理员：无权限操作，只有管理员可以执行此操作
```

### 接口权限表

```text
GET  /clues                         公开       不需要系统密码
GET  /clues/{id}                    公开       不需要系统密码
POST /clues                         公开       不需要系统密码
GET  /verified-items                公开       不需要系统密码
GET  /verified-items/{id}           公开       不需要系统密码
GET  /map-points                    公开       不需要系统密码
GET  /map-points/{id}               公开       不需要系统密码
GET  /routes                        公开       不需要系统密码
GET  /routes/{id}                   公开       不需要系统密码
POST /feedbacks                     公开       不需要系统密码
POST /feedbacks-v2                  公开       不需要系统密码
PATCH /auth/profile                 登录用户   不需要系统密码
PATCH /auth/profile-v2              登录用户   不需要系统密码

POST   /scheduler/test-run                  管理员   不需要系统密码
POST   /crawler/targets                     管理员   不需要系统密码
PATCH  /crawler/targets/{id}                管理员   不需要系统密码
POST   /crawler/targets/{id}/toggle         管理员   不需要系统密码
DELETE /crawler/targets/{id}                管理员   不需要系统密码
POST   /crawler/public-url                  管理员   不需要系统密码
POST   /verified-items                      管理员   不需要系统密码
POST   /admin/clues/{id}/approve            管理员   不需要系统密码
PATCH  /clues/{id}/status                   管理员   不需要系统密码
PATCH  /clues/{id}                          管理员   不需要系统密码
DELETE /clues/{id}                          管理员   不需要系统密码
POST   /clues/{id}/archive                  管理员   不需要系统密码
POST   /clues/{id}/restore                  管理员   不需要系统密码
PATCH  /verified-items/{id}                 管理员   不需要系统密码
DELETE /verified-items/{id}                 管理员   不需要系统密码
POST   /verified-items/{id}/archive         管理员   不需要系统密码
POST   /verified-items/{id}/restore         管理员   不需要系统密码
DELETE /feedbacks/{id}                      管理员   不需要系统密码
POST   /map-points                          管理员   不需要系统密码
PATCH  /map-points/{id}                     管理员   不需要系统密码
POST   /map-points/{id}/archive             管理员   不需要系统密码
POST   /map-points/{id}/restore             管理员   不需要系统密码
DELETE /map-points/{id}                     管理员   不需要系统密码
POST   /routes                              管理员   不需要系统密码
PATCH  /routes/{id}                         管理员   不需要系统密码
POST   /routes/{id}/archive                 管理员   不需要系统密码
POST   /routes/{id}/restore                 管理员   不需要系统密码
DELETE /routes/{id}                         管理员   不需要系统密码
PATCH  /detail-edit/clues/{id}              管理员   不需要系统密码
PATCH  /detail-edit/verified-items/{id}     管理员   不需要系统密码
GET    /backup/export                       管理员   不需要系统密码
GET    /backup/import-code                  管理员   不需要系统密码
POST   /backup/import                       管理员   旧接口仍需要 password 或 system_password
POST   /backup/import-v2                    管理员   旧接口仍需要旧 password + 验证码
POST   /backup/import-v3                    管理员   需要系统密码 + 验证码
DELETE /update-history/clear-all            管理员   旧接口仍需要 password + 确认文字
DELETE /update-history/clear-all-admin      管理员   需要管理员账号密码 + 确认文字
DELETE /update-history/clear-all-admin-v2   管理员   需要系统密码 + 确认文字
DELETE /update-history/clear-range-admin    管理员   需要系统密码 + 确认文字
```

### 前端变化

补 token 的页面/脚本：

```text
app.js
manage.js
map.js
route.js
detail.js
map-route-detail.js
map-route-detail-edit.js
clue-item-detail-edit.js
backup.js
backup-import.js
feedback-admin-v2.js
feedback-admin-final.js
submit-action-fix.js
```

统一使用已有 token key：

```text
xgw_user_token
```

更新版本号：

```text
admin.html: app.js?v=9
crawler.html: app.js?v=9, submit-action-fix.js?v=10
manage.html: manage.js?v=2
map.html: map.js?v=9, submit-action-fix.js?v=10
route.html: route.js?v=10, submit-action-fix.js?v=10
clue-detail.html / item-detail.html: detail.js?v=9, clue-item-detail-edit.js?v=23, submit-action-fix.js?v=10
map-detail.html / route-detail.html: map-route-detail.js?v=21, map-route-detail-edit.js?v=22
backup.html: backup.js?v=32, backup-import.js?v=51
feedback-admin.html: feedback-admin-v2.js?v=24, feedback-admin-final.js?v=25
submit.html: submit-action-fix.js?v=10
clue-detail.html / item-detail.html: feedback.js?v=9
map-detail.html / route-detail.html: map-route-feedback.js?v=24
```

没有新增全局 JS，没有修改 `global-loader.js`。

### 测试步骤

1. 启动后端：双击项目根目录 `打开后端.bat`，或在 `01_后端服务` 运行 `py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
2. 打开 `http://127.0.0.1:8000/docs`，确认接口仍存在。
3. 管理员测试：登录管理员账号，分别打开审核中心、数据管理、采集管理、备份、反馈中心、地图中心、路线中心，验证页面加载正常。
4. 管理员安全测试：不要真实删除数据；可用错误密码、空 token、未来日期 0 条影响等方式测试拒绝和 0 影响路径。
5. 普通用户测试：登录普通用户，确认管理页仍被前端权限守卫拦截；如果直接请求本期加固接口，应返回 401 或 403。
6. 错误 token 测试：在接口工具中传空 token 或伪造 token 请求 `GET /backup/export?token=bad`、`DELETE /feedbacks/1?token=bad`，应返回“请先登录”类错误，不应执行操作。
7. 公开功能回归：未登录或普通用户打开首页、列表、详情页，提交线索、提交反馈，应继续可用。

安全测试提醒：

```text
不要真实删除线索、真实库、地图点、路线、反馈、用户。
不要真实清空历史。
不要真实导入备份。
不要修改真实用户密码。
```

### 当前测试状态

已完成静态检查：

```text
C:\Users\86138\miniconda3\python.exe -m py_compile app.py
通过。

Get-Content -Raw <修改过的 JS> | node --check -
全部通过。
```

未完成后端导入/启动检查：

```text
C:\Users\86138\miniconda3\python.exe -c "import app"
失败原因：当前 Miniconda 环境缺少 fastapi。
```

说明：这不是代码语法错误；实际项目后端可能使用另一个已安装依赖的 Python 环境启动。待人工用 `打开后端.bat` 或原后端环境做浏览器测试。

### 第二期待做

1. 核对 `detail.js` 中“用户核验”按钮是否应继续允许普通用户修改线索状态。本期将 `PATCH /clues/{id}/status` 归为管理员写接口；如果要保留普通用户核验，应下一期新增独立、安全的普通用户核验接口，而不是复用管理状态修改接口。
2. 核对未被当前主要页面引用的旧脚本：`feedback-admin.js`、`feedback.js`、`map-route-feedback.js`、`detail.js` 中旧管理入口是否仍有页面可触达；本期已加固后端，即使旧脚本不传 token 也会被拒绝。
3. 数据看板统计接口目前仍复用公开 GET 数据源，前端已有管理员权限守卫；如需进一步加固，可新增专用 dashboard 后端接口并要求 admin。
4. `/update-history` 的创建接口仍公开，用于前端自动记录历史；后续如要防滥用，可设计服务端内部记录机制或登录用户限制。
5. 继续梳理审核、采集、数据管理页面的普通用户体验，避免按钮闪现后再被后端拒绝。

## 2026-06-06 本次修改记录

### 本次功能名称

后端接口真正权限校验（第一期）非破坏性自检 + 测试准备

### 本次完成

本次没有继续开发新功能，没有修改业务逻辑。已确认上一任务“后端接口真正权限校验（第一期）”已经记录到本文档中，且记录包含：

```text
修改文件清单
加固接口清单 / 接口权限表
测试状态
第二期待做
手动测试步骤
```

本次新增人工测试清单和安全测试 bat，方便后续人工测试时按步骤执行。

### 修改文件

```text
权限加固第一期_人工测试清单.md
新增人工测试清单，包含测试前准备、公开功能测试、管理员页面测试、管理员低风险操作测试、普通用户权限测试、bad token 测试、失败信息收集和禁止测试事项。

权限加固_安全测试命令.bat
新增非破坏性 CMD 测试脚本，只使用 bad token 和未来日期测试权限拒绝；不写真实管理员 token，不删除数据，不导入备份，不修改数据库。

项目交接与续接总文档.md
追加本次非破坏性自检和测试准备记录。
```

### 业务代码变化

无。本次没有修改：

```text
01_后端服务/app.py
01_后端服务/models.py
01_后端服务/database.py
02_前端页面/*.js
02_前端页面/*.html
02_前端页面/global-loader.js
```

### 安全测试 bat 内容说明

`权限加固_安全测试命令.bat` 只测试：

```text
GET    /backup/export?token=bad
DELETE /update-history/clear-range-admin
```

其中区间清空请求使用：

```text
token=bad
system_password=xgw2026
confirm_text=清空历史
start_date=2099-01-01
end_date=2099-01-02
```

预期返回：

```text
401 或 403
```

不应返回备份 JSON，也不应删除任何更新历史。

### 测试准备状态

已完成测试准备，待人工浏览器测试。

下一次继续时，优先执行：

```text
权限加固第一期_人工测试清单.md
权限加固_安全测试命令.bat
```

不要直接继续新功能；先确认“后端接口真正权限校验（第一期）”人工测试通过。

### 下次继续建议

1. 先关闭旧后端。
2. 双击 `打开后端.bat`。
3. 打开 `http://127.0.0.1:8000/docs`。
4. 按 `权限加固第一期_人工测试清单.md` 执行测试。
5. 可先运行 `权限加固_安全测试命令.bat` 做 bad token 非破坏性测试。
6. 测试期间不要真实删除、真实清空、真实导入、修改密码或改数据库。

## 2026-06-06 本次修改记录

### 本次功能名称

权限加固安全测试 bat 修复

### 本次完成

只修复 `权限加固_安全测试命令.bat` 的第二条安全测试命令，没有修改后端代码、前端业务 JS 或任何功能逻辑。

### 问题记录

运行原 `权限加固_安全测试命令.bat` 时：

```text
GET /backup/export?token=bad
返回 401，通过。

DELETE /update-history/clear-range-admin with bad token
原 bat 中 PowerShell 构造 confirm_text 失败，报错：
值不能为 null。参数名: argument
导致接口返回 422。
```

用户手动运行修正版命令后，`DELETE /update-history/clear-range-admin` 使用 `token=bad` 和未来日期返回：

```text
STATUS: 401
```

说明后端权限拦截通过。

### 修改文件

```text
权限加固_安全测试命令.bat
将第二条测试里的 confirm_text 构造方式改为稳定写法：
$confirm=([string][char]28165)+([string][char]31354)+([string][char]21382)+([string][char]21490)
```

### 当前测试状态

待重新运行 `权限加固_安全测试命令.bat` 确认脚本整体输出。已知用户手动修正版命令验证过后端返回 401。

### 下次继续建议

下次继续时先运行修复后的 `权限加固_安全测试命令.bat`。预期两条测试都返回 401 或 403，且不返回备份 JSON、不删除任何更新历史。

## 2026-06-06 本次修改记录

### 本次功能名称

普通用户无权限页请求日志清理 + 上下箭头切换选项快捷键

### 本次完成

修复普通用户访问管理页面时，“无权限访问”页面上方反复显示 `/auth/me` 请求日志的问题。请求诊断信息默认只保留在浏览器 Console，不再追加到页面 DOM；管理员权限守卫在 token 未变化且角色已确认时复用已知角色，避免无权限页面持续重复请求 `/auth/me`。

完善全局快捷键：当焦点在 `select` 或 `radio` 上时，按 `ArrowUp` / `ArrowDown` 可以切换上一个或下一个选项，并触发 `change` 事件。普通文本输入框和 `textarea` 不拦截上下箭头，原有 Enter / Y / N 弹窗快捷键逻辑保持不变。

### 修改文件清单

```text
02_前端页面/request-timeout-fix.js
默认不再把请求诊断日志写入页面 DOM；如确实需要页面内诊断框，可手动设置 window.XGW_SHOW_REQUEST_DEBUG_BOX = true。

02_前端页面/admin-permission-guard.js
token 未变化且角色已确认时不再重复请求 /auth/me，只重新应用当前权限状态。

02_前端页面/modal-shortcuts.js
升级为 V3，新增 select/radio 的 ArrowUp / ArrowDown 选项切换逻辑。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 52 更新到 53。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=52 同步为 v=53，确保浏览器加载最新全局脚本。
```

### 当前测试状态

已完成 JS 语法检查：

```text
node --check 02_前端页面/request-timeout-fix.js
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查结果通过。本次没有修改后端代码、数据库结构或后端权限逻辑。

### 手动测试建议

1. 关闭旧后端后双击 `打开后端.bat`。
2. 使用普通用户登录，依次打开 `admin.html`、`manage.html`、`crawler.html`、`backup.html`、`feedback-admin.html`、`dashboard.html`。
3. 预期只看到干净的“无权限访问”提示，页面正文不再出现“正在请求 / 请求完成 / auth/me”日志。
4. 使用管理员登录，确认上述管理页面仍能正常进入。
5. 在任意包含 `select` 的页面中聚焦下拉框，按上下箭头确认选项会切换并触发页面原有联动。
6. 在任意同组 `radio` 上聚焦，按上下箭头确认可切换同组单选项。
7. 在普通文本输入框和 `textarea` 中按上下箭头，预期仍是默认光标或滚动行为，不应强制切换选项。

### 下次继续建议

继续完成“后端接口真正权限校验（第一期）”人工测试。若发现某个页面仍有请求日志残留，请记录页面名称、当前登录角色、浏览器 Console、页面正文截图和具体请求 URL。

## 2026-06-06 本次修改记录

### 本次功能名称

初始化本地 Git 仓库，建立安全版本回退点

### 本次完成

已在项目根目录初始化本地 Git 仓库，用于本机版本回退和后续变更追踪。本次不上传 GitHub，不移动文件，不重构目录，不删除文件，不修改业务代码。

### .gitignore 重要规则

已新增项目根目录 `.gitignore`，重点忽略：

```text
Python 缓存：__pycache__/、*.pyc、*.pyo
虚拟环境：.venv/、venv/、env/
数据库和本地运行数据：*.db、*.sqlite、*.sqlite3、xiaoguwei.db
备份和压缩包：*.zip、xgw_backup_*.json、xgw_before_auth_test_*.zip
环境密钥：.env、.env.*
系统文件：.DS_Store、Thumbs.db
编辑器个人状态：.idea/workspace.xml、.idea/tasks.xml、.idea/usage.statistics.xml、.idea/shelf/
```

数据库文件 `01_后端服务/xiaoguwei.db` 不纳入 Git，避免提交本地测试数据、用户数据或运行态数据。

### 当前提交状态

```text
chore: initialize project git checkpoint after permission hardening
```

当前提交尚未完成。原因是本机 Git 未配置 `user.name` 和 `user.email`，Git 拒绝创建提交。当前项目文件已执行 `git add .` 暂存，待用户配置真实提交身份后，可继续执行同一条 `git commit` 命令完成本地回退点。

### 后续 Git 建议

1. 以后每次 Codex 修改前，建议先运行 `git status` 查看当前工作区状态。
2. 修改完成并测试后，建议及时 `git add .` 和 `git commit` 建立回退点。
3. 如果要做大规模文件夹整理、批量重命名或跨模块重构，应先新建分支，或至少先提交当前稳定状态。
4. 数据库、备份包、虚拟环境、缓存和环境密钥继续保持不提交。

## 2026-06-06 项目目录细分整理方案（暂未执行）

### 当前状态

本次只做方案记录，没有移动文件，没有修改代码，没有修改 HTML 引用。

当前前端目录 `02_前端页面` 采用平铺结构：HTML、CSS、页面脚本、全局脚本、智能填充脚本、工具脚本都放在同一层。随着功能增加，文件数量已经较多，后续适合逐步细分。

当前后端目录 `01_后端服务` 主要文件：

```text
app.py
models.py
database.py
crawler_service.py
scheduler.py
xiaoguwei.db
.venv/
__pycache__/
```

其中 `xiaoguwei.db`、`.venv/`、`__pycache__/` 已由 `.gitignore` 忽略，不应纳入 Git。

### 建议目标目录结构

前端建议目标结构：

```text
02_前端页面/
  pages/
    index.html
    submit.html
    admin.html
    manage.html
    crawler.html
    backup.html
    dashboard.html
    feedback-admin.html
    history.html
    map.html
    route.html
    clue-detail.html
    item-detail.html
    map-detail.html
    route-detail.html

  css/
    style.css

  js/
    global/
      global-loader.js
      page-scope-cleanup.js
      user-widget.js
      user-widget-v2.js
      modal-shortcuts.js
      password-eye.js
      user-message-clear.js
      user-avatar-click-upload.js
      danger-message-clear.js
      admin-permission-guard.js

    common/
      app.js
      archive-filter.js
      detail-link.js
      detail.js
      feedback.js
      request-timeout-fix.js

    history/
      history.js
      history-auto.js
      history-badge.js
      history-danger-zone.js
      history-filter-v2.js
      history-inline.js

    admin/
      manage.js
      manage-filter.js
      dashboard.js
      dashboard-v2.js
      dashboard-feedback.js

    backup/
      backup.js
      backup-import.js
      backup-import-layout.js

    feedback/
      feedback-admin.js
      feedback-admin-v2.js
      feedback-admin-final.js
      feedback-history-link.js

    map-route/
      map.js
      route.js
      map-route-detail.js
      map-route-detail-edit.js
      map-route-detail-link.js
      map-route-feedback.js
      map-smart-fill.js
      route-smart-fill.js
      route-smart-fix.js
      route-load-fix.js
      route-ui-refactor.js

    smart-fill/
      smart-core.js
      generic-smart-fill.js
      generic-smart-fix.js
      smart-progress.js
      smart-progress-cleanup.js
      smart-edit-panel.js
      smart-edit-hook.js
      smart-form-mode.js
      submit-action-fix.js
      submit-success-reload.js
      clue-item-detail-edit.js

  tools/
    add-global-loader-to-html.py
    cleanup-global-scripts.py

  assets/
    （后续图片、图标、导入模板等静态资源放这里）
```

后端建议目标结构，放在后续阶段，不建议现在立即执行：

```text
01_后端服务/
  app.py                  （第一阶段暂时保留，避免破坏 uvicorn app:app 启动命令）
  database.py             （第一阶段暂时保留）
  models.py               （第一阶段暂时保留）
  crawler_service.py      （后续可迁入 app/services/）
  scheduler.py            （后续可迁入 app/services/ 或 app/jobs/）

  app/
    routers/
      auth.py
      clues.py
      admin.py
      history.py
      backup.py
      feedback.py
      map_route.py
      crawler.py
    services/
      crawler_service.py
      backup_service.py
      permission_service.py
      history_service.py
    schemas/
      auth.py
      admin.py
      history.py
      backup.py
    models/
      （仅当决定拆分 SQLAlchemy 模型时再启用）

  data/
    （本地数据库或运行态数据，仅本地使用，不提交 Git）

  scripts/
    （维护脚本、初始化脚本、非破坏性测试脚本）
```

后端当前 `app.py` 较大，但它直接承载 FastAPI 实例和大量接口。后端拆分风险高于前端静态文件整理，建议等权限加固第一期人工测试通过并完成 Git 提交后，再单独开任务处理。

### 全局脚本识别

当前由 `global-loader.js` 自动加载的全局脚本：

```text
page-scope-cleanup.js
user-widget.js
user-widget-v2.js
modal-shortcuts.js
password-eye.js
user-message-clear.js
user-avatar-click-upload.js
danger-message-clear.js
admin-permission-guard.js
```

这些脚本适合迁入：

```text
02_前端页面/js/global/
```

移动后必须同步修改 `global-loader.js` 中的加载路径。例如：

```text
原：page-scope-cleanup.js
新：js/global/page-scope-cleanup.js
```

如果 `global-loader.js` 自身也移动到 `js/global/global-loader.js`，所有 HTML 中的引用也要从：

```text
global-loader.js?v=53
```

改为：

```text
js/global/global-loader.js?v=新版本号
```

并继续更新 `GLOBAL_SCRIPT_VERSION`，避免浏览器缓存旧脚本。

### 页面专属脚本识别

按当前 HTML 直接引用关系，页面专属或页面组脚本建议归类如下。

首页、审核、采集、提交相关：

```text
index.html:
app.js, archive-filter.js, detail-link.js, history-badge.js, history-auto.js, global-loader.js

admin.html:
app.js, archive-filter.js, detail-link.js, history-auto.js, history-badge.js, global-loader.js

crawler.html:
app.js, generic-smart-fill.js, smart-progress.js, generic-smart-fix.js, submit-action-fix.js,
request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js, submit-success-reload.js,
smart-form-mode.js, smart-progress-cleanup.js, history-auto.js, global-loader.js

submit.html:
app.js, generic-smart-fill.js, smart-progress.js, generic-smart-fix.js, submit-action-fix.js,
request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js, submit-success-reload.js,
smart-form-mode.js, smart-progress-cleanup.js, history-auto.js, global-loader.js
```

备份页：

```text
backup.html:
backup.js, backup-import.js, backup-import-layout.js, global-loader.js
```

历史页：

```text
history.html:
history.js, history-filter-v2.js, history-danger-zone.js, global-loader.js
```

管理和看板页：

```text
manage.html:
manage.js, manage-filter.js, detail-link.js, history-auto.js, history-badge.js, global-loader.js

dashboard.html:
history-auto.js, dashboard-v2.js, global-loader.js
```

反馈管理页：

```text
feedback-admin.html:
feedback-admin.js, history-auto.js, history-badge.js, feedback-admin-v2.js,
feedback-admin-final.js, global-loader.js
```

线索/真实库详情页：

```text
clue-detail.html / item-detail.html:
detail.js, feedback.js, generic-smart-fill.js, smart-progress.js, generic-smart-fix.js,
submit-action-fix.js, request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js,
submit-success-reload.js, smart-form-mode.js, smart-progress-cleanup.js, history-auto.js,
history-badge.js, history-inline.js, clue-item-detail-edit.js, feedback-history-link.js,
global-loader.js
```

地图和路线页：

```text
map.html:
map.js, smart-core.js, map-smart-fill.js, smart-progress.js, submit-action-fix.js,
request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js, submit-success-reload.js,
smart-form-mode.js, smart-progress-cleanup.js, history-auto.js, history-badge.js,
map-route-detail-link.js, global-loader.js

route.html:
route.js, route-smart-fill.js, smart-progress.js, route-smart-fix.js, submit-action-fix.js,
request-timeout-fix.js, route-load-fix.js, smart-edit-panel.js, smart-edit-hook.js,
submit-success-reload.js, route-ui-refactor.js, smart-form-mode.js,
smart-progress-cleanup.js, history-auto.js, history-badge.js, map-route-detail-link.js,
global-loader.js

map-detail.html / route-detail.html:
map-route-detail.js, history-auto.js, map-route-detail-edit.js,
map-route-feedback.js, feedback-history-link.js, global-loader.js
```

### 移动后需要同步修改的引用

前端每移动一类文件，都必须同步修改：

```text
1. 对应 HTML 的 <script src="..."> 路径。
2. 对应 HTML 的 <link rel="stylesheet" href="..."> 路径。
3. global-loader.js 内 globalScripts 的路径。
4. global-loader.js 的 GLOBAL_SCRIPT_VERSION。
5. 所有 HTML 中 global-loader.js 的版本号。
6. 如果脚本内部有动态加载、跳转或资源路径，也要逐项搜索确认。
```

当前 HTML 全部直接引用 `style.css`，如果移动到 `css/style.css`，所有 HTML 都要改为：

```text
css/style.css
```

如果 HTML 自身移动到 `pages/`，路径会反过来变成：

```text
../css/style.css
../js/...
```

因此第一阶段不建议移动 HTML 页面，只先移动少量 JS 或工具脚本，降低路径改动范围。

### 暂时不建议移动的文件

第一阶段不建议移动：

```text
02_前端页面/*.html
02_前端页面/style.css
02_前端页面/global-loader.js
01_后端服务/app.py
01_后端服务/models.py
01_后端服务/database.py
打开后端.bat
权限加固_安全测试命令.bat
权限加固第一期_人工测试清单.md
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

原因：

```text
HTML 和 style.css 路径影响所有页面。
global-loader.js 是全局脚本入口，移动它会影响所有 HTML。
app.py 是 uvicorn app:app 的启动入口，移动或拆分会影响后端启动方式。
models.py / database.py 被 app.py 直接使用，后端拆分需要单独测试。
bat 和文档位于根目录，便于非技术使用者双击启动和续接。
```

### 分阶段整理方案

第 0 阶段：准备回退点

```text
1. 先配置 Git user.name / user.email。
2. 完成当前首次 commit。
3. 运行 git status，确认工作区干净。
4. 新建分支，例如 git switch -c chore/frontend-folder-split-phase-1。
```

如果工作区不干净，不开始移动文件。

第 1 阶段：只移动工具脚本，风险最低

建议移动：

```text
02_前端页面/add-global-loader-to-html.py -> 02_前端页面/tools/add-global-loader-to-html.py
02_前端页面/cleanup-global-scripts.py -> 02_前端页面/tools/cleanup-global-scripts.py
```

这两个脚本不是页面运行时引用，移动后不需要修改 HTML。测试只需确认文件存在、后续维护脚本路径记录清楚。

第 2 阶段：移动全局脚本，但暂不移动 global-loader.js

建议移动：

```text
page-scope-cleanup.js
user-widget.js
user-widget-v2.js
modal-shortcuts.js
password-eye.js
user-message-clear.js
user-avatar-click-upload.js
danger-message-clear.js
admin-permission-guard.js
```

迁入：

```text
02_前端页面/js/global/
```

同步修改：

```text
global-loader.js 内 globalScripts 路径改为 js/global/xxx.js。
GLOBAL_SCRIPT_VERSION +1。
所有 HTML 中 global-loader.js?v=版本号同步更新。
```

测试重点：

```text
用户登录/退出/头像/用户菜单正常。
普通用户访问管理页仍显示无权限。
管理员访问管理页正常。
弹窗 Enter/Y/N 和 select/radio 上下键正常。
密码眼睛、消息清理、危险操作提示清理正常。
```

第 3 阶段：移动历史、备份、反馈等边界清晰的页面组脚本

优先移动：

```text
js/history/
js/backup/
js/feedback/
```

对应更新：

```text
history.html
backup.html
feedback-admin.html
clue-detail.html
item-detail.html
map-detail.html
route-detail.html
```

测试重点：

```text
history.html 历史列表、筛选、危险操作区。
backup.html 导出、导入区域显示和 bad token 安全测试。
feedback-admin.html 反馈列表、管理员处理状态。
详情页反馈提交和反馈历史入口。
```

第 4 阶段：移动地图/路线和智能填充脚本

建议迁移：

```text
js/map-route/
js/smart-fill/
```

这组脚本互相引用关系和页面交互较多，风险高于历史/备份/反馈。必须逐页测试：

```text
map.html
route.html
map-detail.html
route-detail.html
submit.html
crawler.html
clue-detail.html
item-detail.html
```

第 5 阶段：移动 HTML 到 pages/，移动 CSS 到 css/

这是影响最大的阶段，建议最后执行。移动后几乎所有相对路径都要调整：

```text
pages/index.html 引用 ../css/style.css
pages/index.html 引用 ../js/...
页面之间 location.href / a href 也可能要从 index.html 改为 ../pages/index.html 或同目录相对路径。
打开后端.bat 自动打开首页路径也要从 02_前端页面/index.html 改为 02_前端页面/pages/index.html。
```

不建议在权限加固人工测试完成前执行。

第 6 阶段：后端拆分

后端拆分应单独开任务。建议先从服务层开始，不先改启动入口：

```text
先保留 01_后端服务/app.py。
逐步把 crawler_service.py、scheduler.py 或备份/历史 helper 抽到 app/services/。
最后再把路由拆到 app/routers/。
```

每拆一个接口组，都要跑：

```text
python -m py_compile app.py models.py database.py
py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
打开 /docs
做对应页面的非破坏性测试
```

### 风险点

```text
1. 静态 HTML 使用相对路径，移动 HTML 会导致 CSS/JS 路径失效。
2. global-loader.js 负责全局脚本加载，路径改错会影响所有页面的登录组件、权限守卫和快捷键。
3. 多个页面复用同一脚本，例如 app.js、history-auto.js、history-badge.js、feedback-history-link.js，不能按单一页面理解。
4. 部分脚本有版本号参数，迁移时必须同步更新版本号避免缓存。
5. 后端 app.py 是启动入口，贸然拆分会影响 `py -m uvicorn app:app`。
6. 当前 Git 首次 commit 尚未完成，未建立可用 commit hash 前不适合开始大规模移动。
```

### 测试步骤

每一阶段移动后至少测试：

```text
1. git status，确认只出现本阶段计划内文件移动和引用修改。
2. 打开后端.bat 启动后端。
3. 打开 http://127.0.0.1:8000/docs，确认后端可用。
4. 打开 02_前端页面/index.html，确认首页加载 CSS 和 JS。
5. 浏览器 Network 确认没有 404 的 JS/CSS。
6. 普通用户登录、退出、提交线索、提交反馈。
7. 管理员打开 admin/manage/crawler/backup/feedback-admin/dashboard/history/map/route。
8. 普通用户访问管理页确认仍被拦截。
9. 运行权限加固安全测试 bat，只做 bad token / 未来日期非破坏性测试。
```

### Git 回退方式

开始整理前必须先完成 Git 提交。当前文档记录显示首次 commit 仍因未配置 `user.name` / `user.email` 未完成，因此下一步应先完成：

```text
git config user.name "你的名字"
git config user.email "你的邮箱"
git commit -m "chore: initialize project git checkpoint after permission hardening"
git status
```

如果已经提交并新建分支，移动失败时可用：

```text
git status
git restore .
```

如果某些移动已经暂存：

```text
git restore --staged .
git restore .
```

如果已经产生了一次错误提交，但还没有继续开发，可先看日志：

```text
git log --oneline -5
```

再决定是否用 `git revert <commit>` 做反向提交。除非非常明确当前分支只有本地错误整理提交，否则不要轻易使用 `git reset --hard`。

### 第一阶段建议结论

最安全的第一阶段只移动：

```text
02_前端页面/tools/add-global-loader-to-html.py
02_前端页面/tools/cleanup-global-scripts.py
```

其次再移动全局脚本到 `02_前端页面/js/global/`，但必须同步修改 `global-loader.js` 路径和版本号。HTML、CSS、后端入口文件和 bat 暂时不要移动。

## 2026-06-06 本次修改记录

### 本次功能名称

完成本地 Git 首次提交

### 本次完成

本次只处理本地 Git 首次提交，不做功能开发，不移动文件，不删除文件，不上传 GitHub。

已在当前项目仓库内设置本地 Git 用户信息：

```text
user.name = 86138
user.email = 86138@example.local
```

该邮箱仅作为本地 Git 占位邮箱使用；如果后续要上传 GitHub，可再改为 GitHub 邮箱或 noreply 邮箱。

### .gitignore 检查

已确认 `.gitignore` 包含本地运行数据和敏感/临时文件忽略规则：

```text
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.db
*.sqlite
*.sqlite3
xiaoguwei.db
*.zip
xgw_backup_*.json
xgw_before_auth_test_*.zip
.env
.env.*
```

数据库 `01_后端服务/xiaoguwei.db` 不纳入 Git。

### 提交信息

```text
chore: initialize project checkpoint before directory refactor
```

### 后续建议

后续每次修改前先运行 `git status`，确认工作区状态；修改后完成测试再提交。若要执行目录整理，应先确认当前提交存在，再新建分支或至少保持当前稳定提交可回退。

## 2026-06-06 本次修改记录

### 本次功能名称

表单字段上下箭头键盘切换增强

### 本次完成

本次只修改前端键盘交互，没有修改后端、数据库或业务接口。继续复用已有 `modal-shortcuts.js`，未新增全局 JS 文件。

新增表单字段上下箭头切换能力：当焦点在常见单行输入框上时，`ArrowDown` 聚焦下一个可填写字段，`ArrowUp` 聚焦上一个可填写字段。字段顺序按页面 DOM 自然顺序计算，跳过 disabled、readonly、hidden、不可见元素和按钮类输入。

### select 优先级规则

`select` 下拉框优先处理内部选项：

```text
1. select 获得焦点时，默认进入“先切换 option”状态。
2. ArrowUp / ArrowDown 先切换 select 内部 option，并触发 change 事件。
3. 到达边界时不越界，也不会跳到其他字段。
4. 按 Enter 后，给当前 select 设置临时确认状态：data-xgw-select-confirmed="1"。
5. select 被 Enter 确认后，再按 ArrowDown / ArrowUp 才跳到下一个或上一个字段。
6. select blur 或重新 focus 后会清除确认状态，重新回到“先切换 option”的状态。
```

### textarea 规则

`textarea` 普通 `ArrowUp` / `ArrowDown` 保留浏览器原生行为，避免用户编辑多行说明时误跳字段。

如需从 `textarea` 跳字段：

```text
Ctrl + ArrowDown 或 Alt + ArrowDown：跳到下一个可填写字段
Ctrl + ArrowUp 或 Alt + ArrowUp：跳到上一个可填写字段
```

### radio / checkbox 规则

`radio` 仍然优先在同 name 单选组内切换：

```text
ArrowDown：切换到同组下一个 radio
ArrowUp：切换到同组上一个 radio
切换后 checked = true，并触发 change 事件
到边界时不越界，不跳字段
```

`checkbox` 可参与字段上下切换，但不改变勾选状态，不影响点击逻辑。

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V4，新增表单字段 ArrowUp / ArrowDown 导航；保留 Enter / Y / N 弹窗快捷键；保留 select/radio 内部切换优先级。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 53 更新到 54。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=53 同步为 v=54，确保浏览器加载最新全局脚本。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

建议重点测试：

```text
submit.html：单行输入框 ArrowDown 跳到下一个字段，ArrowUp 回到上一个字段。
route.html：路线名称、路线类型/分类、起点/区域等字段按 DOM 顺序切换。
map.html：地图点表单字段可上下切换。
crawler.html：采集目标表单字段可上下切换。
backup.html / history.html / feedback-admin.html / admin.html / manage.html：页面加载不报错。
```

重点验证：

```text
1. select 上 ArrowDown 先切换 option，不直接跳字段。
2. select 按 Enter 后，再 ArrowDown 才跳到下一个字段。
3. textarea 中普通 ArrowUp / ArrowDown 不跳字段。
4. textarea 中 Ctrl+ArrowUp / Ctrl+ArrowDown 可以跳字段。
5. radio 组中 ArrowUp / ArrowDown 只切换同组 radio。
6. button、submit、reset、hidden 不参与字段切换。
7. 弹窗 Enter / Y / N 快捷键仍正常；输入框里的 Y/N 不误触发弹窗。
```

### 当前测试状态

已测试通过。

用户已完成浏览器测试，结果如下：

```text
1. select 按 ArrowDown 可以展开并选择。
2. 按 Enter 后下拉框收起。
3. 再按 ArrowDown 可以跳到下一个填写框，不再继续改动当前 select。
4. textarea 边界跳转没有发现异常。
5. 普通 input 上下切换没有发现异常。
```

## 2026-06-06 本次修改记录

### 本次功能名称

select confirmed 状态改为 Enter + change 双来源

### 本次完成

本次只修复前端 select 键盘交互 bug，没有修改后端、数据库或业务接口。继续复用 `modal-shortcuts.js`，未新增 JS。

### 修复原因

上一版主要依赖 `keydown Enter` 设置 `data-xgw-select-confirmed="1"`。在原生 select 下拉框展开后，部分浏览器会由原生下拉控件处理 Enter，页面脚本不一定稳定收到该 Enter。因此确认状态不能只依赖 Enter。

### 当前 confirmed 设置来源

```text
1. select keydown Enter：
   设置 data-xgw-select-confirmed="1"，并 preventDefault / stopPropagation / stopImmediatePropagation。

2. select change：
   设置 data-xgw-select-confirmed="1"。
   这是原生下拉框展开后，用户用键盘选择 option 并按 Enter 收起时更可靠的确认来源。
```

### ArrowUp / ArrowDown 处理顺序

select 上按 `ArrowUp` / `ArrowDown` 时：

```text
1. 第一优先级检查 data-xgw-select-confirmed 是否为 "1"。
2. 如果已经 confirmed：
   - preventDefault()
   - stopPropagation()
   - stopImmediatePropagation()
   - 直接跳到上一个/下一个可填写字段
   - 不切换 option
   - 不调用 showPicker()
   - 不调用 click()
3. 只有未 confirmed 时，才尝试展开 select 或 fallback 手动切换 option。
```

### showPicker 处理

```text
1. 如果 select.showPicker() 存在并调用成功，本次 ArrowUp / ArrowDown 只负责展开下拉框。
2. showPicker 成功时不再手动修改 selectedIndex。
3. 后续用户通过原生下拉框选择 option 后，由 change 事件设置 confirmed。
4. 如果 showPicker 不支持或调用失败，才进入 fallback：尝试 click()，并手动切换 selectedIndex、触发 change。
```

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V7，select confirmed 状态由 Enter + change 双来源设置；confirmed 状态下箭头事件会被完全消费并直接跳字段；showPicker 成功时不再手动改 option。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 56 更新到 57。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=56 同步为 v=57。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

真实流程重点测试：

```text
1. 打开 submit.html。
2. 聚焦分类 select。
3. 按 ArrowDown，预期下拉框展开或进入 option 选择，不跳字段。
4. 用键盘选择一个 option。
5. 按 Enter，预期下拉框收回，并通过 change 或 Enter 标记 confirmed。
6. 再按 ArrowDown，预期跳到下一个填写框，不再改变当前 select 的 option。
7. 再聚焦这个 select，预期重新进入选择状态，ArrowDown 又先展开/选择 option。
8. route.html、map.html 的 select 重复测试。
9. textarea 首尾边界跳字段、中间行原生上下移动继续正常。
10. 普通 input 上下键切换字段继续正常。
11. radio 上下键继续只切换同组 radio。
12. 弹窗 Enter / Y / N 快捷键继续正常；输入框中的 y/n 不误触发弹窗。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

## 2026-06-06 本次修改记录

### 本次功能名称

修复 select Enter 确认后 ArrowUp / ArrowDown 未跳字段问题

### 本次完成

本次只修复前端键盘交互 bug，没有修改后端、数据库或业务接口。继续复用 `modal-shortcuts.js`，未新增 JS。

### 修复内容

修复前现象：

```text
select 聚焦后 ArrowDown 可以展开并切换 option。
按 Enter 后下拉框收回。
再次按 ArrowDown 时仍然继续改变 select option，没有跳到下一个填写框。
```

修复后逻辑：

```text
1. select 按 Enter 时，设置 data-xgw-select-confirmed="1"。
2. Enter 事件会 preventDefault 和 stopPropagation，避免提交表单或触发弹窗确认。
3. select 处于 confirmed 状态时，ArrowUp / ArrowDown 先判断 confirmed。
4. confirmed 状态下 ArrowUp / ArrowDown 直接跳到上一个/下一个可填写字段。
5. confirmed 状态下不再切换当前 select 的 option。
6. confirmed 状态下不再调用 showPicker() 或 click()。
7. 跳字段时会 preventDefault 和 stopPropagation，避免浏览器继续处理原生 select。
8. select blur 后延迟清理 confirmed 状态；如果只是原生下拉框收起导致临时 blur，但焦点仍在当前 select，则保留 confirmed。
9. select 重新获得焦点时清除旧 confirmed，重新进入“先切换 option”的状态。
```

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V6，调整 select confirmed 判断顺序，确认后箭头直接跳字段，并消费键盘事件。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 55 更新到 56。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=55 同步为 v=56。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

重点测试：

```text
1. submit.html 中聚焦分类/平台等 select。
2. 按 ArrowDown，确认先展开/切换 option，不跳字段。
3. 按 Enter，确认当前 select。
4. 再按 ArrowDown，确认跳到下一个可填写字段，当前 select option 不再变化。
5. 再回到该 select，按 ArrowDown，确认重新进入先切换 option 的状态。
6. route.html 中测试路线类型/分类 select。
7. map.html 中测试分类 select。
8. textarea 第一行开头 ArrowUp、最后一行末尾 ArrowDown、中间行上下移动继续正常。
9. 普通 input ArrowUp / ArrowDown 仍能切换字段。
10. radio 组 ArrowUp / ArrowDown 仍只切换同组 radio。
11. 弹窗 Enter / Y / N 快捷键仍正常；输入框中的 y/n 不误触发弹窗。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

### 下次继续建议

下次优先在 `submit.html`、`route.html`、`map.html`、`crawler.html` 做键盘实测。如果发现某个字段顺序不符合预期，先记录页面、字段名称和 DOM 顺序，再决定是否给该页面做局部 `tabindex` 或更细的字段选择规则。

## 2026-06-06 本次修改记录

### 本次功能名称

表单键盘交互小修补：select 尝试展开 + textarea 边界跳字段

### 本次完成

本次只修改前端键盘交互，没有修改后端、数据库或业务接口。继续复用 `modal-shortcuts.js`，未新增 JS。

### select 展开兼容逻辑

当焦点在 `select` 上，用户按 `ArrowUp` / `ArrowDown` 时，仍然优先切换下拉框内部 option，并触发 `change`。同时新增“尽量展开下拉框”的兼容尝试：

```text
1. 每次 select 获得焦点后，只尝试展开一次，避免反复闪烁或重复报错。
2. 优先调用 select.showPicker()。
3. 如果浏览器不支持 showPicker，则尝试 select.click()。
4. 如果浏览器安全策略不允许脚本展开原生 select，捕获异常，只保留 option 切换逻辑。
5. select blur 或重新 focus 时清除本次尝试状态。
```

兼容性说明：原生 `select` 是否能被 JavaScript 展开取决于浏览器和安全策略。支持时会尽量展开，不支持时不会报错，仍可用上下键切换 option。

### textarea 边界跳字段

`textarea` 普通 `ArrowUp` / `ArrowDown` 默认仍保留原生多行光标移动。

新增边界跳转：

```text
1. selectionStart === 0 且 selectionEnd === 0 时，按 ArrowUp 跳到上一个可填写字段。
2. selectionStart === value.length 且 selectionEnd === value.length 时，按 ArrowDown 跳到下一个可填写字段。
3. 如果用户选中了文字，即 selectionStart !== selectionEnd，不跳字段，保留原生行为。
4. 光标在中间行或非边界位置时，不跳字段。
5. Ctrl+ArrowUp / Ctrl+ArrowDown 或 Alt+ArrowUp / Alt+ArrowDown 仍保留为强制跳字段。
```

跳字段时会 `preventDefault`，避免页面滚动。

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V5，增加 select 尝试展开逻辑，增加 textarea 首尾边界普通上下键跳字段逻辑。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 54 更新到 55。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=54 同步为 v=55。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

建议测试：

```text
1. submit.html / route.html / map.html / crawler.html 中聚焦 select，按 ArrowDown，确认优先切换 option。
2. 在支持 showPicker 的浏览器中，观察 select 是否尽量展开；不支持时确认没有报错且 option 仍会切换。
3. select 按 Enter 确认后，再按 ArrowDown / ArrowUp，确认可跳到下一个/上一个字段。
4. textarea 光标在中间行时，普通 ArrowUp / ArrowDown 只移动文本光标。
5. textarea 光标在第一行最前时，ArrowUp 跳到上一个字段。
6. textarea 光标在最后一行最后时，ArrowDown 跳到下一个字段。
7. textarea 有选中文本时，ArrowUp / ArrowDown 不跳字段。
8. Ctrl+ArrowUp / Ctrl+ArrowDown 仍可强制跳字段。
9. history.html / backup.html / feedback-admin.html / admin.html / manage.html 页面加载不报错。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

## 2026-06-06 前端权限入口显示最小修复

### 本次完成内容

本次是在回退 public-nav 全局导航方案后采用的最小修复策略。不新增 `public-nav.js`，不全局重建标题栏，不隐藏旧 header 按钮，不大规模修改 HTML 结构。

修复目标：

```text
1. 首页已有的 history.html / 更新历史入口保持公开可见，未登录用户、普通用户、管理员都可以看到。
2. 管理采集目标、采集管理、审核中心、数据管理、数据看板、备份、反馈管理等管理入口只对管理员显示。
3. 后端、数据库、页面结构均未修改。
```

### 前端权限显示规则

公开入口：`history.html` 明确排除在管理员入口隐藏逻辑之外，因此“历史中心 / 更新历史”不会被 `admin-permission-guard.js` 当作管理员入口隐藏。

管理入口：权限守卫现在同时根据按钮目标和按钮文字识别管理入口。目标页面包括：

```text
admin.html
manage.html
crawler.html
dashboard.html
backup.html
feedback-admin.html
```

只要按钮或链接的 `href`、`onclick`、`data-nav-target` 指向以上管理页面，未登录用户和普通用户会隐藏，管理员可见。

### 修改文件清单

```text
02_前端页面/admin-permission-guard.js
最小修复管理入口识别逻辑：显式排除 history.html；新增基于 href / onclick / data-nav-target 的管理页面目标判断；补充管理文字名单。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 57 更新到 58。

02_前端页面/*.html
仅机械同步 global-loader.js 引用版本，从 v=57 更新到 v=58；未改 HTML 结构。

项目交接与续接总文档.md
追加本次记录。
```

### 检查结果

已完成：

```text
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/global-loader.js
```

检查通过。已确认没有新增 `02_前端页面/public-nav.js`。已确认没有修改 `01_后端服务` 和数据库文件。未执行 git commit。

### 人工测试步骤

未登录测试：

```text
1. 打开 index.html。
2. 应能看到“更新历史 / 历史中心”入口。
3. 不应看到“管理采集目标 / 采集管理 / 审核中心 / 数据管理 / 数据看板 / 备份 / 反馈管理”。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 index.html。
3. 应能看到“更新历史 / 历史中心”入口。
4. 不应看到管理入口。
5. 直接打开 crawler.html / manage.html / admin.html / backup.html / dashboard.html / feedback-admin.html，应显示无权限访问。
```

管理员测试：

```text
1. 登录管理员。
2. 打开 index.html。
3. 应能看到历史入口和管理入口。
4. 管理页能正常打开。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

## 2026-06-08 头像菜单增加切换账号

### 本次完成

本次只修改前端用户头像菜单和登录状态刷新逻辑，没有修改后端、数据库、顶部标题栏结构，也没有新增 `public-nav.js`。

已登录用户点击右上角头像后，菜单中在“退出登录”上方新增“切换账号”按钮。点击后会清除当前本地登录 token 和用户缓存，触发全局登录状态变化事件 `xgw-auth-changed`，并打开现有登录弹窗。

### 修改文件

```text
02_前端页面/user-widget.js
新增“切换账号”菜单按钮；新增 clearUserWidgetAuthState、notifyUserWidgetAuthChanged、switchAccount；登录、注册、保存资料、退出登录后触发 xgw-auth-changed；组件监听 xgw-auth-changed 后刷新头像、用户名、角色和菜单。

02_前端页面/user-widget-v2.js
V2 注册和保存资料成功后触发 xgw-auth-changed，确保权限入口和用户显示及时刷新。

02_前端页面/admin-permission-guard.js
监听 xgw-auth-changed，登录状态变化后立即重新检查权限；无 token 时仍直接判定 guest，不请求 /auth/me。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 59 更新到 60。

02_前端页面/*.html
仅机械同步 global-loader.js 引用版本，从 v=59 更新到 v=60，未改 HTML 结构。

项目交接与续接总文档.md
追加本次修改记录。
```

### 新增接口

无。本次没有新增或修改后端接口。

### 前端变化

```text
1. 已登录用户菜单保留“退出登录”，新增“切换账号”。
2. 切换账号不会跳转页面，会打开现有登录弹窗。
3. 切换账号会清除 localStorage 中的 xgw_user_token。
4. 切换账号会清空前端 currentUser 和 editingAvatarBase64。
5. 登录状态变化统一触发 xgw-auth-changed。
6. 权限守卫收到 xgw-auth-changed 后立即刷新管理员入口和无权限页。
```

### 后端变化

无。没有修改 `01_后端服务`，没有修改数据库文件。

### 测试步骤

```text
1. 登录管理员，点击头像，应看到“切换账号”和“退出登录”。
2. 点击“切换账号”，应清除当前账号并打开登录入口。
3. 登录普通用户后，头像和权限显示应立即变成普通用户。
4. 普通用户不应看到管理入口。
5. 再切换回管理员后，管理员入口应恢复。
6. 未登录状态打开管理页，应显示未登录用户无权限。
7. 普通用户打开管理页，应显示普通用户无权限。
8. 不应出现 /auth/me 刷屏；无 token 状态下不应反复请求 /auth/me。
```

### 当前测试状态

已完成 JS 语法检查：

```text
node --check 02_前端页面/user-widget.js
node --check 02_前端页面/user-widget-v2.js
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/global-loader.js
```

检查通过。已确认没有新增 `02_前端页面/public-nav.js`，没有修改后端和数据库。

人工浏览器测试已通过，结果如下：

```text
1. 管理员点击头像后，可以看到“切换账号”和“退出登录”。
2. 点击“切换账号”后，会清除当前账号并打开登录入口。
3. 登录普通用户后，头像、用户名、角色和权限显示能立即更新，不需要手动刷新。
4. 普通用户看不到管理入口。
5. 再切换回管理员后，管理员入口能恢复。
6. 未登录访问管理页显示未登录用户无权限。
7. 普通用户访问管理页显示普通用户无权限。
8. 没有出现 /auth/me 刷屏。
```

当前测试状态：已测试通过。

### 下次继续建议

本功能已通过人工测试，可提交本次前端改动。后续如果继续完善用户系统，可考虑把登录态事件、用户缓存和权限守卫抽成更明确的前端小工具，但本次不做导航重构和新功能包。
## 2026-06-06 修复无权限页未登录/普通用户身份提示不一致

### 本次完成内容

本次只修复前端无权限页身份判断和文案显示，不修改后端、不修改数据库、不新增 `public-nav.js`，不重建顶部导航，不提交 Git。

修复点：

```text
1. 没有 token 时，admin-permission-guard.js 直接判定为 guest，不请求 /auth/me。
2. token 存在但 /auth/me 返回 401/403 时，清除 xgw_user_token，触发 xgw-auth-changed，并按未登录用户显示。
3. 只有 /auth/me 成功返回用户且 role 不是 admin 时，才按普通用户显示无权限页。
4. 管理员 role === admin 时正常进入管理页。
5. 无权限页增加 permissionRole 标记，身份变化时允许重绘，避免旧的“普通用户”文案覆盖“未登录用户”。
6. dashboard-v2.js 的页面专属权限提示也改为 guest/user/admin 三态判断，避免未登录访问 dashboard.html 后被写成普通用户提示。
```

### 修改文件清单

```text
02_前端页面/admin-permission-guard.js
修复 token 无效时清理登录状态；无权限页按当前 role 重绘；触发 xgw-auth-changed 让顶部头像状态保持一致。

02_前端页面/dashboard-v2.js
将数据看板权限判断从 boolean 改为 guest/user/admin；未登录不请求 /auth/me；401/403 清 token；无权限文案显示当前身份。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 58 更新到 59。

02_前端页面/*.html
仅机械同步 global-loader.js 引用版本，从 v=58 更新到 v=59；dashboard.html 中 dashboard-v2.js 从 v=53 更新到 v=54。

项目交接与续接总文档.md
追加本次记录。
```

### 测试步骤

未登录访问管理页：

```text
1. 退出登录或清除 xgw_user_token。
2. 打开 dashboard.html / admin.html / manage.html。
3. 应一直显示“当前身份是：未登录用户”。
4. 不应一闪后变成“普通用户”。
5. 顶部头像显示未登录。
6. 不应反复请求 /auth/me。
```

普通用户访问管理页：

```text
1. 登录普通用户。
2. 打开 dashboard.html / admin.html / manage.html。
3. 应显示“当前身份是：普通用户”。
4. 顶部头像显示普通用户。
5. 不显示管理员内容。
```

管理员访问管理页：

```text
1. 登录管理员。
2. 打开 dashboard.html / admin.html / manage.html。
3. 页面正常进入，不显示无权限页。
```

切换账号测试：

```text
1. 管理员切换到普通用户，再打开管理页，应显示普通用户无权限。
2. 退出登录后打开管理页，应显示未登录用户无权限。
3. 不需要手动刷新才能修正身份。
```

### 当前测试状态

已完成 JS 语法检查：

```text
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/dashboard-v2.js
node --check 02_前端页面/global-loader.js
```

检查通过。已确认没有新增 `public-nav.js`，没有修改后端和数据库，未执行 git commit。当前待人工浏览器测试。

## 2026-06-08 权限加固第一期收尾测试记录

### 本次完成

本次只更新文档测试状态，没有修改业务代码、后端代码、数据库或文件位置。当前最新稳定提交点为：

```text
5ada1141a0eaa0a0a7062465818f341d61926850
```

“后端接口真正权限校验（第一期）”已完成一轮人工收尾测试，测试结论为：已测试通过。

### 修改文件

```text
项目交接与续接总文档.md
补充权限加固第一期人工收尾测试范围、测试通过结果、禁止事项和下一步建议。
```

### 本次测试范围

```text
1. 未登录用户打开公开页面：
   index.html
   submit.html
   map.html
   route.html
   history.html

2. 普通用户使用公开功能：
   查看首页
   查看地图中心
   查看路线中心
   查看历史中心
   提交线索
   提交反馈

3. 普通用户访问管理页面：
   admin.html
   manage.html
   crawler.html
   backup.html
   dashboard.html
   feedback-admin.html

4. 未登录用户访问管理页面。

5. 管理员访问管理页面：
   admin.html
   manage.html
   crawler.html
   backup.html
   dashboard.html
   feedback-admin.html
   history.html

6. bad token 非破坏性安全测试：
   GET /backup/export?token=bad
   DELETE /update-history/clear-range-admin 使用 bad token 和未来日期

7. 普通用户无权限页请求日志检查。

8. 右上角头像菜单切换账号功能回归。
```

### 测试通过结果

```text
1. 未登录用户可以正常打开公开页面。
2. 普通用户可以正常使用公开功能。
3. 普通用户打开管理页面时显示无权限访问，不显示管理员内容。
4. 未登录用户打开管理页面时显示未登录用户无权限。
5. 管理员可以正常打开管理页面。
6. GET /backup/export?token=bad 返回 401。
7. DELETE /update-history/clear-range-admin 使用 bad token 和未来日期返回 401。
8. 普通用户无权限页不再刷 /auth/me 日志。
9. 右上角头像菜单切换账号功能正常。
```

### 禁止事项

权限加固第一期测试通过后，后续继续保持非破坏性测试原则：

```text
1. 禁止用真实数据做真实删除测试。
2. 禁止做真实备份导入测试。
3. 禁止真实清空更新历史或正式数据。
4. 禁止为验证权限而修改数据库。
```

### 当前测试状态

已测试通过。权限加固第一期可以作为稳定基础进入下一阶段。

### 下次继续建议

下一步可以开始考虑小范围目录整理第一阶段，或继续做管理员用户管理页。若做目录整理，必须继续保持小步、可回退、先提交稳定点；若做管理员用户管理页，应优先明确后端权限校验和禁止越权修改规则。

## 2026-06-08 项目目录整理第一阶段

### 本次完成

本次完成“项目目录整理第一阶段：只移动低风险工具脚本”。没有修改后端、数据库、HTML 页面结构、业务 JS、CSS、`global-loader.js` 或 `GLOBAL_SCRIPT_VERSION`，没有新增 `public-nav.js`，没有做新功能。

本次只处理两个前端维护工具脚本，并创建工具目录：

```text
02_前端页面/tools/
```

### 修改文件

```text
02_前端页面/add-global-loader-to-html.py
通过 git mv 移动到 02_前端页面/tools/add-global-loader-to-html.py。

02_前端页面/cleanup-global-scripts.py
通过 git mv 移动到 02_前端页面/tools/cleanup-global-scripts.py。

02_前端页面/tools/add-global-loader-to-html.py
移动后将 HTML 扫描目录适配为父目录 02_前端页面，避免脚本只扫描 tools 目录；同步更新脚本顶部用法注释。

02_前端页面/tools/cleanup-global-scripts.py
移动后将 HTML 扫描目录适配为父目录 02_前端页面，避免脚本只扫描 tools 目录。

项目交接与续接总文档.md
更新工具脚本新路径，追加本次整理记录。

小谷围岛广州大学城真实生活信息共建系统_续接文档.md
更新工具脚本新路径和运行命令。
```

### 前端变化

无页面功能变化。没有移动 HTML、业务 JS、CSS；没有修改任何 HTML 的 `<script>` 引用；没有修改 `global-loader.js`。

### 后端变化

无。没有修改 `01_后端服务`，没有修改数据库文件。

### 测试步骤

```text
1. 运行 git status，确认仅出现两个工具脚本移动、两个工具脚本路径适配和必要文档更新。
2. 确认 02_前端页面/tools/add-global-loader-to-html.py 存在。
3. 确认 02_前端页面/tools/cleanup-global-scripts.py 存在。
4. 全局搜索 add-global-loader-to-html.py 和 cleanup-global-scripts.py，确认旧路径说明已更新为 tools/ 下的新路径。
5. 确认没有修改 01_后端服务。
6. 确认没有修改数据库文件。
7. 确认没有修改 HTML 页面结构和 HTML script 引用。
8. 确认没有修改业务 JS、CSS、global-loader.js 或 GLOBAL_SCRIPT_VERSION。
```

### 当前测试状态

已完成文件移动、路径引用检查和静态状态检查；待人工确认目录整理结果。未执行 Git 提交。

### 下次继续建议

测试确认无误后提交 Git。第二阶段再考虑整理更多前端脚本，但必须另开任务，不能一次性大搬家；移动任何业务 JS 前必须先明确页面引用、`global-loader.js` 加载路径和回退方案。

## 2026-06-08 统一顶部标题栏按钮

### 本次完成

本次统一各页面顶部标题栏按钮命名和显示顺序，并压缩顶部导航按钮样式，使桌面宽度下尽量一行展示。

本次没有使用 `public-nav.js`，没有新增 `public-nav.js`，没有通过 JS 重建导航栏，没有隐藏旧 header 再生成新导航，没有拦截导航点击，也没有使用 `preventDefault` 阻止跳转。所有顶部按钮继续使用页面内原有的 `button onclick="location.href='...'"` 方式跳转。

### 顶部入口顺序

公开入口固定顺序：

```text
首页 -> index.html
提交线索 -> submit.html
地图中心 -> map.html
路线中心 -> route.html
历史中心 -> history.html
```

管理员入口固定顺序：

```text
审核中心 -> admin.html
数据管理 -> manage.html
采集管理 -> crawler.html
数据看板 -> dashboard.html
备份 -> backup.html
反馈管理 -> feedback-admin.html
```

权限显示继续复用现有 `admin-permission-guard.js`。未登录用户和普通用户不应看到管理员入口，管理员可见。

### 修改文件

```text
02_前端页面/index.html
02_前端页面/submit.html
02_前端页面/map.html
02_前端页面/route.html
02_前端页面/history.html
02_前端页面/admin.html
02_前端页面/manage.html
02_前端页面/crawler.html
02_前端页面/dashboard.html
02_前端页面/backup.html
02_前端页面/feedback-admin.html
02_前端页面/clue-detail.html
02_前端页面/item-detail.html
02_前端页面/map-detail.html
02_前端页面/route-detail.html
统一顶部 header-actions 内按钮名称和顺序。同步将历史页标题改为“历史中心”，备份页标题改为“备份”，管理员反馈页标题改为“反馈管理”，提交页标题改为“提交线索”。首页中原导航性质的“备份导出”入口改为“备份”。

02_前端页面/style.css
只调整顶部标题栏相关样式：缩小 header padding、标题字号、顶部按钮字号、按钮 padding 和按钮间距；header-actions 保持 flex 布局，桌面宽度下尽量不换行。

项目交接与续接总文档.md
追加本次记录。
```

### 未修改内容

```text
没有修改后端。
没有修改数据库。
没有修改 global-loader.js。
没有修改 GLOBAL_SCRIPT_VERSION。
没有修改 admin-permission-guard.js。
没有修改头像菜单、切换账号功能、无权限页逻辑或表单快捷键。
没有移动文件。
没有新增 public-nav.js。
没有做管理员用户管理页。
```

### 检查结果

已完成：

```text
1. 检查 15 个重点页面 header-actions 中的按钮 href 顺序一致。
2. 搜索确认顶部按钮中不再出现“返回首页”“更新历史”“管理采集目标”“提交一条线索”“备份导出”“反馈中心”等旧导航名称。
3. 确认没有修改 01_后端服务。
4. 确认没有修改数据库文件。
5. 确认没有修改 global-loader.js 或其他全局 JS。
6. 确认没有新增 02_前端页面/public-nav.js。
```

本次没有修改 JS，因此没有运行 JS 语法检查。

### 人工测试步骤

未登录测试：

```text
1. 打开 index.html。
2. 顶部公共入口名称应统一：首页、提交线索、地图中心、路线中心、历史中心。
3. 不应看到管理员入口。
4. 不应出现“返回首页”“更新历史”等不统一名称。
5. 顶部按钮应比之前小，尽量一行。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 index.html、submit.html、map.html、route.html、history.html。
3. 顶部公共入口名称和顺序应基本一致。
4. 不应看到管理员入口。
5. 点击每个顶部按钮都能正常跳转。
6. 不应出现乱码。
```

管理员测试：

```text
1. 登录管理员。
2. 打开 index.html、admin.html、manage.html、crawler.html、dashboard.html、backup.html、feedback-admin.html。
3. 管理员应能看到公共入口和管理员入口。
4. 管理员入口名称应统一：审核中心、数据管理、采集管理、数据看板、备份、反馈管理。
5. 点击顶部按钮应能正常跳转。
6. 页面不应乱码。
```

不应影响：

```text
提交按钮、保存按钮、搜索按钮、筛选按钮、删除按钮、清空按钮、用户头像菜单、切换账号功能、无权限页判断、表单上下箭头快捷键。
```

### 当前测试状态

已测试通过。用户已完成人工浏览器测试，确认：

```text
1. 首页、提交线索、地图中心、路线中心、历史中心 5 个公共按钮在各页面顺序一致。
2. 5 个公共按钮的横向起点和相对位置稳定。
3. clue-detail.html、item-detail.html、map-detail.html、route-detail.html 已和普通页面保持同一套顶部导航结构。
4. 用户头像固定在右侧，不再挤压公共按钮。
5. 管理员入口在公共入口后方，顺序正确。
6. 未登录用户和普通用户看不到管理员入口，管理员可以看到管理员入口。
7. 顶部按钮点击能正常跳转。
8. 中文没有乱码，没有 ??。
9. 没有新增 public-nav.js。
10. 没有修改 global-loader.js。
11. 没有修改后端、数据库、业务 JS。
```

### 下次继续建议

先完成人工测试并确认未登录、普通用户、管理员三种状态下顶部导航显示和跳转都正常；测试通过后再提交 Git。不要回到 `public-nav.js` 或全局 JS 重建导航方案。

## 2026-06-08 顶部标题栏布局稳定性修复

### 本次完成

本次在上一轮“统一顶部标题栏按钮”未提交改动基础上继续修复，只处理标题栏布局稳定性，没有做新功能，没有提交 Git。

修复重点：

```text
1. 顶部标题栏固定为三块区域：左侧标题与副标题、中间固定导航、右侧用户头像/登录状态。
2. 公共入口固定为前 5 个按钮，顺序和相对位置不随页面标题长短变化。
3. 管理员入口固定排在公共入口后面；管理员入口隐藏或显示时，不改变公共入口顺序和起点。
4. 详情页 clue-detail.html、item-detail.html、map-detail.html、route-detail.html 继续使用同一套顶部标题栏结构。
```

### 修改文件

```text
02_前端页面/style.css
将 .header-inner 调整为三列 grid：
左列为稳定标题区，中列为 header-actions 导航区，右列为用户头像组件区。
header-actions 使用 justify-content:flex-start，避免因居中或标题长度导致公共按钮漂移。
桌面宽度下尽量不换行，窄屏允许改为单列堆叠。

项目交接与续接总文档.md
追加本次布局稳定性修复记录。
```

### 未修改内容

```text
没有修改后端。
没有修改数据库。
没有新增 public-nav.js。
没有修改 global-loader.js。
没有修改 admin-permission-guard.js。
没有修改业务 JS。
没有修改头像菜单、切换账号功能、无权限页逻辑或表单快捷键。
没有提交 Git。
```

### 检查结果

已完成：

```text
1. 检查 15 个页面 header-actions 按钮 href 顺序完全一致。
2. 搜索确认没有 ??、冲突标记、返回首页、管理采集目标、提交一条线索、更新历史按钮、备份导出按钮、反馈中心按钮等异常残留。
3. 确认没有新增 02_前端页面/public-nav.js。
4. 确认没有修改 global-loader.js。
5. 确认没有修改 01_后端服务。
6. 确认没有修改数据库。
7. 确认没有修改业务 JS。
```

### 人工测试步骤

```text
1. 未登录打开 index.html、submit.html、map.html、route.html、history.html。
2. 确认公共入口始终为：首页、提交线索、地图中心、路线中心、历史中心。
3. 确认公共入口在不同页面之间起点和相对位置稳定，不随页面标题长短漂移。
4. 登录普通用户后重复打开公开页面，确认公共入口位置不变，管理员入口不可见。
5. 登录管理员后打开 index.html、admin.html、manage.html、crawler.html、dashboard.html、backup.html、feedback-admin.html。
6. 确认管理员入口固定排在公共入口后面：审核中心、数据管理、采集管理、数据看板、备份、反馈管理。
7. 打开 clue-detail.html、item-detail.html、map-detail.html、route-detail.html，确认详情页顶部结构一致。
8. 点击顶部按钮确认正常跳转，不出现乱码。
9. 确认页面主体提交、保存、搜索、筛选、删除、清空、头像菜单、切换账号、无权限页、表单上下箭头快捷键不受影响。
```

### 当前测试状态

已完成静态检查，待人工浏览器测试。未执行 Git 提交。

### 下次继续建议

先完成浏览器测试，重点看不同页面之间公共按钮是否仍有水平漂移。测试通过后再提交 Git；不要新增 `public-nav.js`，不要改用全局 JS 重建导航。

## 2026-06-08 顶部公共按钮和用户头像固定布局补充修复

### 本次完成

本次继续在未提交的顶部标题栏统一改动基础上修复布局稳定性，只修改 `style.css` 和本文档，没有提交 Git。

修复点：

```text
1. 标题区、公共导航区、用户头像区改为更明确的三列固定布局。
2. 标题区固定在第一列，标题长短不再推动导航起点。
3. header-actions 固定在第二列并左对齐，公共入口前 5 个按钮起点固定。
4. userWidget 固定在第三列并右对齐，用户头像不再挤压或改变导航起点。
5. 管理员入口继续排在公共入口之后；管理员入口隐藏或显示不改变前 5 个公共按钮的顺序和相对起点。
6. 详情页继续沿用同一套 header-actions 顺序和布局。
```

### 修改文件

```text
02_前端页面/style.css
进一步固定 header 三列布局：
grid-template-columns: 320px minmax(0, 1fr) 170px；
标题区 grid-column:1；
header-actions grid-column:2 且 justify-self:start；
#userWidget grid-column:3 且 justify-self:end；
窄屏时三块区域回到单列排列。

项目交接与续接总文档.md
追加本次补充修复记录。
```

### 检查结果

已完成：

```text
1. 检查 15 个页面 header-actions 按钮 href 顺序完全一致。
2. 检查没有 ??、冲突标记、返回首页、管理采集目标、提交一条线索、更新历史按钮、备份导出按钮、反馈中心按钮等异常残留。
3. 确认没有新增 public-nav.js。
4. 确认没有修改 global-loader.js。
5. 确认没有修改后端。
6. 确认没有修改数据库。
7. 确认没有修改业务 JS。
```

### 测试步骤

```text
1. 分别打开 index.html、submit.html、map.html、route.html、history.html。
2. 对比公共入口：首页、提交线索、地图中心、路线中心、历史中心，确认横向起点一致。
3. 打开 clue-detail.html、item-detail.html、map-detail.html、route-detail.html，确认详情页公共入口起点一致。
4. 未登录和普通用户状态下，确认管理员入口隐藏后公共入口仍保持同一位置。
5. 管理员状态下，确认管理员入口出现在公共入口之后，且公共入口前 5 个按钮位置不变。
6. 确认右上角用户头像固定在右侧，头像菜单和切换账号功能正常。
7. 确认顶部按钮正常跳转，页面无乱码。
```

### 当前测试状态

已完成静态检查，待人工浏览器测试。未执行 Git 提交。

### 下次继续建议

人工测试时重点对比不同页面公共按钮左边缘是否一致。如果仍有轻微漂移，下一步优先考虑给 header 内部标题、导航、用户区增加显式 class，但仍不要新增 `public-nav.js` 或用 JS 重建导航。

## 2026-06-08 管理员用户管理页（第一期）

### 本次完成

新增管理员专用“用户管理”第一期功能。管理员可以查看用户列表、按用户名/昵称搜索、按角色筛选，并在输入系统密码和确认文字后修改其他用户的角色。

本期明确不做：

```text
1. 不删除用户。
2. 不重置用户密码。
3. 不封禁账号。
4. 不修改数据库结构。
5. 不修改数据库文件。
6. 不返回 password_hash、password_salt、session_token、token 等敏感字段。
```

### 修改文件

```text
01_后端服务/app.py
新增管理员用户列表接口和管理员修改用户角色接口；复用现有 require_admin_user、verify_system_password、AuthUser 模型。

02_前端页面/user-admin.html
新增用户管理页面，包含搜索区、角色筛选区、用户表格和修改角色弹窗。

02_前端页面/user-admin.js
新增页面逻辑：读取当前登录 token、校验管理员身份、加载用户列表、搜索筛选、修改角色、错误提示和无权限提示。

02_前端页面/admin.html
在审核中心内容区新增“进入用户管理”入口，不改顶部导航布局。

02_前端页面/style.css
新增用户管理页专用表格和筛选区样式，仅作用于 user-admin 相关 class。

02_前端页面/admin-permission-guard.js
将 user-admin.html 加入管理员页面拦截范围，并将“用户管理”识别为管理员入口。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 60 更新到 61。

02_前端页面/*.html
机械同步 global-loader.js 引用版本，从 v=60 更新到 v=61；新增 user-admin.html 直接引用 v=61。

项目交接与续接总文档.md
追加本次记录。
```

### 新增接口

```text
GET /auth/users-admin
参数：
token：当前管理员 token，必填
keyword：可选，按用户名/昵称搜索
role：可选，admin 或 user
limit：可选，默认 50，最大 100
offset：可选，默认 0

权限：
必须登录管理员。未登录或 bad token 返回 401，普通用户返回 403。

返回：
id
username
account
nickname
role
avatar_base64
created_at
updated_at（当前模型无该字段，返回 null）
last_login_at
```

```text
PATCH /auth/users/{user_id}/role-admin
请求体：
token
system_password
new_role：admin 或 user
confirm_text：必须为 修改角色

权限和限制：
必须登录管理员。
必须输入正确系统密码 xgw2026。
必须输入确认文字 修改角色。
不允许修改当前登录管理员自己的角色。
如果目标用户是最后一个管理员，不允许降级为普通用户。
用户不存在返回 404。
成功后返回修改后的安全用户信息。
```

### 权限规则

```text
1. user-admin.html 属于管理员页面，未登录和普通用户应显示无权限访问。
2. 用户管理入口只放在 admin.html 的管理员审核中心内容区。
3. 前端请求统一复用 localStorage 中的 xgw_user_token。
4. 后端接口必须通过 require_admin_user 校验管理员身份，不能只靠前端隐藏。
5. 修改角色还必须校验系统密码和确认文字。
```

### 安全限制

```text
1. 用户列表接口和修改角色接口只返回安全字段。
2. 不返回 password_hash、password_salt、session_token、token、password。
3. 不允许管理员修改当前登录管理员自己的角色，防止把自己降级锁死。
4. 目标用户为最后一个管理员时，不允许降级。
5. 本期不做删除用户、重置密码、封禁账号。
```

### 测试步骤

管理员测试：

```text
1. 登录管理员。
2. 打开 admin.html，点击“进入用户管理”。
3. user-admin.html 用户列表应能加载。
4. 搜索用户名/昵称应能过滤。
5. 按角色筛选“管理员 / 普通用户”应能过滤。
6. 当前管理员自己的“修改角色”按钮应不可用。
7. 修改其他用户角色时，输错系统密码应失败。
8. 确认文字不是“修改角色”应失败。
9. 修改成功后用户列表刷新。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 user-admin.html。
3. 应显示无权限访问。
4. 直接请求 GET /auth/users-admin 应返回 403。
```

未登录测试：

```text
1. 退出登录。
2. 打开 user-admin.html。
3. 应显示未登录用户无权限。
4. 后端接口不带 token 或 bad token 应返回 401。
```

安全字段测试：

```text
1. 在 Network 响应中检查 /auth/users-admin。
2. 不应出现 password_hash。
3. 不应出现 password_salt。
4. 不应出现 session_token。
5. 不应出现 token。
```

### 当前测试状态

已完成代码实现、文档记录和静态检查。已通过：

```text
1. python -B -m py_compile app.py
2. node --check 02_前端页面/user-admin.js
3. node --check 02_前端页面/admin-permission-guard.js
4. node --check 02_前端页面/global-loader.js
5. git diff --check
6. GET /auth/users-admin?token=bad 返回 401
7. PATCH /auth/users/1/role-admin 使用 bad token 返回 401
8. 确认没有数据库文件变更。
9. 确认没有新增 public-nav.js。
```

人工浏览器测试已通过。用户已确认：

```text
1. 管理员可以从 admin.html 进入 user-admin.html。
2. 用户列表能正常加载。
3. 用户搜索和角色筛选可用。
4. 当前管理员自己的角色修改按钮不可用或被后端拒绝。
5. 输错系统密码会失败。
6. 确认文字不是“修改角色”会失败。
7. 普通用户打开 user-admin.html 会显示无权限。
8. 未登录用户打开 user-admin.html 会显示未登录无权限。
9. Network 响应未发现 password_hash、password、session_token、token 等敏感字段。
10. 没有修改数据库结构，没有修改数据库文件。
```

当前测试状态：已测试通过。待执行 Git 提交。

### 下次继续建议

测试通过后再提交 Git。第二期如需封禁账号，应先确认是否新增模型字段并另开迁移任务；管理员重置密码属于高风险操作，必须另开任务；用户详情页也应另开任务，不要和角色管理混在一起。

## 2026-06-08 管理员用户管理页体验优化（第一期）

### 本次完成

在不增加删除用户、重置密码、封禁/启用账号等高风险操作的前提下，优化 `user-admin.html` 的用户管理体验。

本次完成：

```text
1. 增加用户列表分页控件：上一页、下一页、当前页码、每页条数。
2. 每页条数默认 20，支持 20 / 50 / 100。
3. 搜索、角色筛选、每页条数变化时自动回到第 1 页。
4. 加载时显示“正在加载用户列表...”。
5. 无结果时显示“暂无匹配用户”。
6. 后端错误、未登录、无权限、token 失效和网络错误在页面区域内显示，不使用 alert 堆叠。
7. 角色显示改为用户管理页专用徽章：admin 显示“管理员”，user 显示“普通用户”。
8. 修改角色弹窗明确显示用户名、昵称、当前角色、将修改成的新角色、系统密码提示和确认文字提示。
9. 修改成功后刷新当前页列表并显示成功提示。
```

### 修改文件

```text
01_后端服务/app.py
GET /auth/users-admin 保留原 data/total/limit/offset 返回，同时增加兼容字段 items 和 total_count；仍只返回安全字段。

02_前端页面/user-admin.html
增加 user-admin-page 页面 class、每页条数选择框、分页挂载区、修改角色弹窗详情区；user-admin.js 版本更新到 v=2。

02_前端页面/user-admin.js
新增分页状态、搜索/筛选回到第一页、加载/空/错误状态、角色徽章渲染、修改角色弹窗详情预览和成功后刷新当前页逻辑。

02_前端页面/style.css
只新增/调整用户管理页相关 class：user-admin-toolbar、user-role-badge、user-admin-pagination、user-admin-message、user-role-detail 等。

项目交接与续接总文档.md
追加本次体验优化记录。
```

### 后端接口变化

`GET /auth/users-admin` 继续兼容原结构：

```text
data
total
limit
offset
```

本次补充：

```text
items       与 data 相同，便于新前端读取。
total_count 与 total 相同，便于分页显示总数。
```

没有新增数据库字段，没有数据库迁移，没有修改数据库文件。

### 安全字段说明

用户列表和修改角色接口仍只返回安全字段：

```text
id
username
account
nickname
role
avatar_base64
created_at
updated_at
last_login_at
```

仍然不返回：

```text
password_hash
password_salt
password
session_token
token
```

修改角色仍保持：

```text
1. 必须登录管理员。
2. 必须校验系统密码 xgw2026。
3. 必须输入确认文字：修改角色。
4. 不允许当前管理员修改自己的角色。
5. 不允许最后一个管理员被降级。
```

### 测试步骤

管理员测试：

```text
1. 登录管理员。
2. 打开 user-admin.html。
3. 用户列表能加载。
4. 分页上一页/下一页可用。
5. 搜索后回到第一页。
6. 角色筛选后回到第一页。
7. 每页条数变化后回到第一页。
8. 无结果时显示“暂无匹配用户”。
9. 角色显示为“管理员 / 普通用户”徽章。
10. 当前管理员自己的修改按钮不可用。
11. 输错系统密码，页面显示错误。
12. 确认文字错误，页面显示错误。
13. 修改成功后刷新当前页列表并显示成功提示。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 user-admin.html。
3. 应显示无权限访问。
```

未登录测试：

```text
1. 退出登录。
2. 打开 user-admin.html。
3. 应显示未登录用户无权限。
```

安全字段测试：

```text
1. 检查 /auth/users-admin 响应。
2. 不应出现 password_hash。
3. 不应出现 password。
4. 不应出现 session_token。
5. 不应出现 token。
```

### 当前测试状态

已完成代码实现、静态检查和人工浏览器测试。

已通过：

```text
1. python -B -m py_compile app.py
2. node --check 02_前端页面/user-admin.js
3. 确认没有数据库文件变更。
4. 确认没有新增 public-nav.js。
```

人工测试已通过，用户已确认：

```text
1. 管理员打开 user-admin.html 后，用户列表能正常加载。
2. 加载状态、空状态、错误提示显示正常。
3. 搜索用户名/昵称可用。
4. 角色筛选可用。
5. 分页显示正常，上一页/下一页/每页条数控件正常。
6. 角色显示为“管理员 / 普通用户”徽章。
7. 当前管理员自己的修改角色按钮不可用。
8. 输错系统密码会失败，并显示错误提示。
9. 确认文字不是“修改角色”会失败，并显示错误提示。
10. 普通用户打开 user-admin.html 显示无权限。
11. 未登录用户打开 user-admin.html 显示未登录无权限。
12. 没有修改数据库结构，没有修改数据库文件。
13. 没有新增 public-nav.js，没有修改 global-loader.js，没有改顶部导航布局。
```

当前测试状态：已测试通过。待执行 Git 提交。

### 下次继续建议

人工测试通过后再提交 Git。后续如果继续用户管理第二期，封禁/启用账号、重置密码、用户详情页都应另开任务；涉及数据库字段时必须单独规划迁移和回退方案。

## 2026-06-08 GitHub 上传前清理：停止跟踪 .idea 配置文件

### 本次完成

本次只做 GitHub private repository 上传前安全清理，停止 Git 跟踪 `.idea/` 下的 PyCharm 项目配置文件。

已完成：

```text
1. 在 .gitignore 中补充 .idea/，忽略整个 IDE 项目配置目录。
2. 执行 git rm -r --cached .idea，仅从 Git 索引移除 .idea/ 跟踪。
3. 保留本地 .idea/ 文件夹和本地 IDE 配置，不删除本地文件。
4. 未修改业务代码、后端代码、数据库文件。
5. 未配置 remote，未执行 git remote add，未执行 git push。
```

### 修改文件

```text
.gitignore
项目交接与续接总文档.md
.idea/ 下已跟踪文件：仅从 Git 索引移除，不删除本地文件
```

### 安全检查结果

```text
1. 本地 .idea/ 文件夹仍存在。
2. 数据库文件仍未被 Git 跟踪。
3. zip 备份文件仍未被 Git 跟踪。
4. 当前仍建议 GitHub 使用 private repository。
5. 当前未配置 Git remote，未上传 GitHub。
```

### 测试步骤

```text
1. git status
2. 检查 .gitignore 是否包含 .idea/
3. git rm -r --cached .idea
4. Test-Path .idea
5. git ls-files -- '*.db' '*.sqlite' '*.sqlite3' '*.zip'
6. git status
```

### 当前测试状态

静态安全检查通过，待提交 Git。提交后应再次确认 `git status` 为 clean，且 `.idea/` 保持本地存在但不再被 Git 跟踪。

### 下一步建议

如需上传 GitHub，建议先创建 private repository；上传前再次确认数据库、zip、`.env`、本地缓存和 IDE 配置均未被跟踪。

## 2026-06-10 GitHub private repository 上传准备与记录

### 本次事项

本次任务用于将项目上传到 GitHub private repository 前完成最终状态记录，并准备把本地 `master` 分支推送到远程仓库。

已确认：

```text
1. GitHub private repository 已准备上传。
2. 上传前最终复查已通过。
3. 数据库、zip、.idea、虚拟环境、.env 均未被 Git 跟踪。
4. GitHub 仓库类型为 private。
5. 远程仓库地址：https://github.com/fzs040617/xiaoguwei-real-life-info-system.git
6. 未记录任何 token。
7. 当前不建议 public，因为项目中仍包含系统密码 xgw2026 和项目交接细节。
8. 上传后下一步建议：后续每次重要修改都先本地 commit，再 push 到 origin master。
```

### 安全说明

```text
1. 未修改业务代码。
2. 未修改后端代码。
3. 未修改数据库文件。
4. 未修改 .gitignore。
5. 未删除或移动文件。
6. 上传目标为 GitHub private repository，不是 public repository。
```

## 2026-06-10 新增安全版 README.md

### 本次完成

本次新增根目录 `README.md`，作为 GitHub private repository 首页说明文档。README 只记录项目简介、目录结构、本地启动方式、安全说明、开发流程和维护原则。

已确认：

```text
1. README.md 是安全版说明文档。
2. README.md 没有写入系统密码、token、密钥。
3. 未修改业务代码。
4. 未修改后端代码。
5. 未修改数据库文件。
6. 未修改 .gitignore。
7. 仓库继续保持 private，不执行改 public 操作。
```

### 修改文件

```text
README.md
项目交接与续接总文档.md
```

### 上传前检查

已执行并通过：

```text
git status
git remote -v
git ls-files "*.db" "*.sqlite" "*.sqlite3"
git ls-files "*.zip"
git ls-files ".idea/*"
git ls-files ".venv/*" "venv/*" "env/*" "01_后端服务/.venv/*"
git ls-files ".env" ".env.*"
git config user.name
git config user.email
```

检查结果：

```text
1. 工作区开始时为 clean。
2. 当前分支 master 已跟踪 origin/master。
3. remote 指向 GitHub private repository。
4. 数据库、zip、.idea、虚拟环境、.env 均未被 Git 跟踪。
5. Git 作者配置为 Sean。
6. 本次修改基于提交 ad998eb docs: record GitHub private backup setup。
```

### 测试步骤

```text
1. 检查 README.md 内容，不包含系统密码、token、密钥。
2. 检查 README.md 仅包含项目说明、启动方式、安全说明和开发流程。
3. 运行 git status 查看仅 README.md 和项目交接与续接总文档.md 发生变化。
4. 再次检查敏感文件跟踪状态。
5. 提交后 push 到 origin master。
```

### 当前测试状态

静态文档检查和 Git 敏感文件跟踪检查通过。待提交并推送到 `origin master`。

### 下一步建议

后续每次重要修改继续保持先本地 commit，再 push 到 `origin master`；README 如需扩展，只补充非敏感说明，不写入密码、token、密钥或真实私密数据。

## 2026-06-10 外部平台每日采集功能第一期

### 本次完成

新增“外部平台每日采集”第一期安全版本，用于后续从公开 RSS、公开 JSON API、公开 HTML 列表页采集与小谷围岛 / 广州大学城 / 周边真实生活信息相关的数据。第一期重点是搭建独立采集数据库、采集任务框架、每日定时任务、管理员接口和采集管理页面。

本次不提交、不推送，等待人工测试通过后再执行 Git 提交和 push。

### 新增采集数据库

```text
01_后端服务/collector_data.db
```

说明：

```text
1. 该数据库只用于外部平台公开源采集数据。
2. 不修改现有主业务数据库 xiaoguwei.db 的结构。
3. 不提交任何 .db 文件。
4. 当前 .gitignore 已覆盖 *.db，因此 collector_data.db 不应被 Git 跟踪。
```

### 采集数据库表结构

```text
collector_sources
- id
- name
- platform
- source_type：rss / api / public_html
- url
- keyword
- enabled
- interval_hours
- last_run_at
- created_at
- updated_at
- notes

collector_runs
- id
- source_id
- status：success / failed / skipped
- started_at
- finished_at
- item_count
- error_message

collector_items
- id
- source_id
- platform
- title
- url
- summary
- published_at
- fetched_at
- raw_hash
- raw_json
- raw_text
- status：new / reviewed / ignored
```

去重规则：

```text
1. collector_items.url 建唯一索引，避免同一链接重复入库。
2. collector_items.raw_hash 建唯一索引，避免同一原始条目重复入库。
```

### 修改文件

```text
01_后端服务/platform_collector.py
新增独立外部平台采集服务模块，使用 sqlite3 管理 collector_data.db，支持公开 RSS/Atom、公开 JSON API、公开 HTML 列表页解析、关键词过滤、去重、运行记录和查询。

01_后端服务/app.py
新增 collector-admin 管理接口；启动时安全初始化采集数据库，初始化失败只打印日志，不影响主系统启动。

01_后端服务/scheduler.py
保留原每天 09:00 旧采集任务，新增每天 03:30 外部平台公开源采集任务；采集异常只记录结果，不影响主系统。

02_前端页面/crawler.html
复用原采集管理页面位置，改为外部平台采集管理第一期页面；顶部导航布局和按钮顺序不做大改。

02_前端页面/crawler-collector.js
新增采集管理页面脚本，支持采集源列表、新增、启用/停用、单源运行、全部运行、采集条目列表和运行日志列表。

02_前端页面/style.css
新增少量 collector 页面专用布局样式。

项目交接与续接总文档.md
追加本次记录。
```

### 新增接口清单

所有接口必须管理员权限，复用现有登录 token 和管理员校验逻辑：

```text
GET  /collector-admin/sources
POST /collector-admin/sources
PATCH /collector-admin/sources/{source_id}
POST /collector-admin/sources/{source_id}/run
POST /collector-admin/run-all
GET  /collector-admin/items
GET  /collector-admin/runs
```

权限规则：

```text
1. 未登录或 bad token 返回现有 401 行为。
2. 普通用户返回现有 403 行为。
3. 只有管理员可以查看、新增、修改和手动运行采集任务。
```

### 定时任务运行方式

```text
1. 后端启动时调用 start_scheduler()。
2. 原 daily_crawl_job 保留：每天 09:00 运行旧公开网页采集到线索库逻辑。
3. 新 daily_platform_collector_job：每天 03:30 运行外部平台公开源采集。
4. 新任务只运行 collector_sources 中 enabled=true 的采集源。
5. 如果没有启用的采集源，会安全跳过。
6. 外部网络失败、解析失败、源不可用时，只写 collector_runs，不影响主系统启动。
```

### 安全边界

```text
1. 不抓登录后内容。
2. 不绕过验证码。
3. 不绕过平台反爬机制。
4. 不抓付费墙、私密内容、登录后内容。
5. 不写入平台账号、cookie、token、key。
6. 所有外部请求设置 timeout。
7. 不高频请求，定时任务默认每天一次。
8. 不预置高风险平台的非公开接口。
9. 不上传采集数据库。
10. 不写入系统危险操作密码值。
```

### 前端人工测试步骤

管理员测试：

```text
1. 启动后端。
2. 登录管理员。
3. 打开 02_前端页面/crawler.html。
4. 确认采集源列表、采集条目、运行日志可以加载；首次为空应显示空状态。
5. 新增一个公开 RSS / 公开 JSON API / 公开 HTML 列表页测试源。
6. 点击“运行单个源”，确认运行日志新增记录。
7. 点击“运行全部启用源”，确认页面显示运行结果。
8. 切换启用/停用，确认采集源状态更新。
9. 检查采集条目列表能显示标题、链接、摘要、来源和抓取时间。
10. 确认页面没有要求填写平台账号、cookie、token、key。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 crawler.html。
3. 应由全局管理员权限守卫显示无权限，接口直接访问也应返回无权限。
```

未登录测试：

```text
1. 退出登录。
2. 打开 crawler.html。
3. 应显示未登录无权限；接口直接访问应返回未登录。
```

后端接口测试：

```text
1. GET /collector-admin/sources 使用 bad token 应失败。
2. POST /collector-admin/sources 使用 bad token 应失败。
3. POST /collector-admin/run-all 使用管理员 token 应返回运行结果。
4. 没有启用源时应安全跳过。
5. 外部源不可访问时应写入 failed 运行记录，不应导致后端崩溃。
```

### Codex 静态检查

本次完成后应执行：

```text
python -B -m py_compile app.py
python -B -m py_compile platform_collector.py
python -B -m py_compile scheduler.py
node --check 02_前端页面/crawler-collector.js
git status
git ls-files "*.db" "*.sqlite" "*.sqlite3"
git ls-files "*.zip"
git ls-files ".idea/*"
git ls-files ".venv/*" "venv/*" "env/*" "01_后端服务/.venv/*"
git ls-files ".env" ".env.*"
```

### 当前测试状态

```text
Codex 静态检查部分通过。
已通过：node --check 02_前端页面/crawler-collector.js。
已通过：Git 敏感文件跟踪检查，数据库、zip、.idea、虚拟环境、.env 均未被 Git 跟踪。
未完成：Python py_compile 检查因当前机器 Python 环境不可用而无法执行。
具体原因：python.exe 为 Microsoft Store 占位命令，py 启动器没有默认 Python，项目 .venv 指向旧用户目录下不存在的 Python。
等待人工浏览器测试。
本次尚未 commit，尚未 push。
```

### 下一步建议

```text
1. 人工测试通过后再 commit 和 push。
2. 后续逐个平台接入时必须单独设计，先确认公开性、合规性、频率限制和字段映射。
3. 如需平台 API key，只能走本地环境配置，不写入代码或文档。
4. 采集条目进入正式线索库前，应另开“采集条目审核入库”任务。
```
## 2026-06-10 外部平台每日采集第一期续修：新增源错误提示与智能识别

### 本次完成

本次在上一轮未提交改动基础上继续处理外部平台每日采集功能第一期，未执行 commit，未执行 push。

已完成：

```text
1. 诊断 crawler.html 新增采集源时出现 “Failed to fetch” 的可能原因。
2. 确认前端采集页使用 http://127.0.0.1:8000 作为后端 API 地址，并读取 localStorage 中的 xgw_user_token。
3. 确认后端已存在 POST /collector-admin/sources，请求体字段与前端 name、platform、source_type、url、keyword、enabled、interval_hours、notes 基本一致。
4. 确认后端已有 CORS 配置，可支持本地前端页面访问。
5. 修复前端网络错误提示：后端不可达或 fetch 网络失败时，明确显示“无法连接后端，请确认 http://127.0.0.1:8000 已启动”，不再只显示 Failed to fetch。
6. 修复 crawler-collector.js 中新增、切换、运行、列表渲染等采集页提示文本与错误 detail 展示逻辑。
7. 新增“智能识别采集源”能力。
8. 新增管理员接口 POST /collector-admin/detect-source。
9. 在 crawler.html 的 URL 输入区附近新增“智能识别”按钮和识别结果提示区域。
10. 样式仅新增 collector 页面相关 class，未修改顶部导航布局，未新增 public-nav.js。
```

### 新增接口

```text
POST /collector-admin/detect-source
```

请求体：

```json
{
  "token": "管理员登录 token",
  "url": "https://example.com/rss.xml"
}
```

成功返回示例：

```json
{
  "ok": true,
  "source_type": "rss",
  "platform": "example.com",
  "name": "示例 RSS",
  "title": "示例 RSS",
  "keyword_suggestion": "小谷围 广州大学城 大学城 贝岗 南亭",
  "message": "识别成功"
}
```

失败返回示例：

```json
{
  "ok": false,
  "message": "无法识别该公开 URL，或请求超时"
}
```

接口规则：

```text
1. 必须管理员权限。
2. 只访问用户填写的 http / https 公开 URL。
3. 所有外部请求使用 timeout。
4. 不保存数据库。
5. 不写入平台账号、cookie、token、密钥。
6. 不绕登录、不绕验证码、不绕反爬、不抓私密内容、不抓付费墙内容。
7. 不把识别到的原始大内容完整写入日志或文档。
8. 识别失败只返回失败原因，不影响后端启动。
```

### 智能识别规则

```text
1. RSS / Atom：XML 根节点或内容包含 rss、feed、channel 等特征时识别为 rss，并尽量读取 channel title / feed title。
2. JSON API：Content-Type 包含 json 或内容可解析为 JSON 时识别为 api。
3. HTML：Content-Type 包含 html 或内容带有 html/title 特征时识别为 public_html，并尽量读取 <title> 或 h1。
4. 平台名称优先从 URL 域名推断。
5. 名称优先使用识别到的标题，失败时使用域名和 URL path 生成默认名称。
6. 关键词建议为“小谷围 广州大学城 大学城 贝岗 南亭”，仅当前端关键词输入框为空时自动填入，不覆盖用户已填内容。
```

### 修改文件

```text
01_后端服务/app.py
- 新增 CollectorSourceDetectRequest。
- 新增 POST /collector-admin/detect-source。
- 接口复用现有 require_admin_token / require_admin_user 管理员校验。

01_后端服务/platform_collector.py
- 新增 detect_source 及 URL 平台推断、RSS/Atom 标题提取、HTML 标题提取等辅助函数。
- 识别逻辑只读公开 URL，不写 collector_data.db。

02_前端页面/crawler.html
- 在采集源 URL 输入区新增“智能识别”按钮。
- 新增 collectorDetectMessage 识别结果提示区域。

02_前端页面/crawler-collector.js
- 统一采集页 API 地址为 http://127.0.0.1:8000 或已有 API_BASE。
- 继续读取 localStorage 的 xgw_user_token。
- 新增 detectCollectorSource()，调用 /collector-admin/detect-source。
- 成功后自动填充类型、平台、名称；关键词为空时填入建议关键词。
- 后端不可达时明确提示“无法连接后端，请确认 http://127.0.0.1:8000 已启动”。
- 后端返回 detail/message 时直接展示，便于定位权限、参数或业务错误。

02_前端页面/style.css
- 新增 collector-url-row、collector-detect-message 等采集页专用样式。

项目交接与续接总文档.md
- 追加本次续修记录。
```

说明：当前工作区还保留上一轮未提交的 scheduler.py 等改动，本次未丢弃、未 restore、未 reset。

### 测试步骤与结果

已执行：

```bat
node --check 02_前端页面/crawler-collector.js
```

结果：通过，无输出。

已执行：

```bat
cd /d "C:\Users\Sean\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务"
.venv\Scripts\python.exe -B -m py_compile app.py scheduler.py platform_collector.py
```

结果：通过，无输出。

已执行敏感文件跟踪检查：

```bat
git ls-files "*.db" "*.sqlite" "*.sqlite3"
git ls-files "*.zip"
git ls-files ".idea/*"
git ls-files ".venv/*" "venv/*" "env/*" "01_后端服务/.venv/*"
git ls-files ".env" ".env.*"
```

结果：均无输出，数据库、zip、.idea、虚拟环境、.env 均未被 Git 跟踪。

### 当前 Git 状态

```text
M  01_后端服务/app.py
M  01_后端服务/scheduler.py
M  02_前端页面/crawler.html
M  02_前端页面/style.css
M  项目交接与续接总文档.md
?? 01_后端服务/platform_collector.py
?? 02_前端页面/crawler-collector.js
```

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
敏感文件跟踪检查通过。
等待人工复测：新增采集源、后端关闭时错误提示、智能识别采集源、管理员权限校验。
```

### 下一步建议

```text
1. 启动后端后，用管理员账号打开 crawler.html。
2. 先在后端正常启动状态下新增一个采集源，确认不再出现笼统的 Failed to fetch。
3. 临时关闭后端后点击新增或智能识别，确认页面提示“无法连接后端，请确认 http://127.0.0.1:8000 已启动”。
4. 使用公开 RSS / JSON API / HTML 页面分别测试智能识别。
5. 人工测试通过后再 commit 和 push。
```
## 2026-06-10 外部平台每日采集第一期续修：智能识别标题乱码修复

### 本次完成

本次只修复“智能识别采集源”返回 RSS / HTML 标题中文乱码的问题，未新增其他业务功能，未大改页面，未修改顶部导航，未新增 public-nav.js，未执行 commit，未执行 push。

已完成：

```text
1. 在 01_后端服务/platform_collector.py 中新增响应内容稳健解码逻辑。
2. 智能识别不再直接依赖 response.text 读取 RSS / HTML 标题。
3. 解码候选顺序包含 HTTP header charset、XML 声明 encoding、response.apparent_encoding、utf-8、gb18030、gbk、gb2312。
4. 对能解码但明显像乱码的结果继续尝试后续编码。
5. RSS / Atom 标题提取继续优先读取 channel title / feed title。
6. HTML title 基于稳健解码后的文本提取。
7. 如果识别到的标题仍包含明显乱码特征，会回退为“域名 + 公开 RSS”或“域名 + 公开页面”。
8. 不把页面原始大内容写入日志或文档。
9. 不影响已有采集入库逻辑，不修改业务数据库结构。
```

### 重点修复说明

针对 `http://www.people.com.cn/rss/politics.xml` 这类可能使用 GB2312 / GBK / GB18030 的公开 RSS，智能识别现在会先从响应原始 bytes 中识别编码，再解析 RSS 标题。

预期人工复测结果：

```text
1. 类型识别为 rss。
2. 平台识别为 www.people.com.cn 或 people.com.cn。
3. 名称 / 标题不再出现 æ□□æ□¿é¢□é□□ 这类乱码。
4. 如果仍无法可靠解码中文标题，名称 / 标题至少回退为 www.people.com.cn 公开 RSS。
5. 关键词建议保持正常。
6. 页面不报错。
```

### 修改文件

```text
01_后端服务/platform_collector.py
- 新增 response_charset_from_header。
- 新增 response_charset_from_xml。
- 新增 decode_response_text。
- 新增 strip_xml_declaration。
- 新增 looks_garbled。
- 新增 clean_detected_title。
- detect_source 使用 decode_response_text 处理 RSS / JSON / HTML 识别文本。
- RSS / HTML 标题返回前先做乱码检测和兜底。

项目交接与续接总文档.md
- 追加本次乱码修复记录。
```

### 测试步骤与结果

已执行本地构造测试：

```text
构造 GB2312 XML RSS 响应，标题为“人民日报政治新闻”。
decode_response_text 能正确解码。
looks_like_rss_or_atom 能提取 title。
clean_detected_title 遇到模拟乱码标题时能回退为“www.people.com.cn 公开 RSS”。
```

已执行：

```bat
node --check 02_前端页面/crawler-collector.js
```

结果：通过，无输出。

已执行：

```bat
cd /d "C:\Users\Sean\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务"
.venv\Scripts\python.exe -B -m py_compile app.py scheduler.py platform_collector.py
```

结果：通过，无输出。

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
等待人工复测智能识别乱码修复。
```

### 下一步建议

```text
1. 启动后端。
2. 用管理员账号打开 crawler.html。
3. 在 URL 输入框填写 http://www.people.com.cn/rss/politics.xml。
4. 点击“智能识别”。
5. 确认名称 / 标题不再出现乱码；若无法读出中文标题，应显示 www.people.com.cn 公开 RSS。
6. 人工复测通过后再 commit 和 push。
```
## 2026-06-10 外部平台每日采集第一期最终人工测试通过与提交准备

### 本次状态补充

本次只补充最终文档状态并准备提交，不新增功能，不继续修改业务逻辑。

人工测试已确认：

```text
1. 本机 Python 环境已修复为 Python 3.12。
2. 后端虚拟环境 01_后端服务/.venv 已重建。
3. 旧坏虚拟环境 .venv_old_broken 已删除。
4. Python 编译检查通过。
5. JS 检查通过。
6. 后端启动通过。
7. 后端启动日志显示每天 09:00 旧采集任务已启动。
8. 后端启动日志显示每天 03:30 外部平台公开源采集任务已启动。
9. collector_data.db 生成成功且未被 Git 跟踪。
10. /docs 接口检查通过，collector-admin 接口存在。
11. crawler.html 页面打开通过。
12. 新增采集源通过。
13. 运行单个源通过。
14. 运行全部启用源通过。
15. 抓到 0 条数据可以接受，因为关键词可能没有匹配项。
16. 智能识别按钮通过。
17. 人民网 RSS 智能识别可识别为 rss，平台为 www.people.com.cn，关键词建议正常。
18. RSS 标题乱码修复复测通过，或可回退为可读的“域名 + 公开 RSS”。
19. 敏感文件跟踪检查通过。
20. 当前进入可提交状态。
```

### 当前安全与提交边界

```text
1. 不提交 01_后端服务/.venv。
2. 不提交 collector_data.db。
3. collector_data.db 是本地运行数据，不进入 Git。
4. 不提交任何 .db / .sqlite / .sqlite3。
5. 不提交任何 .zip。
6. 不提交 .idea。
7. 不提交 .env。
8. 不提交 .venv_old_broken。
9. 本次没有写入平台账号、cookie、token、密钥。
10. 本次没有写入系统密码。
11. 本次没有新增 public-nav.js。
12. 仓库仍为 private。
```

### 本次允许提交文件

```text
01_后端服务/app.py
01_后端服务/scheduler.py
01_后端服务/platform_collector.py
02_前端页面/crawler.html
02_前端页面/crawler-collector.js
02_前端页面/style.css
项目交接与续接总文档.md
```

### 最终测试状态

```text
Python 编译检查通过。
JS 检查通过。
后端启动通过。
collector_data.db 生成成功且未被 Git 跟踪。
/docs 接口检查通过。
crawler.html 页面打开通过。
新增采集源通过。
运行单个源通过。
运行全部启用源通过。
智能识别按钮通过。
RSS 标题乱码修复复测通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
```

## 2026-06-10 外部平台采集续修：智能识别表单覆盖逻辑

### 本次修复

本次只修复 `crawler.html` 外部平台采集管理页中“智能识别采集源”成功后表单字段不覆盖旧值的问题，不新增新功能，不修改业务数据库结构，不修改顶部导航布局，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 智能识别成功后，名称字段会以当前 URL 的识别结果为准刷新。
2. 智能识别成功后，平台字段会以当前 URL 的识别结果为准刷新，不保留旧平台。
3. 智能识别成功后，类型字段会以当前 URL 的识别结果为准刷新，不保留旧类型。
4. 若后端返回 rss / api / public_html，前端会写入对应类型；异常类型兜底为 public_html。
5. 关键词为空时会填入后端 keyword_suggestion。
6. 如果关键词仍等于上一次智能识别自动填入的建议值，则允许被新的识别结果覆盖。
7. 如果用户已经手动修改关键词，则保留用户关键词，不强制覆盖。
8. URL 输入框内容变化时，识别提示会提醒“URL 已修改，请重新智能识别。”。
9. 识别成功提示会显示当前 URL 的类型、平台、标题，避免用户误以为仍在使用旧识别结果。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 新增最近一次智能识别自动填入关键词的前端状态。
- 调整 detectCollectorSource 成功回填逻辑，强制刷新名称、平台、类型。
- 调整关键词回填逻辑，区分自动建议值和用户手动修改值。
- 新增 source_type 规范化和 URL 域名兜底辅助函数。
- URL 修改时刷新识别提示。

项目交接与续接总文档.md
- 追加本次智能识别表单覆盖逻辑修复记录。
```

### 人工复测步骤

```text
1. 打开 crawler.html。
2. URL 填入 https://jsonplaceholder.typicode.com/posts。
3. 点击“智能识别”，确认名称 / 平台 / 类型变为 jsonplaceholder / api 相关。
4. 不清空其他字段，直接把 URL 改成 https://www.sysu.edu.cn/news/。
5. 再点“智能识别”。
6. 预期名称变为中山大学新闻网或相关标题，平台变为 www.sysu.edu.cn，类型变为 public_html，不再残留 jsonplaceholder 的名称和平台。
7. 再把 URL 改成 http://www.people.com.cn/rss/politics.xml。
8. 再点“智能识别”。
9. 预期类型变为 rss，平台变为 www.people.com.cn，名称为可读标题或 www.people.com.cn 公开 RSS，不出现乱码，不残留中山大学或 jsonplaceholder 信息。
```

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
敏感文件跟踪检查通过。
人工复测通过。
名称字段覆盖通过。
平台字段覆盖通过。
类型字段覆盖通过。
关键词保留/覆盖策略符合预期。
当前进入可提交状态。
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```

## 2026-06-12 外部平台每日采集功能第五期：采集源列表前端筛选与异常源定位

### 本次功能名称

```text
采集源列表前端筛选与异常源定位。
```

### 本次完成内容

```text
1. 在采集源列表区域新增前端筛选工具栏。
2. 支持按名称、平台、URL、备注、关键词、类型搜索采集源。
3. 支持按启用状态筛选：全部、启用中、已停用。
4. 支持按最近运行状态筛选：全部、最近成功、最近失败、暂无运行记录、已跳过。
5. 支持按源类型筛选：全部、RSS、public_html、json_api / api。
6. 新增“只看异常源”快捷筛选。
7. 异常源定义为：最近失败，或启用中但暂无运行记录。
8. 新增“清除筛选”能力，筛选结果为 0 时也可直接清除筛选查看全部。
9. 采集源列表标题和筛选摘要显示“显示 X / 共 Y 个”。
10. 筛选后不影响预览、运行单个源、启用/停用、查看该源日志、展开/收起。
```

### 修改文件

```text
02_前端页面/crawler.html
- 在采集源列表 collectorSourceList 前新增筛选工具栏。
- 新增搜索框、启用状态筛选、运行状态筛选、类型筛选、只看异常源、清除筛选和筛选结果提示。

02_前端页面/crawler-collector.js
- 新增 collectorAllSources 缓存，保存 GET /collector-admin/sources 返回的全量源。
- loadCollectorSources 拉取源和最近运行日志后改为调用 applyCollectorSourceFilters。
- 新增 getCollectorSourceFilterState、collectorSourceMatchesKeyword、collectorSourceMatchesEnabled、collectorSourceMatchesType、collectorSourceMatchesRunStatus、collectorSourceIsProblem、applyCollectorSourceFilters、clearCollectorSourceFilters、showCollectorProblemSources。
- 新增采集源筛选相关 document 级事件委托。
- renderCollectorSources 继续负责渲染源卡片，并支持显示筛选数量。

02_前端页面/style.css
- 新增 collector-source-filter-row、collector-source-filter-summary、collector-filter-active、collector-source-empty 样式。

项目交接与续接总文档.md
- 追加本次第五期修改记录。
```

### 接口变化

```text
新增接口：无。
复用既有接口：
GET /collector-admin/sources
GET /collector-admin/runs
```

### 前端变化

```text
1. 采集源列表可以纯前端搜索和筛选。
2. 筛选不会写入后端，不修改采集源数据。
3. 单源运行、运行全部、启用/停用刷新源列表后会重新应用当前筛选。
4. 筛选结果为空时显示明确空状态，并提供清除筛选入口。
```

### 后端变化

```text
无。本次不修改后端代码，不新增后端接口。
```

### 数据库变化

```text
无。本次不修改数据库结构，不修改 collector_data.db。
```

### 删除/批量删除

```text
无。本次不新增删除或批量删除采集源能力。
```

### 测试步骤

```text
1. 启动后端并打开 02_前端页面/crawler.html。
2. 展开采集源列表。
3. 在搜索框输入源名称、平台、URL、备注或关键词片段，预期列表按关键词过滤。
4. 选择“启用中”或“已停用”，预期按启用状态过滤。
5. 选择“最近成功”“最近失败”“暂无运行记录”“已跳过”，预期按最近运行状态过滤。
6. 选择 RSS、public_html、json_api / api，预期按 source_type 过滤。
7. 点击“只看异常源”，预期显示最近失败，或启用中但暂无运行记录的源。
8. 点击“清除筛选”，预期恢复显示全部采集源。
9. 在筛选结果中点击预览、运行单个源、启用/停用、查看该源日志，预期功能仍可用。
10. 运行 node --check 02_前端页面/crawler-collector.js。
11. 运行 git diff --check 和敏感文件跟踪检查。
```

### 当前测试状态

```text
人工测试通过。
搜索筛选通过。
启用状态筛选通过。
运行状态筛选通过。
类型筛选通过。
只看异常源通过。
清除筛选 / 显示全部通过。
筛选统计“显示 X / 共 Y 个”通过。
筛选后源卡片预览、运行、启用/停用、查看该源日志通过。
JS 语法检查通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
git diff --check 如仅有 LF/CRLF 换行提示，可接受，无空白错误。
本次没有修改后端。
本次没有新增接口。
本次没有修改数据库结构。
本次没有修改 collector_data.db。
本次没有删除或批量删除采集源。
本次不涉及真实平台抓取、cookie、token、账号、密钥。
```

### 下次继续建议

```text
1. 本阶段可执行最终检查、确认提交范围、commit 并 push。
2. 后续如确实需要清理测试源，再单独设计删除或批量删除功能，并加入更严格确认流程；当前第五期不做删除。
```

### 安全边界

```text
本次不修改后端代码。
本次不新增后端接口。
本次不修改数据库结构。
本次不修改 collector_data.db。
本次不删除或批量删除采集源。
本次不提交 .db / .sqlite / .sqlite3。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不写入平台账号、cookie、token、密钥。
本次不抓取小红书、美团、大众点评真实内容。
本次不绕过登录、验证码、反爬。
本轮进入提交和推送流程。
```

## 2026-06-12 外部平台每日采集功能第四期：采集源最近运行状态与错误提示增强

### 本次功能名称

```text
采集源最近运行状态与错误提示增强。
```

### 本次完成内容

```text
1. 采集源列表加载时复用现有运行日志接口读取最近运行记录。
2. 前端建立 source_id -> latestRun 映射，不修改数据库结构。
3. 采集源卡片摘要行新增最近运行状态标签。
4. 采集源展开详情中新增最近运行摘要，显示状态、新增条数、结束时间。
5. failed 状态会在源卡片中显示错误原因摘要，完整错误仍可在运行日志中查看。
6. 单源运行后自动展开对应源卡片，刷新采集源、采集条目和运行日志。
7. 全部运行后保留原汇总提示，并刷新采集源、采集条目和运行日志。
8. 采集源详情中新增“查看该源日志”按钮，复用 GET /collector-admin/runs?source_id={id}。
9. 运行日志区域支持显示“正在查看：某采集源的运行日志”，并提供“查看全部运行日志”入口。
10. 如果最近 50 条运行日志未覆盖某个源，源卡片显示“暂无运行记录”，不额外批量请求所有源日志。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 新增 collectorLatestRunsBySource，用于缓存最近运行日志映射。
- loadCollectorSources 读取采集源时同步读取最近运行日志，日志读取失败不影响源列表显示。
- 新增 fetchCollectorRuns、updateCollectorLatestRuns、viewCollectorRunsForSource。
- 新增采集源运行状态标签、运行摘要和错误原因摘要渲染函数。
- 新增查看该源日志 / 查看全部运行日志的 document 级事件委托。
- 单源运行后自动展开并定位到对应采集源。

02_前端页面/crawler.html
- 更新采集源列表和运行日志区域说明，提示可查看最近运行状态和按源日志。

02_前端页面/style.css
- 新增 collector-run-summary、collector-run-status-*、collector-run-error、collector-run-filter-note 样式。

项目交接与续接总文档.md
- 追加本次第四期修改记录。
```

### 接口变化

```text
新增接口：无。
复用既有接口：
GET /collector-admin/sources
GET /collector-admin/runs
GET /collector-admin/runs?source_id={id}
POST /collector-admin/sources/{source_id}/run
POST /collector-admin/run-all
```

### 前端变化

```text
1. 采集源卡片显示最近运行状态：成功、失败、已跳过、未知或暂无运行记录。
2. 成功状态显示新增条数和结束时间。
3. 失败状态显示错误原因摘要，避免卡片被长错误撑开。
4. “查看该源日志”会加载该 source_id 对应运行日志，并滚动到运行日志区域。
5. 运行日志筛选状态下显示当前查看的采集源，并提供“查看全部运行日志”入口。
```

### 后端变化

```text
无。本次不修改后端代码，不新增接口。
```

### 数据库变化

```text
无。本次不修改数据库结构，不修改 collector_data.db。
```

### 测试步骤

```text
1. 启动后端并打开 02_前端页面/crawler.html。
2. 使用管理员身份进入采集管理。
3. 展开采集源列表，确认源卡片摘要行显示最近运行状态标签。
4. 展开单个采集源，确认详情中显示最近运行状态、新增条数、结束时间。
5. 点击“运行单个源”，确认运行后该源卡片自动展开并刷新最近运行状态。
6. 使用错误 URL 或异常源测试失败场景，确认源卡片显示 failed 和错误原因摘要。
7. 点击“查看该源日志”，确认运行日志区域显示该源日志，并出现“查看全部运行日志”入口。
8. 点击“查看全部运行日志”，确认恢复最近运行日志总览。
9. 点击“运行全部启用源”，确认汇总提示保留，源卡片和运行日志刷新。
10. 运行 node --check 02_前端页面/crawler-collector.js。
11. 运行 git diff --check 和敏感文件跟踪检查。
```

### 当前测试状态

```text
人工测试通过。
采集源卡片最近运行状态展示通过。
正常源 success 状态展示通过。
失败源 failed 状态和错误摘要展示通过。
单源运行后源卡片刷新通过。
运行全部后汇总消息、源卡片和运行日志刷新通过。
查看该源日志通过。
查看全部运行日志通过。
JS 语法检查通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
git diff --check 如仅有 LF/CRLF 换行提示，可接受，无空白错误。
本次没有修改后端。
本次没有新增接口。
本次没有修改数据库结构。
本次没有修改 collector_data.db。
本次不涉及真实平台抓取、cookie、token、账号、密钥。
```

### 下次继续建议

```text
1. 本阶段可执行最终检查、确认提交范围、commit 并 push。
2. 如后续源数量很多，可考虑新增后端聚合接口一次性返回每个源的最新运行记录；当前第四期不新增接口。
```

### 安全边界

```text
本次不修改后端代码。
本次不新增后端接口。
本次不修改数据库结构。
本次不修改 collector_data.db。
本次不提交 .db / .sqlite / .sqlite3。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不写入平台账号、cookie、token、密钥。
本次不抓取小红书、美团、大众点评真实内容。
本次不绕过登录、验证码、反爬。
本轮进入提交和推送流程。
```

## 2026-06-12 外部平台每日采集功能第三期：采集条目转线索后的审核跳转增强

### 本次功能名称

```text
采集条目转线索后的审核跳转增强。
```

### 本次完成内容

```text
1. 采集条目执行“转入线索库”成功后，前端接收后端返回的 clue_id。
2. 如果返回 clue_id，成功提示中显示“已转入线索库，等待审核”和“线索 ID”。
3. 成功提示中新增“查看线索详情”入口，跳转到 clue-detail.html?id={clue_id}。
4. 成功提示中新增“进入审核中心”入口，跳转到 admin.html。
5. 如果后端未返回 clue_id，保持原来的成功提示，不报错。
6. 操作成功后仍刷新采集条目列表，new 条目转为 reviewed 后不再显示重复操作按钮。
7. reviewed 条目仍只显示“已转入线索库，等待审核”，不伪造线索详情链接。
8. 本次只做本次成功操作后的临时跳转提示，不做 collector_items 与 clues 的永久关联。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 修改 transferCollectorItemToClue 成功处理逻辑，读取 /collector-admin/items/{item_id}/to-clue 返回的 clue_id。
- 新增 collectorClueTransferMessage，用固定文案和数字 clue_id 渲染临时跳转入口。
- 保留后端错误 detail 的页面可见提示逻辑。

02_前端页面/style.css
- 新增 collector-transfer-actions 样式，用于成功提示中的跳转按钮行。
- 新增 collector-transfer-meta 样式，用于显示线索 ID。

项目交接与续接总文档.md
- 追加本次第三期修改记录。
```

### 接口变化

```text
新增接口：无。
复用既有接口：POST /collector-admin/items/{item_id}/to-clue。
```

### 前端变化

```text
1. 转入线索库成功后，如果后端返回 clue_id，页面提供“查看线索详情”和“进入审核中心”两个临时跳转入口。
2. 查看线索详情跳转到 clue-detail.html?id={clue_id}。
3. 进入审核中心跳转到 admin.html。
4. 未对 reviewed 条目做永久详情链接展示，避免在 collector_items 无 clue_id 字段时产生误关联。
```

### 后端变化

```text
无。本次不修改后端代码，不新增后端接口。
```

### 数据库变化

```text
无。本次不修改数据库结构，不修改 collector_data.db，不把 clue_id 写入 collector_items。
```

### 测试步骤

```text
1. 启动后端，打开 http://127.0.0.1:8000/docs 确认接口可访问。
2. 本地打开 02_前端页面/crawler.html，并确保已使用管理员身份登录。
3. 准备一个 status=new 的采集条目。
4. 点击该条目的“转入线索库”。
5. 预期页面显示“已转入线索库，等待审核”和“线索 ID”。
6. 点击“查看线索详情”，预期进入 clue-detail.html?id={clue_id} 并能看到新建线索。
7. 返回采集管理，点击“进入审核中心”，预期进入 admin.html。
8. 确认该采集条目刷新后变为 reviewed，并且不再显示“转入线索库 / 忽略”按钮。
9. 运行 node --check 02_前端页面/crawler-collector.js。
10. 运行 git diff --check 和敏感文件跟踪检查。
```

### 当前测试状态

```text
人工复测通过。
转入线索库成功提示卡片显示通过。
线索 ID 显示通过。
查看线索详情跳转通过。
进入审核中心跳转通过。
reviewed 条目不伪造永久详情链接，通过。
JS 语法检查通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
git diff --check 如仅有 LF/CRLF 换行提示，可接受，无空白错误。
本次没有修改后端。
本次没有新增接口。
本次没有修改数据库结构。
本次没有修改 collector_data.db。
本次不涉及真实平台抓取、cookie、token、账号、密钥。
```

### 下次继续建议

```text
1. 本阶段可执行最终检查、确认提交范围、commit 并 push。
2. 后续如需 reviewed 条目长期显示对应线索详情，需要另行设计持久化关联；当前版本不修改数据库结构，暂不实现。
```

### 安全边界

```text
本次不修改后端代码。
本次不新增后端接口。
本次不修改数据库结构。
本次不修改 collector_data.db。
本次不把 clue_id 写入 collector_items。
本次不做 reviewed 条目和 clue 的永久关联。
本次不通过标题、URL、摘要倒查线索。
本次不提交 .db / .sqlite / .sqlite3。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不写入平台账号、cookie、token、密钥。
本次不抓取小红书、美团、大众点评真实内容。
本次不绕过登录、验证码、反爬。
本轮进入提交和推送流程。
```

## 2026-06-12 外部平台每日采集功能第三期问题修复：转入线索库成功后临时跳转提示可见性

### 本次修复问题

```text
人工测试发现：点击 new 状态采集条目的“转入线索库”后，后端创建线索成功，采集条目也刷新为 reviewed，但页面没有明显显示临时成功提示中的线索 ID、查看线索详情和进入审核中心入口。
```

### 问题原因判断

```text
第三期原实现把成功提示渲染到 collectorMessage。该区域位于页面上方“新增采集源”附近，而用户执行操作时视线停留在“采集条目列表”面板内。列表刷新后 reviewed 卡片可见，但上方全局提示不在当前可见区域，因此用户难以看到。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 新增 ensureCollectorItemActionMessage，在采集条目列表 collectorItemList 前动态插入临时提示区。
- 新增 scrollCollectorActionMessageIntoView，让成功提示渲染后滚动到当前可见区域。
- 调整 transferCollectorItemToClue 成功顺序：先刷新采集条目列表，再渲染临时成功提示卡片。
- 成功提示仍只使用本次接口返回的 clue_id，不写入 collector_items，不给 reviewed 条目伪造永久链接。

02_前端页面/style.css
- 新增 collector-action-message 和 collector-action-message-success 样式。
- 保留 collector-transfer-actions 和 collector-transfer-meta，用于线索 ID 和跳转按钮行。

项目交接与续接总文档.md
- 补充本次问题修复记录。
```

### 前端修复方式

```text
1. 转入线索库请求成功后，先保存后端返回体中的 clue_id。
2. 执行 loadCollectorItems 刷新采集条目列表。
3. 刷新完成后，在采集条目列表区域顶部渲染临时成功提示卡片。
4. 如果 clue_id 有效，显示“已转入线索库，等待审核”“线索 ID：{clue_id}”“查看线索详情”“进入审核中心”。
5. 查看线索详情跳转 clue-detail.html?id={clue_id}。
6. 进入审核中心跳转 admin.html。
7. 如果后端未返回 clue_id，显示普通成功提示，不报错。
8. 渲染提示后调用 scrollIntoView，让用户能看到提示。
```

### 后端变化

```text
无。本次不修改后端代码，不新增接口。
```

### 数据库变化

```text
无。本次不修改数据库结构，不修改 collector_data.db，不把 clue_id 写入 collector_items。
```

### 测试步骤

```text
1. 启动后端并打开 02_前端页面/crawler.html。
2. 使用管理员身份进入采集管理。
3. 找到一个 status=new 的采集条目。
4. 点击“转入线索库”并确认。
5. 预期采集条目列表刷新后，在列表顶部看到明显的成功提示卡片。
6. 预期提示卡片显示“已转入线索库，等待审核”和“线索 ID：{clue_id}”。
7. 点击“查看线索详情”，预期进入 clue-detail.html?id={clue_id}。
8. 返回后点击“进入审核中心”，预期进入 admin.html。
9. 确认 reviewed 条目本身仍只显示“已转入线索库，等待审核”，不显示永久线索详情链接。
10. 运行 node --check 02_前端页面/crawler-collector.js。
11. 运行 git diff --check 和敏感文件跟踪检查。
```

### 当前测试状态

```text
人工复测通过。
转入线索库成功提示卡片显示通过。
线索 ID 显示通过。
查看线索详情跳转通过。
进入审核中心跳转通过。
reviewed 条目不伪造永久详情链接，通过。
JS 语法检查通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
git diff --check 如仅有 LF/CRLF 换行提示，可接受，无空白错误。
本次没有修改后端。
本次没有新增接口。
本次没有修改数据库结构。
本次没有修改 collector_data.db。
本次不涉及真实平台抓取、cookie、token、账号、密钥。
```

### 安全边界

```text
本次不修改后端代码。
本次不新增后端接口。
本次不修改数据库结构。
本次不修改 collector_data.db。
本次不把 clue_id 写入 collector_items。
本次不做 reviewed 条目和 clue 的永久关联。
本次不提交 .db / .sqlite / .sqlite3。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不写入平台账号、cookie、token、密钥。
本轮进入提交和推送流程。
```

## 2026-06-12 外部平台采集功能第二期：真实数据快采增强最终提交状态

### 最终人工复测状态

```text
人工复测通过。
RSS 空关键词预览通过。
RSS 空关键词运行单源通过。
public_html 预览通过。
预览结果内联显示通过。
预览详情展开/收起通过。
采集源列表展开/收起通过。
展开区域便捷收起通过。
人工导入区域上移和区分通过。
人工导入保存通过。
人工导入智能识别通过。
小红书 / 美团 / 大众点评仅做 URL/手动文本辅助识别，不主动抓取平台内容。
敏感文件跟踪检查通过。
当前进入可提交状态。
```

### 前端访问说明

```text
当前前端页面仍通过本地 HTML 文件打开。
http://127.0.0.1:8000/crawler.html 返回 Not Found 不代表功能错误。
后端只提供 API 和 /docs。
后续如果要用后端 URL 直接访问前端，需要另做静态文件托管功能。
```

### 最终安全确认

```text
collector_data.db 未被 Git 跟踪。
.venv 未被 Git 跟踪。
.idea 未被 Git 跟踪。
.env 未被 Git 跟踪。
zip 未被 Git 跟踪。
没有新增 public-nav.js。
没有修改顶部导航。
没有写入平台账号、cookie、token、密钥。
没有写入系统密码。
仓库仍为 private。
```

## 2026-06-12 外部平台每日采集功能第一期：最终提交状态

### 本次完成

本次不新增功能，不修改现有业务逻辑，只补充外部平台每日采集功能第一期的最终测试状态、环境状态、提交前检查记录和可提交状态说明。

### 环境状态

```text
Python 版本：Python 3.12。
后端 .venv 已重建。
旧虚拟环境 .venv_old_broken 已删除。
后端依赖已安装。
不提交 .venv。
不提交 collector_data.db。
不提交 .venv_old_broken。
```

### 当前测试状态

```text
Python 编译检查通过。
后端启动通过。
uvicorn 日志显示每日采集任务正常。
collector_data.db 生成成功且未被 Git 跟踪。
/docs 接口检查通过。
crawler.html 页面打开通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
仓库为 private。
未提交敏感文件或虚拟环境。
```

### 本次最终检查命令

```bat
git status
git diff --check
git ls-files "*.db" "*.sqlite" "*.sqlite3"
git ls-files "*.zip"
git ls-files ".idea/*"
git ls-files ".venv/*" "venv/*" "env/*" "01_后端服务/.venv/*"
git ls-files ".env" ".env.*"
node --check 02_前端页面/crawler-collector.js
cd /d "C:\Users\Sean\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务"
.venv\Scripts\python.exe -B -m py_compile app.py scheduler.py platform_collector.py
```

### 可提交范围

```text
允许提交：
01_后端服务/app.py
01_后端服务/scheduler.py
01_后端服务/platform_collector.py
02_前端页面/crawler.html
02_前端页面/crawler-collector.js
02_前端页面/style.css
项目交接与续接总文档.md

绝对不提交：
collector_data.db
.venv 或其他虚拟环境
任何 .db 文件
任何 .zip
.idea
.env / .env.*
.venv_old_broken
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .venv_old_broken。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次只更新最终测试状态并准备提交。
```

## 2026-06-12 外部平台每日采集功能第二期：采集条目审核入线索库闭环

### 本次完成

本次完成“采集条目审核入线索库闭环”最小功能包。采集条目现在可以由管理员从 `collector_items` 转入主系统线索库等待审核，也可以标记忽略。未修改主业务数据库结构，未修改 `collector_data.db` 表结构，未新增任何敏感平台抓取逻辑，未新增 `public-nav.js`，未修改顶部导航。

### 修改文件

```text
01_后端服务/app.py
- 新增采集条目转入线索库接口。
- 新增采集条目标记忽略接口。
- 复用现有 require_admin_token / require_admin_user 管理员权限校验。
- 按当前 Clue 模型真实字段映射采集条目到线索库。

01_后端服务/platform_collector.py
- 新增 get_collector_item。
- 新增 update_collector_item_status。
- 新增 mark_collector_item_reviewed。
- 新增 mark_collector_item_ignored。
- 只更新 collector_items.status，不修改 collector_data.db 表结构。

02_前端页面/crawler.html
- 在采集条目列表区域增加说明：采集条目可转入线索库等待审核，也可标记忽略。

02_前端页面/crawler-collector.js
- 采集条目卡片中 new 状态显示“转入线索库”和“忽略”按钮。
- reviewed 状态显示“已转入线索库，等待审核”。
- ignored 状态显示“已忽略”。
- 新增转入线索库和标记忽略的点击处理、确认提示、处理中状态、成功/失败提示和列表刷新。

02_前端页面/style.css
- 新增采集条目操作按钮区样式。
- 新增 reviewed / ignored 状态提示样式。

项目交接与续接总文档.md
- 追加本次采集条目审核入线索库闭环记录。
```

### 新增接口

```text
POST /collector-admin/items/{item_id}/to-clue
权限：管理员 token。
请求体：
{
  "token": "管理员 token"
}
功能：
1. 读取 collector_items 中指定条目。
2. 只允许 status=new 的条目转入线索库。
3. 根据当前 Clue 模型字段创建线索：
   - title <- collector_items.title
   - category <- 外部线索
   - source_platform <- collector_items.platform，缺失时使用 source_name 或 外部平台采集
   - source_url <- collector_items.url
   - summary <- collector_items.summary
   - status <- 待核验
4. Clue 创建成功后，将 collector_items.status 更新为 reviewed。
5. 已 reviewed 或 ignored 的条目会拒绝重复转入。
返回：
{
  "message": "已转入线索库，等待审核",
  "collector_item_id": 1,
  "collector_item_status": "reviewed",
  "clue_id": 1
}

POST /collector-admin/items/{item_id}/ignore
权限：管理员 token。
请求体：
{
  "token": "管理员 token"
}
功能：
1. 读取 collector_items 中指定条目。
2. status=reviewed 的条目不能标记忽略。
3. status=new 的条目更新为 ignored。
4. 不删除采集条目。
返回：
{
  "message": "已标记忽略",
  "collector_item_id": 1,
  "collector_item_status": "ignored"
}
```

### 前端变化

```text
1. 采集条目列表区域增加操作说明。
2. new 状态采集条目显示“转入线索库”“忽略”按钮。
3. 点击“转入线索库”会弹出确认提示，调用 POST /collector-admin/items/{item_id}/to-clue。
4. 点击“忽略”会弹出确认提示，调用 POST /collector-admin/items/{item_id}/ignore。
5. 操作过程中按钮显示“处理中...”并禁用。
6. 操作成功后显示明确提示并刷新采集条目列表。
7. 后端 detail 错误会显示在页面提示中，不只写 console。
```

### 后端变化

```text
1. 采集条目入线索库复用现有 Clue 模型和 /clues 创建逻辑的字段含义。
2. 不直接进入真实库，统一进入线索库“待核验”审核流程。
3. 只有 status=new 的采集条目可以转入线索库。
4. 只有 Clue 创建成功后才更新 collector_items.status 为 reviewed。
5. 标记忽略只更新 collector_items.status 为 ignored，不删除数据。
6. 所有新增接口都要求管理员 token。
```

### 测试步骤

```text
1. 启动后端。
2. 打开 http://127.0.0.1:8000/docs，确认新增接口存在。
3. 本地打开 02_前端页面/crawler.html。
4. 使用管理员账号登录。
5. 通过运行采集源或人工导入准备一个 status=new 的采集条目。
6. 展开“采集条目列表”。
7. 点击 new 条目的“转入线索库”。
8. 确认弹出提示后继续。
9. 预期页面提示“已转入线索库，等待审核”，该采集条目变为 reviewed。
10. 打开审核中心或 GET /clues，确认新增线索存在且 status=待核验。
11. 对另一个 status=new 条目点击“忽略”。
12. 预期页面提示“已标记忽略”，该采集条目变为 ignored。
13. 对 reviewed 条目再次尝试操作，预期后端拒绝重复转入或忽略，并在页面显示明确错误。
14. 使用非管理员或过期 token 操作，预期页面显示权限错误。
```

### 当前测试状态

```text
人工前后端测试通过。
/docs 新增接口检查通过。
crawler.html 页面操作通过。
转入线索库功能通过。
忽略功能通过。
新线索进入待核验流程通过。
JS 语法检查通过。
Python 编译检查通过。
敏感文件跟踪检查通过。
当前进入可提交状态。
git diff --check 仅有 LF/CRLF 换行提示，无 trailing whitespace、conflict marker 或其他空白错误。
不需要提交 collector_data.db。
不需要提交 .venv。
不需要提交任何 .db / .sqlite / .sqlite3 / .zip / .idea / .env。
本次仍然不涉及小红书/美团/大众点评真实抓取，不绕过登录、验证码、反爬。
```

### 下次继续建议

```text
1. 本阶段可提交并推送。
2. 后续可考虑为 reviewed 条目展示对应 clue_id，但这需要额外持久化映射；当前版本不修改数据库结构，暂不实现。
```

### 安全边界

```text
本次不修改主业务数据库结构。
本次不修改 collector_data.db 表结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不写入平台账号、cookie、token、密钥。
本次不抓取小红书、美团、大众点评真实内容。
本次不绕过登录、验证码、反爬。
本轮进入提交和推送流程。
```

## 2026-06-12 外部平台采集第二期收尾：人工导入智能识别

### 本次新增

本次只补充“人工导入公开信息（不抓取平台，仅手动保存内容）”区域的智能识别能力，不新增违规抓取逻辑，不修改业务数据库结构，不改顶部导航，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 人工导入区域新增“粘贴公开文本（可选，用于智能识别标题和摘要）”textarea。
2. 人工导入区域新增“智能识别人工导入信息”按钮。
3. 智能识别按钮使用 type="button" 和 data-action="manual-detect"。
4. 智能识别点击走 crawler-collector.js 的 document 级事件委托，不触发新增采集源表单。
5. 新增管理员接口 POST /collector-admin/items/manual/detect。
6. 该接口只根据 URL 域名和用户手动粘贴的公开文本识别平台、标题、摘要和默认备注。
7. URL 可为空，raw_text 可为空；URL 和 raw_text 都为空时返回“请填写 URL 或粘贴公开文本后再识别”。
8. 小红书、美团、大众点评只做 URL / 手动文本辅助识别，不主动抓取平台页面内容。
9. 普通公开网页本次也不主动访问外部 URL；URL 仅用于域名和平台推断。
10. 识别不会写入数据库，不保存 raw_text，不记录原文全文。
11. 人工导入保存仍调用 POST /collector-admin/items/manual。
12. 人工导入保存字段仍为 platform、title、url、summary、notes，不把 raw_text 全量保存进数据库。
13. 保存成功后继续显示“人工导入保存成功，已加入采集条目”，并刷新采集条目列表。
14. crawler-collector.js 引用版本更新为 v=20260612-manual-detect。
```

### 接口说明

```text
POST /collector-admin/items/manual/detect

请求体：
{
  "token": "管理员 token",
  "platform": "",
  "title": "",
  "url": "https://example.com/xxx",
  "summary": "",
  "notes": "",
  "raw_text": "用户手动粘贴的公开文本"
}

返回：
{
  "ok": true,
  "platform": "小红书 / 美团 / 大众点评 / 微信公众号 / 中山大学 / 广州大学 / 番禺区政府 / 域名 / 人工导入",
  "title": "从粘贴文本中识别出的标题，或 URL 平台公开网页标题",
  "summary": "从粘贴文本前部生成的摘要",
  "notes": "人工导入，来源由智能识别辅助填写",
  "message": "识别成功",
  "warning": "小红书、美团、大众点评等平台的合规提示，普通来源为空"
}
```

### 修改文件

```text
01_后端服务/app.py
- 新增 CollectorManualItemDetectRequest。
- 新增人工导入识别辅助函数：文本清洗、URL 平台推断、标题候选选择、摘要生成。
- 新增 POST /collector-admin/items/manual/detect，使用管理员权限校验，不保存数据库，不访问外站。

02_前端页面/crawler.html
- 人工导入区域新增粘贴公开文本 textarea。
- 人工导入区域新增“智能识别人工导入信息”按钮。
- crawler-collector.js 引用版本更新为 v=20260612-manual-detect。

02_前端页面/crawler-collector.js
- 新增 data-action="manual-detect" 的 document 级事件委托。
- 新增 detectManualCollectorItem，调用 /collector-admin/items/manual/detect。
- 识别成功后填入平台、标题、摘要；备注为空时填入默认说明。
- 保存人工导入时仍只提交 platform、title、url、summary、notes；保存成功后清空粘贴文本框。

02_前端页面/style.css
- 新增人工导入按钮区和字段说明样式。

项目交接与续接总文档.md
- 追加本次人工导入智能识别记录。
```

### 人工复测建议

```text
1. 打开 crawler.html，并确保后端 http://127.0.0.1:8000 已启动。
2. 在人工导入公开信息区域，将来源平台留空，URL 可留空。
3. 粘贴公开文本：
   大学城贝岗附近新开的糖水店体验
   今天路过贝岗发现一家新开的糖水店，环境比较干净，价格也适合学生党。晚上人比较多，适合和同学一起去。
4. 点击“智能识别人工导入信息”。
5. 预期平台为“人工导入”，标题和摘要从粘贴文本填入。
6. 填入美团或大众点评 URL，并粘贴手动整理文本后再次识别。
7. 预期平台识别为美团或大众点评，并显示“仅根据 URL 和手动粘贴文本辅助识别，不主动抓取平台内容”的合规提示。
8. 填入 https://www.sysu.edu.cn/news/ 且不粘贴文本后识别。
9. 预期平台识别为中山大学，页面不崩，可提示补充粘贴文本。
10. 点击“保存到采集条目”。
11. 预期仍调用 POST /collector-admin/items/manual，保存成功后刷新采集条目列表。
```

### 当前测试状态

```text
Codex 静态检查通过，等待人工复测人工导入智能识别。
Python 编译检查通过。
JS 语法检查通过。
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不抓付费墙内容。
本次不调用小红书、美团、大众点评非公开接口。
本次不伪造 App 请求。
本次不主动抓取小红书、美团、大众点评页面内容。
本次不主动访问外部 URL 提取正文。
本次不保存 raw_text 原文到数据库。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```

## 2026-06-12 外部平台采集第二期页面体验收尾：便捷收起与人工导入修复

### 本次完成

本次只做采集管理页前端交互和人工导入触发修复，不新增采集逻辑，不修改业务数据库结构，不改顶部导航，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 每个折叠面板展开后，在面板底部增加“收起本区”按钮。
2. “收起本区”使用面板内部 sticky 底部工具条，不做全页面 fixed，窄屏下改为普通整行按钮，避免遮挡表单输入。
3. 点击“收起本区”只收起当前区域，并平滑滚动到该区域标题附近，不影响其他区域状态。
4. 采集源列表、采集条目列表、运行日志、预览抓取结果等长区域均支持底部便捷收起。
5. 采集源单项详情底部新增“收起该源”按钮。
6. 点击“收起该源”只收起当前采集源详情，并平滑滚动到该源摘要行附近；该源下方预览结果跟随详情区隐藏。
7. 预览详情展开后，详情底部新增“收起预览详情”按钮。
8. 点击“收起预览详情”只收起详情列表，保留候选数量、命中数量、可入库数量和 reason 摘要，不清空预览结果。
9. 人工导入按钮改为 type="button" 且使用 data-action="manual-import"，由 document 级事件委托触发，不再依赖 inline onclick。
10. 人工导入请求继续调用 POST http://127.0.0.1:8000/collector-admin/items/manual。
11. 人工导入请求字段为 platform、title、url、summary、notes，并携带管理员 token，方式与其他 collector-admin 接口一致。
12. 人工导入标题为空时显示“人工导入失败：标题不能为空”。
13. 人工导入标题不为空时，保存成功显示“人工导入保存成功，已加入采集条目”。
14. 人工导入平台为空时，前端默认写入“人工导入”。
15. 人工导入 URL 允许为空，保存时不访问外部 URL。
16. 人工导入保存成功后刷新采集条目列表，并自动展开采集条目列表。
17. 后端返回错误时继续显示 detail 或 message；后端不可达时显示“无法连接后端，请确认 http://127.0.0.1:8000 已启动”。
18. 排查确认人工导入问题主要在前端触发方式：按钮缺少 type 和明确 data-action，可能与页面其他按钮/表单行为混淆；后端接口和字段已匹配，无需修改后端。
```

### 修改文件

```text
02_前端页面/crawler.html
- 为所有 collector-panel 面板体底部增加“收起本区”按钮。
- 人工导入保存按钮改为 type="button" + data-action="manual-import"。
- crawler-collector.js 引用版本更新为 v=20260612-collapse-manual-fix。

02_前端页面/crawler-collector.js
- 新增 collapse-section、collapse-source-detail、collapse-preview-detail、manual-import 的 document 级事件委托。
- 新增区域收起后滚动到标题、采集源详情收起后滚动到摘要、预览详情收起后滚动到预览摘要。
- 采集源详情底部渲染“收起该源”。
- 预览详情底部渲染“收起预览详情”。
- 人工导入平台为空时默认写入“人工导入”。

02_前端页面/style.css
- 新增 collector-collapse-row、collector-collapse-floating、collector-source-collapse-row 样式。
- 窄屏下收起按钮改为普通整行按钮，避免遮挡输入区。

项目交接与续接总文档.md
- 追加本次第二期页面体验收尾记录。
```

### 接口确认

```text
POST /collector-admin/items/manual
权限：管理员 token。
请求字段：platform、title、url、summary、notes。
行为：只写入 collector_items，不访问外部 URL。
title 为空时返回“标题不能为空”。
source_id 可为空，人工导入写入 NULL。
status 默认为 new。
写入后可通过 GET /collector-admin/items 查询。
```

### 当前测试状态

```text
Codex 静态检查通过，等待人工复测悬浮收起和人工导入
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不抓付费墙内容。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```
## 2026-06-10 外部平台采集功能第二期：真实数据快采增强

### 本次新增

本次在第一期外部平台采集框架基础上做低风险增强，目标是在合规公开数据范围内更快抓到真实可用数据。未绕过登录、验证码、反爬，未使用平台账号、cookie、token、密钥，未调用小红书、美团、大众点评非公开接口，未修改业务数据库结构，未提交数据库、虚拟环境、zip、`.idea` 或 `.env`，未新增 `public-nav.js`，未修改顶部导航布局，未执行 commit，未执行 push。

已完成：

```text
1. 增强 public_html 采集：当前 HTML 页面本身可作为候选条目。
2. public_html 当前页会提取 title、meta description、h1/h2/h3、p 标签前若干段和安全摘要。
3. public_html 增加一层同域浅抓，最多取前 20 个候选链接。
4. 浅抓只访问公开 http/https 页面，不递归超过一层。
5. 浅抓会跳过 login、passport、account、user、cart、pay、order、admin、api 等路径。
6. 浅抓会跳过 pdf、doc、xls、ppt、zip、rar、7z 等下载文件。
7. 外部请求继续使用 timeout，并在浅抓详情页之间加入很小延迟，避免高频请求。
8. 每个源每次最多新增 30 条，继续依赖 URL 和 raw_hash 去重。
9. 关键词改为 OR 匹配，命中任一关键词即可。
10. 关键词分隔支持空格、中文逗号、英文逗号、分号、顿号和换行。
11. 关键词为空时不过滤公开结果，但仍受每源最多 30 条限制。
12. 命中范围包括 title、summary、url、raw_text 的安全摘要部分。
13. 新增 POST /collector-admin/sources/{source_id}/preview，预览抓取结果但不入库。
14. 预览返回前 10 条 title、url、summary、matched_keywords、would_save，以及 candidates_count、matched_count、reason。
15. 前端采集源列表新增“预览”按钮和预览结果区域。
16. 前端新增“推荐公开源模板”区域，模板只填表，不自动新增、不默认启用。
17. 前端新增美团、大众点评、小红书合规说明。
18. 新增 POST /collector-admin/items/manual，用于管理员人工导入公开信息。
19. 前端新增“人工导入公开信息”区域，保存时不访问外部 URL。
```

### 新增或修改文件

```text
01_后端服务/platform_collector.py
- 增加 public_html 当前页候选条目提取。
- 增加 public_html 一层同域浅抓和安全 URL 过滤。
- 增加关键词 OR 匹配、空关键词不过滤、matched_keywords 计算。
- 增加 preview_source 预览逻辑。
- 增加 create_manual_item 人工导入写入 collector_items。

01_后端服务/app.py
- 新增 POST /collector-admin/sources/{source_id}/preview。
- 新增 POST /collector-admin/items/manual。

02_前端页面/crawler.html
- 新增推荐公开源模板区域。
- 新增美团/点评/小红书合规说明。
- 新增预览抓取结果区域。
- 新增人工导入公开信息区域。

02_前端页面/crawler-collector.js
- 新增公开源模板填表逻辑。
- 新增采集源预览请求和结果渲染。
- 新增人工导入公开信息保存逻辑。
- 更新空关键词展示文案。

02_前端页面/style.css
- 新增 collector 页面模板、预览、人工导入相关局部样式。

项目交接与续接总文档.md
- 追加本次真实数据快采增强记录。
```

### 测试步骤

```text
1. 打开 crawler.html，确认推荐公开源模板区域显示。
2. 点击任一模板，确认只填入表单，不自动新增采集源。
3. 新增一个 public_html 公开源，点击“预览”，确认能看到 candidates_count、matched_count、reason 和前 10 条候选结果。
4. 使用空关键词源预览，确认空关键词不过滤公开结果。
5. 运行单个 public_html 源，确认当前页命中关键词时也可入库。
6. 使用人民网 RSS 或 JSONPlaceholder API 源预览，确认预览不入库。
7. 在人工导入公开信息区域填写来源平台、标题、URL、摘要、备注，点击保存，确认采集条目中出现新条目且状态为 new。
8. 确认页面合规说明明确不爬取美团、大众点评、小红书等需要登录、动态渲染、风控或授权的平台。
```

### 本次静态检查结果

已执行：

```bat
node --check 02_前端页面/crawler-collector.js
```

结果：通过，无输出。

已执行：

```bat
cd /d "C:\Users\Sean\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务"
.venv\Scripts\python.exe -B -m py_compile app.py scheduler.py platform_collector.py
```

结果：通过，无输出。

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
等待人工测试。
```

### 安全边界

```text
本次不写入任何系统密码、cookie、token、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不抓付费墙内容。
本次不调用小红书、美团、大众点评非公开接口。
本次不伪造 App 请求。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```
## 2026-06-10 外部平台采集第二期续修：预览按钮点击无反应

### 本次修复

本次只修复 `crawler.html` 中第二期新增“预览”按钮点击无反应的问题，不新增大功能，不修改业务数据库结构，不改顶部导航，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 采集源列表中的“预览”按钮改为明确的 collector-preview-btn class。
2. 预览按钮增加 data-action="preview" 和 data-source-id。
3. 在 collectorSourceList 容器上绑定 click 事件委托，动态渲染列表后按钮仍可响应。
4. 点击预览后调用 POST /collector-admin/sources/{source_id}/preview。
5. 请求体继续携带管理员 token，方式与运行单源、运行全部源一致。
6. 点击后立即在 collectorPreviewBox 显示“正在预览...”。
7. 接口成功后渲染 candidates_count、matched_count、reason 和前 10 条候选结果。
8. 空结果会显示 reason 和空状态。
9. 接口失败或后端不可达时，页面会显示明确错误；后端不可达时仍显示“无法连接后端，请确认 http://127.0.0.1:8000 已启动”。
10. crawler.html 中 crawler-collector.js 版本号从 v=1 更新为 v=2，降低浏览器缓存导致旧脚本继续运行的概率。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 新增 bindCollectorSourceListEvents。
- 预览按钮改为事件委托响应。
- 新增 showCollectorPreviewMessage，确保加载、失败、后端不可达都显示在页面上。
- 预览按钮渲染改为 collector-preview-btn + data-source-id。

02_前端页面/crawler.html
- crawler-collector.js 引用版本更新为 v=2。

项目交接与续接总文档.md
- 追加本次预览按钮修复记录。
```

### 测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
等待人工复测预览按钮。
```

### 人工复测建议

```text
1. 强制刷新 crawler.html。
2. 选择或新增一个 public_html 源，例如 https://www.sysu.edu.cn/news/。
3. 点击该源旁边的“预览”。
4. 页面应立即显示“正在预览...”。
5. 随后显示预览结果或明确失败原因。
6. 再点击 http://www.people.com.cn/rss/society.xml 这类 RSS 源的“预览”，确认也有结果或明确提示。
7. 页面不能无反应，不能只在 console 报错。
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```
## 2026-06-10 外部平台采集第二期续修：预览按钮 document 级委托

### 本次再次修复

本次继续只修复 `crawler.html` 中“预览”按钮点击无反应的问题，不新增其他功能，不修改业务数据库结构，不改顶部导航，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 预览按钮不再依赖 inline onclick。
2. 预览按钮渲染为 button type="button"。
3. 预览按钮包含 class="small-button btn-secondary collector-preview-btn"。
4. 预览按钮包含 data-action="preview"。
5. 预览按钮包含 data-source-id。
6. crawler-collector.js 改为 document 级 click 事件委托。
7. handler 使用 event.target.closest('[data-action="preview"], .collector-preview-btn') 定位按钮。
8. handler 找到按钮后会 preventDefault 和 stopPropagation。
9. 如果缺少 source_id，页面显示“预览失败：缺少采集源 ID”。
10. 如果有 source_id，点击后立即在 collectorPreviewBox 显示“已收到预览点击，正在请求后端... 正在预览采集源 #ID ...”。
11. previewCollectorSource 会再次显示“正在预览采集源 #ID ... 预览不会写入数据库。”。
12. token 缺失或权限错误时，页面显示“预览失败：请先以管理员身份登录”。
13. 后端不可达时，页面显示“无法连接后端，请确认 http://127.0.0.1:8000 已启动”。
14. 接口成功但无结果时，页面显示“预览完成，但没有匹配结果”，并显示 reason、candidates_count、matched_count。
15. 接口成功且有结果时，页面显示前 10 条 title、url、summary、matched_keywords。
16. 页面加载后会在预览区域显示“预览功能已加载，等待点击采集源预览按钮。”，用于确认 crawler-collector.js 已在浏览器中执行。
17. crawler.html 中 crawler-collector.js 版本号更新为 v=20260610-preview-fix-2，强制浏览器加载新脚本。
```

### 预览接口确认

```text
POST /collector-admin/sources/{source_id}/preview 已存在于 app.py。
该接口使用 require_admin_token，必须管理员权限。
source_id 不存在时返回“采集源不存在”。
接口调用 collector_preview_source，只运行抓取解析和预览统计，不写入 collector_items。
源不可访问、超时或格式不可解析时返回 reason，不让后端崩溃。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 改为 document 级事件委托。
- 新增 handleCollectorPreviewClick。
- 新增脚本加载后的预览功能可见状态。
- 预览按钮点击后立即显示页面反馈。
- 预览权限错误、后端不可达、空结果、成功结果均显示在页面上。

02_前端页面/crawler.html
- crawler-collector.js 引用版本更新为 v=20260610-preview-fix-2。

项目交接与续接总文档.md
- 追加本次 document 级预览按钮修复记录。
```

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
等待人工复测预览按钮。
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```
## 2026-06-10 外部平台采集第二期续修：预览位置与人工导入区分优化

### 本次优化

本次只做页面交互和布局优化，不新增采集逻辑，不修改业务数据库结构，不改顶部导航，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 优化预览结果显示位置。
2. 每个采集源卡片下方新增内联预览容器 collector-preview-inline-{source_id}。
3. 点击某个采集源的“预览”后，优先在该采集源卡片下方显示“正在预览采集源 #ID ...”。
4. 接口返回后，在对应卡片下方显示 candidates_count、matched_count、reason 和前 10 条预览结果。
5. 失败、后端不可达、权限错误、空结果均优先显示在对应卡片下方。
6. 保留页面总预览区作为辅助状态，但不再只依赖总预览区。
7. 点击预览后会对内联预览容器执行 scrollIntoView({ behavior: "smooth", block: "nearest" })，避免用户需要滚动到页面较远位置。
8. 继续保留 document 级事件委托，预览按钮不依赖 inline onclick。
9. 新增采集源表单增加说明：“这里用于添加 RSS / JSON API / HTML 页面采集源，需要填写 URL。”
10. 人工导入区域标题改为“人工导入公开信息（不抓取平台，仅手动保存内容）”。
11. 人工导入区域增加独立浅色边框与说明，强调“这里不是新增采集源”。
12. 人工导入标题为空时提示改为“人工导入失败：标题不能为空”，避免与新增采集源表单混淆。
```

### 修改文件

```text
02_前端页面/crawler-collector.js
- 预览消息和结果支持按 source_id 写入对应卡片下方的内联预览容器。
- 新增 collectorPreviewBoxForSource 和 scrollCollectorPreviewIntoView。
- renderCollectorSources 为每个采集源卡片渲染 collector-inline-preview 容器。
- 人工导入标题为空提示改为“人工导入失败：标题不能为空”。

02_前端页面/crawler.html
- 新增采集源表单增加标题和用途说明。
- 人工导入区域标题和说明增强，明确不抓取平台、仅手动保存内容。
- 人工导入区域增加 collector-manual-section class。

02_前端页面/style.css
- 新增 collector-inline-preview 样式。
- 新增 collector-manual-section 样式。

项目交接与续接总文档.md
- 追加本次预览位置与人工导入区分优化记录。
```

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
等待人工复测页面交互。
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```
## 2026-06-10 外部平台采集第二期续修：采集管理页折叠面板优化

### 本次优化

本次只做前端 UI 交互优化，不新增后端采集逻辑，不修改业务数据库结构，不改顶部导航，不新增 `public-nav.js`，未执行 commit，未执行 push。

已完成：

```text
1. 采集管理页改为后台管理面板式折叠布局。
2. 页面拆分为：新增采集源、推荐公开源模板、人工导入公开信息、采集源列表、预览抓取结果、采集条目列表、运行日志。
3. 每个区域标题右侧都有“展开 / 收起”按钮。
4. 默认展开：新增采集源、推荐公开源模板、人工导入公开信息。
5. 默认折叠：采集源列表、采集条目列表、运行日志。
6. 新增通用折叠交互，使用 document 级事件委托和 data-action="toggle-section"，不依赖 inline onclick。
7. 采集源列表改为简洁摘要行，默认只显示类型、启用状态、名称、平台、短 URL 和操作按钮。
8. 每个采集源支持单项“展开 / 收起”，展开后显示关键词、备注、最近运行时间、完整 URL 和内联预览区。
9. 点击某个源的“预览”时，会自动展开采集源列表区域和该源详情区。
10. 预览结果仍显示在对应采集源卡片下方。
11. 预览结果顶部只显示候选数量、命中数量、可入库数量和 reason。
12. 前 10 条预览详情默认折叠，支持“展开预览详情 / 收起预览详情”。
13. 人工导入公开信息区域上移到采集源列表前，默认展开，并使用独立边框样式突出。
14. 人工导入保存成功提示为“人工导入保存成功，已加入采集条目”。
15. 人工导入保存成功后自动展开采集条目列表。
16. 采集条目列表标题显示“采集条目列表（共 N 条）”。
17. 运行日志标题显示“运行日志（最近 N 条）”。
18. crawler-collector.js 版本号更新为 v=20260610-panel-ui，降低浏览器缓存旧脚本的概率。
```

### 修改文件

```text
02_前端页面/crawler.html
- 重组采集管理页为多个 collector-panel 折叠面板。
- 推荐公开源模板独立成面板。
- 人工导入区域上移到采集源列表前，并默认展开。
- 采集源列表、采集条目列表、运行日志默认折叠。
- crawler-collector.js 引用版本更新为 v=20260610-panel-ui。

02_前端页面/crawler-collector.js
- 新增通用 section 展开/收起事件委托。
- 新增采集源单项详情展开/收起状态管理。
- 采集源列表渲染改为简洁摘要 + 详情区。
- 点击预览时自动展开采集源列表和对应源详情区。
- 预览结果渲染改为摘要 + 可折叠详情。
- 人工导入成功后自动展开采集条目列表。
- 采集源、采集条目、运行日志标题增加数量摘要。

02_前端页面/style.css
- 新增 collector-panel、collector-panel-header、collector-section-collapsed 样式。
- 新增采集源简洁列表、操作按钮区、详情区样式。
- 新增预览详情折叠区样式。
- 保留并强化人工导入区域独立样式。

项目交接与续接总文档.md
- 追加本次采集管理页折叠面板优化记录。
```

### 当前测试状态

```text
Codex 静态检查通过。
Python 编译检查通过。
JS 语法检查通过。
等待人工复测页面折叠交互。
```

### 安全边界

```text
本次不写入任何系统密码、token、cookie、密钥。
本次不写入平台账号、cookie、token、密钥。
本次不绕登录、不绕验证码、不绕反爬。
本次不抓私密内容。
本次不修改业务数据库结构。
本次不提交 collector_data.db。
本次不提交 .venv。
本次不提交 .idea。
本次不提交 zip。
本次不提交 .env。
本次不新增 public-nav.js。
本次不改顶部导航。
本次不 commit。
本次不 push。
```

## 2026-06-15 外部平台每日采集功能第六期：采集条目来源筛选与采集源联动定位

### 功能名称

采集条目来源筛选与采集源联动定位。

### 完成内容

1. 在采集条目列表工具栏增加平台筛选输入框，调用既有 `GET /collector-admin/items?platform={platform}`。
2. 在采集条目列表工具栏增加来源采集源下拉框，复用前端已加载的采集源列表，显示源名称，value 使用 `source_id`，调用既有 `GET /collector-admin/items?source_id={id}`。
3. 增加采集条目“清除筛选”按钮，一次清除 keyword、status、platform、source_id，并恢复默认条目列表。
4. 保留既有关键词搜索、status 筛选、limit/offset、转入线索库、忽略逻辑。
5. 增加当前筛选提示：无筛选时显示“当前显示全部采集条目”，有筛选时显示状态、平台、来源等条件。
6. 在采集源卡片详情区新增“查看该源采集条目”按钮。
7. 点击“查看该源采集条目”后，自动展开采集条目列表区域，设置 `source_id` 筛选，调用 `loadCollectorItems()`，滚动到采集条目列表区域，并显示“正在查看：{源名称} 的采集条目”。
8. 优化条目卡片来源显示：有 `source_name` 时显示源名称和 ID；只有 `source_id` 时显示采集源 ID；没有 `source_id` 时显示“人工导入 / 无采集源”。

### 修改文件

```text
02_前端页面/crawler.html
02_前端页面/crawler-collector.js
02_前端页面/style.css
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。复用既有：

```text
GET /collector-admin/items
```

### 前端变化

1. `crawler.html` 的采集条目列表工具栏新增平台输入、来源下拉、清除筛选按钮和当前筛选提示。
2. `crawler-collector.js` 新增采集条目筛选状态读取、来源下拉渲染、清除筛选、采集源联动查看条目和来源显示辅助函数。
3. 采集源详情区新增“查看该源采集条目”按钮，不影响预览、运行单个源、启用/停用、查看该源日志、展开/收起。
4. `style.css` 少量补充采集条目筛选工具栏、当前筛选提示和来源标签样式。

### 后端变化

无。

### 数据库变化

无。未修改数据库结构，未修改 `collector_data.db`。

### 批量操作

无。未新增批量转入线索库、批量忽略、批量删除。

### 删除操作

无。

### 测试步骤

1. 启动后端并以管理员身份打开 `02_前端页面/crawler.html`。
2. 展开“采集条目列表”，确认默认提示为“当前显示全部采集条目”。
3. 输入关键词并筛选，确认仍按既有 keyword 逻辑加载。
4. 选择 status 为 `new`、`reviewed` 或 `ignored` 并筛选，确认既有状态筛选可用。
5. 在平台输入框填写一个已有平台值，确认请求使用 `platform` 参数并刷新列表。
6. 在来源下拉选择一个采集源，确认请求使用 `source_id` 参数并刷新列表。
7. 点击“清除筛选”，确认 keyword、status、platform、source_id 全部清空，列表恢复默认。
8. 展开“采集源列表”中的某个采集源详情，点击“查看该源采集条目”，确认页面展开并滚动到采集条目列表，来源下拉自动选中该源，并显示“正在查看：{源名称} 的采集条目”。
9. 确认采集源卡片原有预览、运行单个源、启用/停用、查看该源日志、展开/收起仍可使用。
10. 检查条目卡片来源显示：有 `source_name` 时显示“来源：{source_name}（ID: {source_id}）”；只有 `source_id` 时显示“来源：采集源 ID {source_id}”；没有 `source_id` 时显示“来源：人工导入 / 无采集源”。

### 当前测试状态

人工复测通过。

### 下次继续建议

1. 若人工测试确认筛选和联动稳定，可继续做采集条目审核效率相关的小功能，但仍应避免批量转入、批量忽略和批量删除。
2. 如需“只看人工导入 / 无采集源”，建议另开后端筛选能力任务，不在前端伪造精确筛选。
3. 后续如增加分页 UI，可继续复用 `limit` / `offset`，不要新增接口。

### 人工测试补修记录

人工测试发现：`/collector-admin/items` 在 `collector_items` 与 `collector_sources` JOIN 后，同时使用 keyword、status、source_id 等筛选时，keyword 搜索里的 `url` 字段未限定表名，触发 SQLite 错误 `ambiguous column name: url`。

已修复：

1. `platform_collector.py` 中 `list_items()` 的筛选条件改为明确限定 `collector_items.platform`、`collector_items.source_id`、`collector_items.status`。
2. keyword 搜索改为 `collector_items.title LIKE ? OR collector_items.summary LIKE ? OR collector_items.url LIKE ?`。
3. 保持 `source_name` 返回逻辑不变。
4. 新增接口：无。
5. 后端接口变化：无。
6. 数据库结构变化：无。
7. 批量操作：无。
8. 删除操作：无。

本轮顺便修复项目启动 bat：

1. 找到并修改已有 `打开后端.bat`，未新建 bat 文件。
2. 修复原因：原脚本使用全局 `py` / `python` 启动 uvicorn，可能导致 `No module named uvicorn`。
3. 修复方式：启动后端时优先且仅使用 `01_后端服务\.venv\Scripts\python.exe` 运行 `-m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
4. 如果未找到后端虚拟环境 Python，脚本会给出明确提示并退出。

当前测试状态：人工复测通过。

### 人工复测二次补修记录

人工复测发现：点击采集源卡片里的“查看该源采集条目”后，页面会跳转并自动选择来源，但部分源显示 `采集条目列表（共 0 条）` 和 `暂无采集条目`，容易误判为来源联动查询失效。

诊断结果：

1. 前端按钮传入的 `source.id` 正确，来源下拉框 `collectorItemSource` 的 value 也是对应 `source_id`。
2. `loadCollectorItems()` 会把 `source_id` 拼入 `/collector-admin/items` 请求。
3. 后端 `list_items()` 已使用 `collector_items.source_id = ?`，count 查询和 rows 查询使用同一 WHERE 条件。
4. 只读检查 `collector_data.db` 发现当前历史数据中，`collector_items` 总数 56 条，其中 5 条 `source_id` 为空，只有 `source_id=22` 有 30 条、`source_id=23` 有 21 条；例如 `source_id=17` 当前确实没有采集条目。
5. 前端联动查看某源时原本会保留已有 keyword、status、platform 筛选条件，这些旧条件与 `source_id` 叠加后也可能导致结果为 0。

已修复：

1. `viewCollectorItemsForSource()` 点击来源联动时，先清空采集条目列表中的 keyword、status、platform，只保留目标 `source_id`。
2. 来源筛选为空时的空状态改为源级提示：该采集源暂无采集条目，并提示同名采集源的历史条目不会合并显示。
3. 新增接口：无。
4. 数据库结构变化：无。
5. 后端变化：无。
6. 批量操作：无。
7. 删除操作：无。

当前测试状态：人工复测通过。

### 最终人工复测状态

人工复测通过，当前进入可提交状态。

复测确认：

1. 条目关键词筛选通过。
2. 条目状态筛选通过。
3. 条目平台筛选通过。
4. 条目来源筛选通过。
5. 采集源卡片“查看该源采集条目”通过。
6. 联动时清空旧 keyword、status、platform 筛选通过。
7. `source_id=22` 的社会频道有条目源显示通过。
8. `source_id=23` 的中山大学新闻网有条目源显示通过。
9. 无条目源空状态提示通过。
10. SQL 歧义修复复测通过。
11. `keyword + status + source_id` 组合筛选不再 500。
12. `keyword + platform` 筛选不再 500。
13. 仅来源筛选不再 500。
14. 采集条目“转入线索库 / 忽略”回归正常。
15. 采集源卡片原有预览、运行单个源、启用/停用、查看该源日志、展开/收起未发现异常。
16. `打开后端.bat` 使用项目后端虚拟环境 Python 启动 uvicorn 通过，不再出现 `No module named uvicorn`，`/docs` 可正常打开。

最终状态说明：

1. 本次新增接口：无。
2. 本次数据库结构变化：无。
3. 本次批量操作：无。
4. 本次删除操作：无。
5. 本次后端变化仅为 `platform_collector.py` 中 `list_items()` SQL 字段限定修复。
6. 本次 bat 变化为 `打开后端.bat` 改用 `01_后端服务\.venv\Scripts\python.exe` 启动 uvicorn。
7. 本次不涉及真实平台抓取、cookie、token、账号、密钥。
8. JS 语法检查通过。
9. Python 编译检查通过。
10. 敏感文件跟踪检查通过。
11. `git diff --check` 仅有 LF/CRLF 换行提示，无空白错误，可接受。

## 2026-06-16 外部平台每日采集功能第七期：采集源健康诊断与安全停用辅助
### 功能名称

采集源健康诊断与安全停用辅助。

### 本次完成内容

1. 在采集源列表上方新增只读健康诊断摘要，展示采集源总数、启用源、停用源、有条目的源、启用但无条目源、最近失败源、疑似重复源、疑似测试源。
2. 前端复用现有 `GET /collector-admin/sources`、`GET /collector-admin/runs` 和 `GET /collector-admin/items?source_id={id}&limit=1`，按具体 `source_id` 读取条目 total，不新增接口。
3. 为采集源卡片增加诊断标签：重复名称、重复 URL、platform+url 重复、启用但无条目、最近失败、疑似测试源、有历史条目。
4. 扩展“只看异常源”，覆盖最近失败、启用但无条目、重复名称、重复 URL、platform+url 重复、疑似测试源，并保留原有启用但无运行记录判断。
5. 在采集源详情区增加安全治理建议，仅提示人工判断和逐个停用，不提供删除、合并、迁移入口。
6. 诊断统计请求失败时降级显示“诊断数据部分加载失败”，不阻断采集源列表渲染。

### 修改文件

```text
02_前端页面/crawler.html
02_前端页面/crawler-collector.js
02_前端页面/style.css
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。复用既有接口。

### 前端变化

1. `crawler.html` 在采集源筛选工具栏下方新增 `collectorSourceHealthSummary` 健康诊断区域，并更新 `crawler-collector.js` 引用版本为 `v=20260616-source-health`。
2. `crawler-collector.js` 新增采集源条目数缓存、重复源计算、疑似测试源识别、健康摘要渲染、诊断标签渲染和治理建议渲染。
3. `style.css` 新增健康摘要、诊断标签、治理建议提示的局部样式。

### 后端变化

无。未修改 `app.py`、`platform_collector.py`、`scheduler.py`、`models.py`、`database.py`。

### 数据库变化

无。未修改数据库结构，未写入 `collector_data.db`。

### 删除操作

无。未删除采集源，未删除采集条目。

### 合并 source_id

无。未合并任何 `source_id`。

### 迁移历史条目

无。未迁移 `collector_items.source_id`。

### 批量操作

无。未新增批量停用、批量删除、批量转入线索库、批量忽略。

### 测试步骤

1. 启动后端并以管理员身份打开 `02_前端页面/crawler.html`。
2. 展开“采集源列表”，确认健康诊断摘要正常显示。
3. 确认摘要包含总数、启用源、停用源、有条目的源、启用但无条目、最近失败、疑似重复、疑似测试源。
4. 检查 `source_id=22`、`source_id=23` 显示“有历史条目”标签。
5. 检查多个“社会频道”“中山大学新闻网”等重复源显示重复名称 / 重复 URL / platform+url 重复标签。
6. 检查启用但无条目的源显示“启用但无条目”标签。
7. 检查 `source_id=24` 显示最近失败 / 疑似测试源相关提示，且不会自动启用或停用。
8. 点击“只看异常源”，确认最近失败、启用但无条目、重复源、疑似测试源都能被筛出。
9. 点击“清除筛选”，确认恢复全部采集源。
10. 验证原有关键词筛选、启用状态筛选、运行状态筛选、类型筛选仍可使用。
11. 验证单源“查看该源采集条目”“查看该源日志”“预览”“运行单个源”“停用/启用”仍可使用。

### 当前测试状态

Codex 已完成静态检查；页面交互待人工测试。

### 下次继续建议

1. 先人工复测第七期诊断摘要、诊断标签和异常源筛选。
2. 若确认稳定，可继续做采集条目分页 UI，复用现有 `limit` / `offset`。
3. 暂不建议新增删除、合并、迁移、批量停用等高风险治理能力。

### 2026-06-16 第七期人工复测通过与可提交状态更新

第七期功能“采集源健康诊断与安全停用辅助”已经人工测试通过，当前进入可提交状态。

人工测试确认：

```text
1. 健康诊断摘要显示通过。
2. 健康诊断摘要中的采集源总数、启用源数量、停用源数量、有条目源数量、启用但无条目源数量、最近失败源数量、疑似重复源数量、疑似测试源数量显示通过。
3. 有历史条目源标签显示通过，source_id=22 社会频道、source_id=23 中山大学新闻网均可显示对应诊断标签。
4. 重复名称 / 重复 URL / platform+url 重复标签显示通过。
5. 启用但无条目标签显示通过。
6. 最近失败标签显示通过，source_id=24 人民网社会 RSS 测试可显示最近失败提示。
7. 疑似测试源标签显示通过，jsonplaceholder、人民网公开 RSS 测试、人民网社会 RSS 测试等源可显示疑似测试源提示。
8. “只看异常源”增强通过，可筛出最近失败源、启用但无条目源、重复名称 / 重复 URL / platform+url 重复源、疑似测试源。
9. “清除筛选 / 显示全部”通过。
10. 采集源原有功能回归通过：展开 / 收起、预览、运行单个源、查看该源日志、查看该源采集条目、启用 / 停用均正常。
11. JS 语法检查通过。
12. 敏感文件跟踪检查通过。
```

最终安全确认：

```text
本次新增接口：无。
本次后端变化：无。
本次数据库结构变化：无。
本次未修改 collector_data.db。
本次删除采集源：无。
本次合并 source_id：无。
本次迁移 collector_items.source_id：无。
本次批量操作：无。
本次不涉及真实平台抓取、cookie、token、账号、密钥。
git diff --check 如只有 LF/CRLF 换行提示，可接受；无空白错误。
```

## 2026-06-16 操作历史与审核审计能力：线索关键操作后端审计补强
### 功能名称

线索关键操作后端审计补强。

### 本次完成内容

1. 在 `01_后端服务/app.py` 中新增内部后端审计 helper：`record_update_history()`。
2. helper 直接写入现有 `UpdateHistory` 模型，不调用 `POST /update-history`，不新增接口。
3. helper 对 `target_type`、`action`、`operator` 做最小去空白处理；`title` 和 `detail` 允许为空。
4. helper 写入失败时回滚审计写入并返回 `None`，不破坏已经成功的主业务操作。
5. 在线索关键后端接口成功后主动写入 `UpdateHistory`，降低只依赖前端 `history-auto.js` 的漏记风险。
6. 修改 `02_前端页面/history-auto.js`，对已由后端主动审计的接口跳过前端自动记录，避免重复历史。

### 修改文件

```text
01_后端服务/app.py
02_前端页面/history-auto.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。

### 数据库结构变化

无。继续使用现有 `UpdateHistory` 表，未新增字段，未新增审计表。

### 审计覆盖接口

```text
PATCH /clues/{clue_id}
PATCH /clues/{clue_id}/status
POST /admin/clues/{clue_id}/approve
POST /clues/{clue_id}/archive
POST /clues/{clue_id}/restore
DELETE /clues/{clue_id}
POST /collector-admin/items/{item_id}/to-clue
```

### action / detail 说明

1. `PATCH /clues/{clue_id}`：action 为“编辑线索”，detail 记录被编辑字段及简要旧值/新值；摘要字段只记录“已更新”。
2. `PATCH /clues/{clue_id}/status`：action 为“修改线索状态”，detail 记录旧状态 -> 新状态。
3. `POST /admin/clues/{clue_id}/approve`：action 为“审核通过并同步真实库”，detail 记录旧状态 -> 已转入真实库，并记录真实库新 ID。
4. `POST /clues/{clue_id}/archive`：action 为“归档线索”，detail 记录旧状态 -> 已归档。
5. `POST /clues/{clue_id}/restore`：action 为“恢复线索”，detail 记录旧状态 -> 待核验。
6. `DELETE /clues/{clue_id}`：action 为“删除线索”，detail 保留删除前标题、分类、状态。
7. `POST /collector-admin/items/{item_id}/to-clue`：action 为“采集条目转入线索库”，target 为新建线索，detail 记录 collector_item id、来源平台、来源链接、分类和状态。

### 重复记录规避

`history-auto.js` 跳过上述已由后端主动审计的接口，继续保留其他对象和其他接口的前端自动记录能力，并继续跳过 `/update-history` 自身避免循环。

### 未覆盖范围

```text
采集源启停
采集条目标记忽略
运行采集源
全量强审计
POST /update-history 权限加固
全局历史页面新增能力
历史分页接口
before / after 结构化字段
```

### 测试步骤

1. 启动后端，登录管理员。
2. 在审核中心修改线索状态，进入线索详情页确认出现“修改线索状态”历史。
3. 编辑线索字段，确认出现“编辑线索”历史，且不要出现重复的同类历史。
4. 审核通过并同步真实库，确认出现“审核通过并同步真实库”历史，detail 包含真实库 ID。
5. 归档线索，确认出现“归档线索”历史。
6. 恢复线索，确认出现“恢复线索”历史。
7. 删除一条测试线索，确认全局历史中心仍能看到“删除线索”记录和删除前信息。
8. 在采集管理页将采集条目转入线索库，确认新线索历史里出现“采集条目转入线索库”。
9. 打开 `history.html`，按对象类型和对象 ID 筛选，确认后端审计记录可检索。
10. 回归确认前端 `history-auto.js` 对其他页面对象仍能自动记录。

### 当前测试状态

待人工测试。

### 下次继续建议

1. 先人工复测本次线索关键操作后端审计与重复记录规避。
2. 若确认稳定，可继续考虑采集源启停、采集条目标记忽略、运行采集源等采集模块审计。
3. 若需要更强审计，再单独规划 `POST /update-history` 权限加固和结构化 before / after 字段。

### 最终人工测试通过与可提交状态更新

功能“线索关键操作后端审计补强”已经人工测试通过，当前进入可提交状态。

人工测试确认：

```text
1. 修改线索状态历史记录通过，且不重复。
2. 编辑线索字段历史记录通过，且不重复。
3. 审核通过并同步真实库历史记录通过，且不重复。
4. 归档线索历史记录通过，且不重复。
5. 恢复线索历史记录通过，且不重复。
6. 删除测试线索后全局历史可见通过。
7. 采集条目转线索历史记录通过。
8. history-auto.js 对后端已审计接口的重复记录规避通过。
9. 业务功能回归通过。
10. Python 编译检查通过。
11. JS 语法检查通过。
12. 敏感文件跟踪检查通过。
```

最终安全确认：

```text
本次新增接口：无。
本次数据库结构变化：无。
本次新增审计表：无。
本次未修改 models.py / database.py。
本次未覆盖范围仍包括：采集源启停、采集条目标记忽略、运行采集源、全量强审计、POST /update-history 权限加固。
本次不涉及账号、cookie、token、密钥。
git diff --check 如只有 LF/CRLF 换行提示，可接受；无空白错误。
用户提出的数据管理页面收起按钮为下一轮独立 UI 小修，当前未处理。
```

## 2026-06-16 独立 UI 小修：数据管理页面收起按钮
### 功能名称

数据管理页面收起按钮。

### 本次完成内容

1. 确认数据管理页面真实入口为 `02_前端页面/manage.html`，页面标题为“数据管理中心”，顶部导航按钮文案为“数据管理”。
2. 确认页面主要大区域为“真实库数据”和“线索库数据”，当前没有收起按钮。
3. 在“真实库数据”和“线索库数据”两个主要区域标题右侧增加“收起 / 展开”按钮。
4. 默认保持当前页面原样展开，不在进入页面时隐藏重要内容。
5. 点击“收起”后仅隐藏对应区域内容；点击“展开”后恢复显示。
6. 修改 `manage.js` 增加最小事件委托和 class 切换逻辑，不影响原有加载、筛选、编辑、归档、恢复、删除能力。
7. 修改 `style.css` 增加少量数据管理区标题行和折叠隐藏样式。

### 修改文件

```text
02_前端页面/manage.html
02_前端页面/manage.js
02_前端页面/style.css
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。

### 后端变化

无。

### 数据库变化

无。

### 顶部导航变化

无。

### 测试步骤

1. 打开 `02_前端页面/manage.html`。
2. 确认“真实库数据”和“线索库数据”默认仍展开。
3. 点击“真实库数据”区域的“收起”，确认真实库列表隐藏，按钮变为“展开”。
4. 再点击“展开”，确认真实库列表恢复显示。
5. 对“线索库数据”区域重复收起 / 展开测试。
6. 点击“刷新数据”，确认折叠按钮和数据加载不互相影响。
7. 切换数据筛选“全部 / 正常 / 已归档”，确认原有筛选功能仍正常。
8. 回归测试编辑、归档、恢复、彻底删除按钮仍存在且未被改动。

### 当前测试状态

待人工测试。

### 下次继续建议

1. 先人工复测数据管理页两个主要区域的收起 / 展开交互。
2. 若后续还需要更细粒度折叠，可另开任务处理筛选区或单条卡片详情，不在本次 UI 小修扩大范围。

## 2026-06-16 线索库与审核工作流优化：线索审核筛选与详情识别增强
### 功能名称

线索审核筛选与详情识别增强。

### 本次完成内容

1. 在审核中心线索审核列表上方新增前端本地组合筛选工具栏。
2. 状态筛选支持全部状态、待核验、已转入真实库、已归档、用户核验：属实、用户核验：不准确、用户反馈：已过期。
3. 分类筛选从当前已加载线索数据动态提取 `category`，不硬编码全部分类。
4. 来源平台筛选从当前已加载线索数据动态提取 `source_platform`，不硬编码来源列表。
5. 新增快捷筛选：只看外部线索、只看有来源链接、清除筛选 / 显示全部。
6. 新增筛选统计，显示当前筛选命中数量 / 当前线索总数，并展示当前筛选条件。
7. 保留原有“只看待核验 / 只看已转入真实库 / 查看全部线索”按钮，并与新状态筛选联动。
8. 保留原有关键词搜索能力，搜索结果仍基于 `/search?keyword=` 返回结果渲染。
9. 线索卡片增强外部线索标签、状态标签、来源平台、来源链接和简短审核提示。
10. 线索详情页新增页面内“返回审核中心”按钮，并强化状态、分类、来源平台、来源链接、摘要和审核提示展示。
11. 对外部线索提示需人工核验后再转入真实库；对已转入真实库和已归档线索显示谨慎处理提示。
12. 未新增批量审核、批量删除或高风险状态迁移能力。

### 修改文件

```text
02_前端页面/admin.html
02_前端页面/app.js
02_前端页面/clue-detail.html
02_前端页面/detail.js
02_前端页面/style.css
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。继续复用既有接口。

### 后端变化

无。未修改 `01_后端服务/app.py`、`models.py`、`database.py`。

### 数据库变化

无。未修改数据库结构，未写入或提交数据库文件。

### 批量审核

无。

### 批量删除

无。

### 高风险状态迁移

无。

### 测试步骤

1. 启动后端，打开 `02_前端页面/admin.html`。
2. 确认线索审核列表上方显示状态、分类、来源平台筛选和快捷筛选按钮。
3. 分别测试状态筛选：全部状态、待核验、已转入真实库、已归档、用户核验：属实、用户核验：不准确、用户反馈：已过期。
4. 确认分类筛选项来自当前线索数据，且可筛选外部线索和其他已有分类。
5. 确认来源平台筛选项来自当前线索数据，且可按来源平台筛选。
6. 点击“只看外部线索”，确认可筛出外部采集或公开来源线索。
7. 点击“只看有来源链接”，确认仅显示有 `source_url` 的线索。
8. 点击“清除筛选 / 显示全部”，确认恢复全部线索。
9. 使用关键词搜索，确认搜索结果仍可继续组合筛选。
10. 检查线索卡片是否显示外部线索标签、状态标签、来源平台、来源链接和审核提示。
11. 进入 `clue-detail.html?id={id}`，确认页面内“返回审核中心”按钮、来源信息、状态提示和摘要展示正常。
12. 回归测试原有修改状态、审核通过、归档、删除、编辑字段、反馈和历史记录展示。

### 当前测试状态

待人工测试。

### 下次继续建议

1. 先人工复测本次审核中心筛选、卡片识别和详情页提示。
2. 若线索数量继续增长，再考虑给 `GET /clues` 增加后端分页与服务端组合筛选。
3. 若需要更可靠审计，建议单独规划后端统一操作审计，不依赖前端自动记录脚本。

### 小修记录：分类 / 来源平台下拉选项动态生成修复

人工测试发现审核中心新增筛选栏中，分类下拉和来源平台下拉只显示默认项，未显示当前线索数据中的 `category` 和 `source_platform`。

原因：`admin.html` 在 `app.js` 后继续加载 `archive-filter.js`，该脚本重新定义了 `loadHomeData()` 和 `search()`，覆盖了 `app.js` 中加载数据后刷新筛选下拉选项的调用。线索卡片仍可正常渲染，所以数据存在，但下拉选项没有被重新填充。

修复：在 `app.js` 的 `renderClues(items)` 开头调用 `renderClueFilterOptions(items)`，确保无论线索数据来自初始 `/clues` 加载、关键词搜索结果，还是清除筛选后的当前线索数组，分类和来源平台下拉都会基于当前线索数据重新生成。

当前测试状态：待人工复测。

### 最终人工测试通过与可提交状态更新

功能“线索审核筛选与详情识别增强”已经人工测试通过，当前进入可提交状态。

人工测试确认：

```text
1. 审核中心筛选栏显示通过。
2. 状态筛选通过。
3. 分类筛选通过，分类下拉能动态显示“外部线索”等实际分类。
4. 来源平台筛选通过，来源平台下拉能动态显示实际 source_platform。
5. 只看外部线索通过。
6. 只看有来源链接通过。
7. 清除筛选 / 显示全部通过。
8. 筛选统计通过。
9. 关键词搜索后动态下拉仍正常。
10. 线索卡片识别增强通过。
11. 详情页识别增强通过。
12. 原有审核中心功能回归通过。
13. JS 语法检查通过。
14. 敏感文件跟踪检查通过。
```

最终安全确认：

```text
本次新增接口：无。
本次后端变化：无。
本次数据库结构变化：无。
本次批量审核：无。
本次批量删除：无。
本次高风险状态迁移：无。
本次不涉及账号、cookie、token、密钥。
git diff --check 如只有 LF/CRLF 换行提示，可接受；无空白错误。
```

## 2026-06-16 独立 UI 小修补充：数据管理页面刷新按钮修复

### 功能名称

数据管理页面收起按钮小修补充。

### 本次修复内容

1. 人工测试确认“真实库数据”和“线索库数据”收起 / 展开正常，但顶部“刷新数据”按钮点击后缺少有效刷新反馈。
2. 将刷新按钮改为明确调用 `refreshManageData(this)`。
3. 在 `02_前端页面/manage.js` 中新增最小刷新包装函数，继续复用现有 `loadManageData()`。
4. 刷新期间按钮显示“刷新中...”，并临时禁用，刷新完成后恢复“刷新数据”。
5. 未修改 `manage-filter.js`，保留现有“全部 / 正常 / 已归档”筛选覆盖逻辑。
6. 未影响“真实库数据 / 线索库数据”的收起和展开。

### 修改文件

```text
02_前端页面/manage.html
02_前端页面/manage.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。

### 后端变化

无。

### 数据库变化

无。

### 顶部导航变化

无。

### 当前测试状态

待人工复测。

### 最终人工测试通过与可提交状态更新

功能“数据管理页面收起按钮”已经人工测试通过，当前进入可提交状态。

人工测试确认：

```text
1. 真实库数据收起 / 展开通过。
2. 线索库数据收起 / 展开通过。
3. 两个区域独立折叠通过。
4. 默认展开状态通过。
5. 刷新数据按钮修复通过。
6. 展开状态下刷新通过。
7. 收起状态下刷新通过。
8. 筛选、编辑、归档、恢复等原功能回归通过。
9. JS 语法检查通过。
10. 敏感文件跟踪检查通过。
```

最终安全确认：

```text
本次新增接口：无。
本次后端变化：无。
本次数据库结构变化：无。
本次顶部导航变化：无。
本次未新增 public-nav.js。
本次不涉及账号、cookie、token、密钥。
git diff --check 如只有 LF/CRLF 换行提示，可以记录为可接受，无空白错误。
```
## 2026-06-16 管理员用户管理 / 权限审计增强：系统密码配置化与危险操作提示收口

### 功能名称

系统密码配置化与危险操作提示收口。

### 本次完成内容

1. 后端系统管理员二次确认密码改为优先读取 `XGW_SYSTEM_ADMIN_PASSWORD` 环境变量。
2. 本地开发继续保留集中默认值，默认值仅用于本地开发；部署时应设置 `XGW_SYSTEM_ADMIN_PASSWORD`。
3. 旧接口中的局部系统密码硬编码校验已收口到统一配置和 `verify_system_password()` 校验逻辑。
4. 用户管理、备份导入、历史危险区页面不再直接展示真实系统密码明文。
5. 保留原有系统密码输入框、确认文字、验证码、双重确认和管理员 token 校验流程。
6. 未改变现有接口路径、请求体字段和业务流程。

### 修改文件

```text
01_后端服务/app.py
02_前端页面/user-admin.html
02_前端页面/user-admin.js
02_前端页面/backup-import.js
02_前端页面/history-danger-zone.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。

### 数据库结构变化

无。

### 是否新增 `.env`

无。

### 后端变化

1. 系统密码优先读取 `XGW_SYSTEM_ADMIN_PASSWORD`。
2. 本地开发保留集中默认值，并注明部署时应设置环境变量。
3. 旧硬编码校验收口到统一校验逻辑。

### 前端变化

1. 移除危险操作页面中的真实系统密码明文提示。
2. 保留系统密码输入框和现有二次确认流程。
3. 提示文案改为从部署配置或管理员处获取系统密码。

### 未处理范围

1. 不清理历史文档中既有记录。
2. 不改 token 过期机制。
3. 不改完整权限模型。
4. 不改备份导入或历史清空业务流程。
5. 不做危险操作审计补强。

### 测试步骤

1. 不设置环境变量启动后端，确认本地开发默认流程仍可使用。
2. 设置 `XGW_SYSTEM_ADMIN_PASSWORD` 为测试值并重启后端。
3. 在用户管理页使用旧二次确认密码修改角色，应失败。
4. 使用环境变量中的测试值修改角色，应成功。
5. 在备份导入页确认系统密码输入框不显示真实密码明文，验证码和双重确认仍存在。
6. 在历史危险区确认系统密码输入框不显示真实密码明文，确认文字和双重确认仍存在。
7. 确认不新增 `.env`，不修改数据库结构。

### 当前测试状态

待人工测试。

### 下次继续建议

1. 人工测试本次系统密码配置化与前端提示收口。
2. 通过后可继续规划“管理员危险操作后端审计补强”，覆盖角色修改、备份导入、历史清空等操作。
### 小修记录：历史危险区日期输入体验修复

1. 修复历史危险区“按日期清空历史”开始日期 / 结束日期输入可出现异常格式的问题。
2. 保持原生 `type="date"`，补充 `min/max` 与提交前真实日期校验，限制为合法 `YYYY-MM-DD` 日期。
3. 增强日期输入框点击主体时打开日期选择器的体验，在支持的浏览器中点击 / 聚焦会尝试调用 `input.showPicker()`。
4. 阻止日期框手动键入、粘贴、拖放造成类似异常拼接的显示状态；不支持 `showPicker()` 的浏览器会保持原生行为且不报错。
5. 未修改系统密码、确认文字、验证码、双确认流程、接口路径、请求字段、后端、数据库或顶部导航。
6. 当前测试状态：待人工复测。
### 最终人工测试通过记录：系统密码配置化与危险操作提示收口

功能“系统密码配置化与危险操作提示收口”已人工测试通过，当前进入可提交状态。

人工测试确认：
1. 后端系统密码优先读取 `XGW_SYSTEM_ADMIN_PASSWORD` 通过。
2. 环境变量覆盖默认系统密码通过。
3. 设置环境变量后，旧默认值不再通过。
4. 新环境变量值可通过。
5. 前端危险操作页面不再显示旧系统密码明文。
6. 用户管理页面提示收口通过。
7. 备份导入页面提示收口通过。
8. 历史危险区提示收口通过。
9. 历史危险区日期异常格式限制通过。
10. 日期输入框主体点击打开日期选择器体验通过。
11. 空日期、非法日期、结束日期早于开始日期校验通过。
12. 管理员 token、验证码、确认文字、双确认流程回归通过。
13. Python 编译检查通过。
14. JS 语法检查通过。
15. 敏感文件跟踪检查通过。

最终安全确认：
1. 本次新增接口：无。
2. 本次数据库结构变化：无。
3. 本次新增 `.env`：无。
4. 本次未提交 `.env`。
5. 本次顶部导航变化：无。
6. 本次未新增 public-nav.js。
7. 本次不涉及账号、cookie、token、密钥。
8. `git diff --check` 如只有 LF/CRLF 换行提示，记录为可接受，无空白错误。

后续建议：下一轮可独立做“危险操作动态验证码统一增强”，包含结束日期自动从开始日期起步、日期选择体验继续优化、所有系统密码验证处补动态验证码。

## 2026-06-17 危险操作动态验证码统一增强

### 功能名称

危险操作动态验证码统一增强。

### 本次完成内容

1. 新增通用后端动态验证码能力，支持按 purpose 生成、校验和一次性使用。
2. 将注册管理员、修改用户角色、备份导入、清空全部历史、按日期清空历史纳入动态验证码保护。
3. 保留原有管理员 token、系统密码、确认文字、双 confirm 等保护流程。
4. 保留 `/backup/import-code` 旧入口，内部复用通用验证码生成逻辑，避免破坏现有备份导入流程。
5. 历史危险区增加日期联动：选择开始日期后，结束日期 `min` 自动变为开始日期；结束日期为空或早于开始日期时自动设为开始日期。

### 修改文件

```text
01_后端服务/app.py
02_前端页面/user-widget-v2.js
02_前端页面/user-admin.html
02_前端页面/user-admin.js
02_前端页面/backup-import.js
02_前端页面/history-danger-zone.js
02_前端页面/style.css
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

```text
GET /admin/verify-code
```

用途：生成危险操作动态验证码。`purpose=admin_register` 可无管理员 token 获取；其他 purpose 必须校验管理员 token。

### 数据库结构变化

无。

### 前端变化

1. 注册管理员时显示并提交动态验证码，普通用户注册不受影响。
2. 用户管理修改角色弹窗新增动态验证码。
3. 备份导入验证码改为调用通用验证码接口，导入请求字段保持不变。
4. 清空全部历史弹窗新增动态验证码。
5. 按日期清空历史区域新增动态验证码。
6. 历史危险区日期输入增加开始日期与结束日期联动。

### 后端变化

1. 新增内存验证码存储 `ADMIN_VERIFY_CODES`。
2. 验证码绑定 purpose，支持 `admin_register`、`user_role_change`、`backup_import`、`history_clear_all`、`history_clear_range`。
3. 验证码有效期为 5 分钟。
4. 验证码校验成功后删除，避免重复使用。
5. 各当前主流程危险接口增加验证码校验。

### 未处理范围

1. 不清理旧接口。
2. 不改完整权限模型。
3. 不做 token 过期机制。
4. 不改数据库结构。
5. 不改顶部导航。
6. 不做自定义日期选择器。

### 测试步骤

1. 普通用户注册：确认不需要动态验证码。
2. 注册管理员：确认系统密码和动态验证码均必填，验证码错误或过期时失败，正确时成功。
3. 修改用户角色：确认管理员 token、系统密码、确认文字、动态验证码均生效。
4. 备份导入：确认验证码可刷新，错误验证码失败，正确验证码可进入原有导入确认流程。
5. 清空全部历史：确认验证码必填；不要实际确认清空。
6. 按日期清空历史：确认验证码必填，开始日期会联动结束日期；不要实际确认清空。
7. 确认不新增 `.env`，不修改数据库结构，不新增 public-nav.js。

### 当前测试状态

待人工测试。

### 下次继续建议

1. 人工复测所有系统密码场景的动态验证码。
2. 如需要更强安全性，可后续给验证码增加更细粒度的频率限制或审计记录。

## 2026-06-17 动态验证码统一增强人工测试小修

### 功能名称
动态验证码统一增强人工测试小修。

### 本次修复内容
1. 修复切换用户流程中取消、关闭、返回或新登录失败后，当前账号误变成未登录的问题。
2. 修复注册管理员弹窗内容过长时，关闭按钮和底部取消/注册按钮可能跑出屏幕的问题。

### 修改文件
```text
02_前端页面/user-widget.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口
无。

### 数据库结构变化
无。

### 后端变化
无。

### 当前测试状态
待人工复测。

### 下次测试重点
1. 已登录用户点击切换账号后，直接关闭弹窗或点击取消，原账号应保持登录状态。
2. 已登录用户点击切换账号后，输入错误账号或密码登录失败，原账号应保持登录状态。
3. 已登录用户点击切换账号后，新账号登录成功，才切换为新账号。
4. 退出登录按钮仍应正常清空当前登录状态。
5. 小屏幕或浏览器缩放后打开注册管理员弹窗，弹窗不应超过视口，内容可滚动，关闭按钮和底部操作按钮应可见。

## 2026-06-17 动态验证码统一增强人工复测二次修复

### 功能名称
动态验证码统一增强人工复测二次修复。

### 上一轮小修未解决的问题
1. 切换账号后取消、关闭或登录失败，当前账号仍可能变成未登录。
2. 注册管理员弹窗内容过长时，关闭按钮和取消/注册按钮仍可能跑出屏幕或不可用。

### 本轮真实原因
1. 切换账号虽然入口不再主动清空 token，但没有为切换登录弹窗保存旧 token 和旧用户信息；如果登录失败、关闭弹窗或其他异步状态刷新把界面渲染成未登录，关闭/失败路径不会显式恢复原账号。
2. 注册弹窗真实容器由 user-widget.js 的 showUserModal() 统一生成，上一轮只是把全部内容包进滚动区域并让按钮区 sticky；底部按钮仍在滚动主体内部，且 bottom 使用负值，不能保证小屏幕下始终可见。

### 本轮修复方式
1. 切换账号时使用 switchAccount 模式打开登录弹窗，保存旧 token 和旧 currentUser 快照。
2. 新账号登录成功后才清除快照并覆盖 xgw_user_token；登录失败、请求异常、取消或关闭弹窗时恢复旧 token 和旧用户显示。
3. 重构 user-widget.js 的用户弹窗 DOM 生成方式：标题和关闭按钮放入固定 header，表单放入可滚动 body，取消/确认按钮放入固定 footer。
4. 弹窗外层限制最大高度，body 设置 min-height: 0 和 overflow-y: auto，footer 不再使用 bottom 负值。

### 修改文件
```text
02_前端页面/user-widget.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口
无。

### 数据库结构变化
无。

### 后端变化
无。

### 当前测试状态
待人工复测。

### 下次测试重点
1. 已登录状态点击切换账号后直接点取消、右上角关闭、Esc 返回，原账号应保持登录。
2. 已登录状态点击切换账号后输入错误账号或密码，登录失败后原账号应保持登录。
3. 已登录状态点击切换账号后输入正确新账号，只有成功登录后才切换为新账号。
4. 明确点击退出登录仍应正常清空登录态。
5. 在小屏幕以及浏览器 125% / 150% 缩放下打开注册管理员弹窗，关闭按钮和底部取消/注册按钮应始终可见，中间表单内容可滚动。

## 2026-06-17 恢复注册管理员入口小修

### 功能名称
恢复注册管理员入口小修。

### 人工复测发现
注册弹窗缺失管理员注册入口，只显示普通注册字段，管理员注册所需的角色选择、系统密码和动态验证码区域没有出现。

### 真实原因
管理员注册入口代码仍在 user-widget-v2.js 中，但全局脚本通过 global-loader 动态加载 user-widget.js 和 user-widget-v2.js。动态脚本存在执行顺序竞争：如果 user-widget-v2.js 先执行、user-widget.js 后执行，user-widget.js 中旧版 openRegisterModal/registerUser 会覆盖 v2 的注册弹窗与提交逻辑，最终显示旧版普通注册模板。

### 修复方式
1. 在 user-widget-v2.js 中保存 v2 注册、验证码、资料编辑相关覆盖函数，并提供 installUserWidgetV2Overrides()。
2. user-widget-v2.js 自身执行和页面 load 时都会重新安装 v2 覆盖。
3. 在 user-widget.js 末尾检测 installUserWidgetV2Overrides()，如果 v2 已先加载，则在 v1 定义完成后立即重新安装 v2 覆盖。
4. 保持上一轮弹窗 header/body/footer 结构和切换账号快照保护逻辑不变。

### 修改文件
```text
02_前端页面/user-widget.js
02_前端页面/user-widget-v2.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口
无。

### 数据库结构变化
无。

### 后端变化
无。

### 当前测试状态
待人工复测。

### 下次测试重点
1. 普通注册仍应正常，不要求系统密码和动态验证码。
2. 注册弹窗应显示普通用户 / 管理员选择入口。
3. 选择管理员后，应显示系统密码输入框和 admin_register 动态验证码区域。
4. 管理员注册弹窗在小屏幕和 125% / 150% 缩放下仍应保持中间内容滚动、关闭按钮和底部取消/注册按钮可见。
5. 切换账号取消、关闭、登录失败后仍应保持原账号登录。

## 2026-06-17 危险操作动态验证码统一增强最终测试状态

### 当前状态
人工测试通过，当前进入可提交状态。

### 通过项
1. 注册管理员验证码通过。
2. 普通用户注册不受验证码影响。
3. 管理员注册入口恢复通过。
4. 管理员注册弹窗滚动与按钮可见性通过。
5. 修改用户角色验证码通过。
6. 备份导入验证码回归通过。
7. 清空全部历史验证码通过。
8. 按日期清空历史验证码通过。
9. 日期联动通过。
10. 切换账号取消、关闭、登录失败后保持原账号通过。
11. 退出登录回归通过。
12. Python 编译检查通过。
13. JS 语法检查通过。
14. 敏感文件跟踪检查通过。

### 新增接口
```text
GET /admin/verify-code
```

### 数据库结构变化
无。

### 新增 .env
无。

### 顶部导航变化
无。

### public-nav.js
未新增。

### 未处理范围
1. 不清理旧接口。
2. 不做完整权限模型重构。
3. 不做 token 过期。
4. 不改数据库。
5. 不改顶部导航。

### 下次建议
可单独做旧危险接口收口、危险操作后端审计补强、token 过期机制；建议先只读摸底后再进入开发。

## 2026-06-17 旧危险接口收口

### 功能名称

旧危险接口收口。

### 本次完成内容

1. 对旧备份导入与旧历史清空危险接口采用低风险弃用策略。
2. 旧接口保留路由定义，但进入函数后立即返回 HTTP 410 Gone。
3. 旧接口不再执行旧导入、旧清空历史等危险写入逻辑。
4. 当前主流程接口保持不变，动态验证码流程保持不变。
5. 未修改前端主流程。

### 修改文件

```text
01_后端服务/app.py
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 收口接口

```text
POST /backup/import
POST /backup/import-v2
DELETE /update-history/clear-all
DELETE /update-history/clear-all-admin
```

以上接口当前返回 HTTP 410，提示使用当前安全接口。

### 保留可用接口

```text
POST /backup/import-v3
GET /backup/import-code
GET /admin/verify-code
DELETE /update-history/clear-all-admin-v2
DELETE /update-history/clear-range-admin
```

### 数据库结构变化

无。

### 新增接口

无。

### 前端变化

未修改前端主流程。`backup.html` 当前引用 `backup-import.js` 并调用 `/backup/import-v3`；`history.html` 当前引用 `history-danger-zone.js` 并调用新版历史清空接口；`dashboard.html` 当前引用 `dashboard-v2.js`，不引用旧 `dashboard.js`。

### 后端变化

1. 两个重复定义的 `POST /backup/import` 入口均已收口为 HTTP 410。
2. `POST /backup/import-v2` 已收口为 HTTP 410。
3. `DELETE /update-history/clear-all` 已收口为 HTTP 410。
4. `DELETE /update-history/clear-all-admin` 已收口为 HTTP 410。
5. 收口提示不包含系统密码明文。

### 未处理范围

1. 未清理 `dashboard.js` 遗留旧调用。
2. 未做危险操作后端审计补强。
3. 未做 token 过期机制。
4. 未做用户禁用 / 重置密码。
5. 未做大范围权限重构。

### 测试步骤

1. 启动后端并打开 `/docs`，确认旧接口仍存在但调用返回 HTTP 410。
2. 调用 `POST /backup/import`，预期返回 HTTP 410，不执行导入。
3. 调用 `POST /backup/import-v2`，预期返回 HTTP 410，不执行导入。
4. 调用 `DELETE /update-history/clear-all`，预期返回 HTTP 410，不清空历史。
5. 调用 `DELETE /update-history/clear-all-admin`，预期返回 HTTP 410，不清空历史。
6. 在 `backup.html` 走当前备份导入主流程，确认仍调用 `/backup/import-v3`。
7. 在 `history.html` 打开危险操作区，确认清空全部历史仍调用 `/update-history/clear-all-admin-v2`，按日期清空仍调用 `/update-history/clear-range-admin`。
8. 确认 `/admin/verify-code` 与 `/backup/import-code` 仍可生成对应验证码。

### 当前测试状态

待人工测试。

### 下次建议

危险操作后端审计补强。

---

## 2026-06-18 小修记录：历史危险区确认文字校验小修

### 功能名称

历史危险区确认文字校验小修。

### 人工测试发现

历史危险区按日期清空历史时，输入页面提示要求的确认文字后仍被判定错误。

### 真实原因

现有代码虽然在比较前做了 `.trim()`，但确认文字、占位提示和错误提示分散硬编码，缺少统一来源；同时复制粘贴可能带入零宽字符，页面显示看起来是“清空历史”，实际字符串仍可能与校验值不相等。

### 修复方式

在 `history-danger-zone.js` 中新增统一确认文字常量 `HISTORY_CLEAR_CONFIRM_TEXT`，清空全部历史和按日期清空历史的 placeholder、前端校验和错误提示均使用同一常量；输入值比较前统一去除常见零宽字符并执行 `.trim()`。

### 修改文件

```text
02_前端页面/history-danger-zone.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增接口

无。

### 数据库结构变化

无。

### 后端变化

无。

### 当前测试状态

待人工复测。

### 下次测试重点

1. 清空全部历史确认文字。
2. 按日期清空历史确认文字。
3. 错误确认文字提示。
4. 正确确认文字通过。
5. 旧接口仍 410。
6. 新接口仍正常。

---

## 2026-06-18 最终收尾：旧危险接口收口验收通过

### 状态

旧危险接口收口与历史危险区确认文字校验小修均已人工测试通过，当前进入可提交状态。

### 旧接口收口测试

1. 旧 `POST /backup/import` 返回 410 通过。
2. 旧 `POST /backup/import-v2` 返回 410 通过。
3. 旧 `DELETE /update-history/clear-all` 返回 410 通过。
4. 旧 `DELETE /update-history/clear-all-admin` 返回 410 通过。
5. 旧入口不再执行旧导入 / 旧清空历史危险写入。

### 主流程保留状态

1. 主流程 `POST /backup/import-v3` 保持正常。
2. 主流程 `GET /backup/import-code` 保持正常。
3. 主流程 `GET /admin/verify-code` 保持正常。
4. 主流程 `DELETE /update-history/clear-all-admin-v2` 保持正常。
5. 主流程 `DELETE /update-history/clear-range-admin` 保持正常。

### 历史危险区确认文字小修复测

1. 历史危险区确认文字提示与实际校验一致。
2. 错误确认文字提示正常。
3. 正确确认文字通过前端校验。

### 检查状态

1. Python 编译检查通过。
2. JS 语法检查通过。
3. 敏感文件跟踪检查通过。

### 变更确认

1. 新增接口：无。
2. 数据库结构变化：无。
3. 新增 `.env`：无。
4. 顶部导航变化：无。
5. `public-nav.js`：未新增。

### 前端主流程变化

1. 未修改备份导入主流程。
2. 未修改历史清空接口路径。
3. 仅修复历史确认文字前端校验一致性。

### 后端变化

1. 旧危险接口入口返回 410。
2. 不再执行旧导入 / 旧清空历史危险写入。

### 未处理范围

1. 未清理 `dashboard.js` 遗留旧调用痕迹。
2. 未做危险操作后端审计补强。
3. 未做 token 过期机制。
4. 未做用户禁用 / 重置密码。
5. 未做大范围权限重构。

### 下次建议

可单独做“危险操作后端审计补强”。

---

## 2026-06-18 危险操作独立安全审计最小包

### 功能名称

危险操作独立安全审计最小包。

### 本次完成内容

1. 新增独立安全审计模型 `SecurityAuditLog`。
2. 新增后端安全审计 helper `record_security_audit()`。
3. 对最高危危险操作补充成功与失败审计记录。
4. 安全审计只写入后端独立表，暂不新增前端展示页。
5. 保持旧危险接口 410 收口逻辑不变。
6. 保持动态验证码主流程和前端请求字段不变。

### 修改文件

```text
01_后端服务/models.py
01_后端服务/app.py
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 新增模型 / 表

```text
SecurityAuditLog
security_audit_logs
```

主要字段：

```text
id
event_type
result
reason
actor_user_id
actor_account
actor_nickname
actor_role
target_type
target_id
target_title
detail
request_path
is_dangerous
created_at
```

### 新增接口

无。

### 前端变化

无。本轮未修改前端页面、前端脚本、请求字段或顶部导航。

### 数据库结构变化

1. 新增安全审计表 `security_audit_logs`。
2. 不提交数据库文件。
3. 后端启动后由 SQLAlchemy `create_all` 自动创建缺失表。

### 审计覆盖

1. 注册管理员。
2. 修改用户角色。
3. 备份导入。
4. 清空全部历史。
5. 按日期清空历史。

### 成功 / 失败记录范围

1. 成功：`success`。
2. 权限不足：`permission_denied`。
3. 系统密码错误：`system_password_error`。
4. 验证码错误 / 过期：`verify_code_error`。
5. 确认文字错误：`confirm_text_error`。
6. 日期非法：`invalid_date`。
7. 备份文件格式错误：`invalid_file`。
8. 导入异常：`import_error`。
9. 其他异常：`unexpected_error`。

### 不记录内容

1. 不记录系统密码。
2. 不记录验证码。
3. 不记录 token。
4. 不记录备份文件内容。

### 清空历史边界

清空历史接口仍只删除 `UpdateHistory` / `update_histories`，不会删除 `SecurityAuditLog` / `security_audit_logs`。

### 未处理范围

1. 不新增审计查看页。
2. 不新增审计查询接口。
3. 不覆盖登录 / 退出 / 采集 / 真实库 / 地图 / 路线等操作。
4. 不做 token 过期机制。
5. 不做用户禁用 / 重置密码。
6. 不改顶部导航。

### 当前测试状态

待人工测试。

### 下次测试重点

1. 启动后端，确认 `security_audit_logs` 表自动创建。
2. 注册管理员成功 / 系统密码错误 / 验证码错误均写入安全审计。
3. 修改用户角色成功 / 权限不足 / 系统密码错误 / 验证码错误 / 确认文字错误 / 修改自己 / 降级最后管理员均写入安全审计。
4. 备份导入成功 / 权限不足 / 系统密码错误 / 验证码错误 / 文件格式错误 / 导入异常均写入安全审计。
5. 清空全部历史成功 / 失败均写入安全审计，且不会删除 `security_audit_logs`。
6. 按日期清空历史成功 / 日期非法 / 失败均写入安全审计。
7. 检查安全审计记录不包含系统密码、验证码、token 或备份文件内容。
---

## 2026-06-18 日期输入增强：支持 YYYYMMDD 手动输入

### 功能名称

日期输入增强：支持 YYYYMMDD 手动输入。

### 用户需求

1. 所有可选日期字段点击后仍能弹出浏览器原生日历。
2. 日期字段也能手动输入紧凑日期，例如 `20990102`。
3. 手动输入后自动规范化为 `YYYY-MM-DD`，例如 `2099-01-02`。

### 本次修复内容

1. 为历史危险区的开始日期、结束日期增加灵活日期解析。
2. 支持 `YYYYMMDD`、`YYYY-MM-DD`、`YYYY/M/D`、`YYYY.M.D` 输入格式。
3. 继续沿用 `2000-01-01` 到 `2099-12-31` 的日期范围限制。
4. 继续校验真实日期，非法日期不会通过前端校验。
5. 保留开始日期与结束日期联动，结束日期不能早于开始日期。
6. 点击日期输入框仍调用原生 `showPicker()`，保留日历选择能力。

### 修改文件

```text
02_前端页面/history-danger-zone.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 前端变化

1. 历史危险区按日期清空历史的开始日期、结束日期支持手动输入 `YYYYMMDD`。
2. 提交前会再次规范化日期输入，避免未失焦时直接提交导致误判。
3. 未修改后端接口字段、验证码逻辑、系统密码逻辑或安全审计逻辑。

### 后端变化

无。

### 新增接口

无。

### 数据库结构变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 开始日期输入 `20990102` 是否变为 `2099-01-02`。
2. 结束日期输入 `20990103` 是否变为 `2099-01-03`。
3. 输入 `2099-01-02` 是否仍可用。
4. 输入 `2099/1/2` 或 `2099.1.2` 是否规范化为 `2099-01-02`。
5. 输入 `20990231`、`20261301`、`abcd` 是否被阻止。
6. 点击开始日期、结束日期是否仍能选择日历。
7. 开始日期 / 结束日期联动是否仍正常。
8. 危险操作独立安全审计功能是否不受影响。

---

## 2026-06-18 日期输入增强修正：文本输入 + 日历选择

### 功能名称

日期输入增强修正：文本输入 + 日历选择。

### 问题背景

上一次日期输入增强仍保留可见 `input type="date"`，人工测试发现历史危险区按日期清空历史仍无法稳定手动输入 `20990102`。

### 本次修复内容

1. 将历史危险区开始日期、结束日期的可见输入框改为 `type="text"`。
2. 可见文本框支持手动输入 `YYYYMMDD`、`YYYY-MM-DD`、`YYYY/M/D`、`YYYY.M.D`。
3. 每个文本日期框旁保留“选择日期”按钮，通过隐藏 `input type="date"` 调用浏览器原生日历。
4. 日历选择后自动回填可见文本框。
5. 失焦、按 Enter、提交前都会规范化为 `YYYY-MM-DD`。
6. 继续校验真实日期和 `2000-2099` 年份范围。
7. 保留开始日期 / 结束日期联动。

### 修改文件

```text
02_前端页面/history-danger-zone.js
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

### 前端变化

1. 历史危险区可见日期输入框不再使用 `type="date"`。
2. 保留独立日历按钮选择日期。
3. 未修改后端接口字段、验证码逻辑、系统密码逻辑或安全审计逻辑。

### 后端变化

无。

### 新增接口

无。

### 数据库结构变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 开始日期手动输入 `20990102`。
2. 结束日期手动输入 `20990103`。
3. 点击日历按钮选择日期。
4. 非法日期阻止。
5. 开始 / 结束日期联动。
6. 安全审计不受影响。

---

## 2026-06-18 危险操作独立安全审计最小包 + 日期输入增强修正

### 功能名称

危险操作独立安全审计最小包 + 日期输入增强修正。

### 完成内容

1. 新增 `SecurityAuditLog` 模型。
2. 新增 `security_audit_logs` 独立安全审计表。
3. 新增 `record_security_audit()` helper。
4. 覆盖最高危操作：注册管理员、修改用户角色、备份导入、清空全部历史、按日期清空历史。
5. 覆盖成功和失败记录：`success`、`permission_denied`、`system_password_error`、`verify_code_error`、`confirm_text_error`、`invalid_date`、`invalid_file`、`import_error`、`unexpected_error`。
6. 清空历史只删除 `UpdateHistory`，不会删除 `SecurityAuditLog`。
7. 日期输入增强修正：历史危险区可见日期框改为 `type="text"`，支持手动输入 `YYYYMMDD`，保留隐藏 date input + “选择日期”按钮，不改变后端接口字段。
8. 不记录系统密码明文、验证码明文、token 明文或备份文件内容。

### 人工测试结果

1. `security_audit_logs` 已由后端启动后自动创建。
2. `history_clear_range` 的系统密码错误、验证码错误、未来日期成功清空 0 条均已写入安全审计。
3. `history_clear_all` 的系统密码错误已写入安全审计。
4. `user_role_change` 的系统密码错误、验证码错误已写入安全审计。
5. `backup_import` 的系统密码错误、验证码错误已写入安全审计。
6. `admin_register` 的系统密码错误已写入安全审计。
7. 历史危险区开始日期、结束日期支持手动输入 `20990102` 并规范化为 `2099-01-02`，同时保留“选择日期”日历按钮。
8. 最近安全审计记录未出现系统密码明文、验证码明文、token 明文或备份文件内容。

### 边界说明

1. 新增接口：无。
2. 新增前端页面：无。
3. 审计查询页：未做。
4. 数据库文件未提交。
5. `.env` 未提交。
6. `.venv`、`.idea`、zip、`public-nav.js` 未提交。
7. 顶部导航未修改。
8. 前端除 `history-danger-zone.js` 外未修改。
9. 未新增后端业务接口。

### 未处理范围

1. 登录 / 退出审计。
2. 采集源启停审计。
3. 采集条目忽略审计。
4. 真实库 / 地图 / 路线等普通业务操作审计。
5. token 过期机制。
6. 用户禁用 / 重置密码。
7. 安全审计查看页。

### 当前测试状态

人工测试通过。

### 下次建议

1. 可做“安全审计查看页”。
2. 可继续做 token 过期机制。
3. 可做用户禁用 / 重置密码。

---

## 后续计划清单 / 需求池（唯一维护版）

本章节为当前最新优先级与需求池。文档中更早的下一步建议、待做清单、历史记录可继续保留作为历史参考；后续实际协作、计划调整、每轮修改记录、测试状态和下次建议，以本章节和本文档末尾最新记录为准。

### 当前稳定状态

当前稳定节点：

```text
d6e95ed feat: add security audit logging
```

当前已完成：

1. 危险操作动态验证码统一增强。
2. 旧危险接口收口。
3. 危险操作独立安全审计最小包。
4. 历史危险区日期输入增强。
5. `security_audit_logs` 独立安全审计表已可写入。
6. 工作区应保持 clean，`origin/master` 已同步。

### 当前最新优先级

1. 系统美化一期
2. 合法真实探店数据采集 / 导入 MVP
3. 探店数据去重、清洗、批量转入线索库
4. 系统美化二期：内容展示、卡片化、详情页优化
5. 安全审计查看页
6. token 过期机制
7. 用户禁用 / 重置密码
8. 全项目回归清单

### 第一阶段：系统美化一期

目标：

```text
让系统从“能用”变成“看起来像正式产品”，优先提升观感、层级、提示、空状态和页面一致性。
```

建议范围：

1. 首页。
2. 外部采集页。
3. 线索审核页。
4. 线索详情页。
5. 数据管理页。
6. 历史记录页。
7. 登录 / 用户组件。
8. 空状态、加载状态、错误提示。

建议内容：

1. 统一页面标题和说明文字。
2. 统一卡片样式。
3. 统一按钮样式。
4. 统一表格 / 列表样式。
5. 统一成功 / 失败 / 警告提示。
6. 空数据时显示友好说明。
7. 加载中状态更明显。
8. 页面视觉层级更清楚。
9. 窄屏 / 移动端做基础适配。
10. 不改核心业务逻辑。

边界：

1. 不改数据库结构。
2. 不改后端接口。
3. 不改顶部导航逻辑。
4. 不新增 `public-nav.js`。
5. 不大范围重构。
6. 不影响权限、验证码、安全审计。
7. 先做统一视觉，不做复杂动画。

### 第二阶段：合法真实探店数据采集 / 导入 MVP

目标：

```text
尽快让系统具备真实探店数据入口，但必须合法合规。第一阶段优先支持用户提供的公开 URL 或用户自有表格数据，不做绕登录、绕验证码、绕反爬的真实平台爬虫。
```

路线 A：合法公开 URL 采集 MVP

用户提供一批公开 URL，系统只抓取当前 URL 的公开页面信息，解析：

1. `title`。
2. `meta description`。
3. `og:title`。
4. `og:description`。
5. JSON-LD 中的 `name` / `description` / `address` / `geo`。
6. 来源域名。
7. 原始 URL。

数据进入流程：

1. 优先写入 `collector_items`。
2. 状态为 `new`。
3. 由人工“转入线索库”。
4. 不直接进入正式库。

路线 B：用户自有表格导入 MVP

用户整理 Excel / CSV / JSON，建议字段：

```text
名称
分类
区域
地址
来源平台
来源链接
推荐理由
人均价格
标签
图片链接
经纬度
备注
```

导入后：

1. 先进入采集条目或线索库。
2. 状态为待审核。
3. 支持失败行提示。
4. 支持重复链接 / 名称地址去重。
5. 不直接进入正式库。

### 真实数据采集合规边界

1. 不登录小红书 / 美团 / 大众点评等平台。
2. 不使用 cookie。
3. 不绕验证码。
4. 不绕反爬。
5. 不模拟用户批量浏览。
6. 不抓登录后内容。
7. 不抓评论分页。
8. 不批量搬运图片。
9. 不抓付费、私密或受限内容。
10. 不写入真实账号、cookie、token、密钥。
11. 用户必须确认 URL 或数据来源可公开访问、可合法使用。
12. 采集频率必须限速。
13. 单页大小要有限制。
14. 失败不能导致整个批次崩溃。
15. 采集结果必须人工审核后再进入正式库。

### 暂缓事项

1. 暂缓安全审计查看页，除非用户重新提高优先级。
2. 暂缓 token 过期机制。
3. 暂缓用户禁用 / 重置密码。
4. 暂缓全项目大回归。
5. 暂缓任何绕登录、cookie、验证码、反爬的真实平台爬虫。
6. 暂缓大范围重构。
7. 暂缓顶部导航改造，除非用户明确要求。

### 后续协作规则

1. 本项目以后只维护一份续接文档：`项目交接与续接总文档.md`。
2. 所有计划清单、需求池、每轮修改记录、测试状态、下次建议，都统一写入这一份文档。
3. 不再要求同时更新第二份续接文档。
4. 每轮开发前，先对照“后续计划清单 / 需求池”确认优先级。
5. 用户可以随时调整优先级。
6. 调整优先级时，先更新本续接文档，再进入开发。
7. 不在用户未确认的情况下，把候选需求直接推进开发。
8. 每轮实际改代码后，必须更新本续接文档。
9. 每轮测试步骤必须明确列出每个需要填写的字段和具体填写值。
10. 严禁提交数据库、`.env`、`.venv`、`.idea`、zip、`public-nav.js`。
11. 严禁写入真实账号、cookie、token、密钥。
12. 涉及真实数据采集时，必须先写清合规边界和测试数据来源。

---

## 2026-06-22 系统美化一期 A：全局视觉基础统一

### 功能名称

系统美化一期 A：全局视觉基础统一。

### 本次完成内容

1. 统一全局视觉变量，包括页面背景、文字色、边框、圆角、阴影和主色。
2. 优化 `.container`、`.box`、`.section`、`.card` 的间距、边框、圆角和阴影。
3. 统一按钮基础样式、hover、disabled、危险按钮、筛选按钮和次级按钮视觉。
4. 统一输入框、下拉框、文本域的边框、focus 状态和 placeholder 颜色。
5. 统一 `.notice`、`.message`、`.empty`、`.warning` 等提示和空状态样式。
6. 增加通用表格样式与横向滚动容器兼容。
7. 增加基础窄屏适配，降低按钮、搜索框、卡片在移动端拥挤的问题。

### 修改文件

```text
02_前端页面/style.css
项目交接与续接总文档.md
```

### 前端变化

1. 全站基础视觉更统一，页面更接近正式产品观感。
2. 首页、采集页、审核页、详情页、数据管理页、历史页、用户管理页、备份页可共享新的基础样式。
3. 本轮没有修改 HTML 结构、页面 id、onclick、data-action 或接口字段。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### 权限 / 验证码 / 安全审计变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 首页：搜索框、分类按钮、真实库和线索库空状态是否显示正常。
2. 外部采集页：新增采集源、人工导入、采集源列表、采集条目、运行日志区块是否仍可正常展开、收起和操作。
3. 线索审核页：搜索、筛选按钮、线索列表和真实库参考是否正常。
4. 线索详情页：详情卡片、反馈表单、反馈列表空状态是否正常。
5. 数据管理页：刷新按钮、真实库数据区、线索库数据区和折叠按钮是否正常。
6. 历史记录页：历史筛选、历史列表、管理员危险区、日期输入和验证码区域是否正常。
7. 用户管理页：筛选工具条、用户表格、分页和修改角色弹窗是否正常。
8. 备份页：导出按钮、导入区、验证码区、预览区是否正常。
9. 窄屏测试：约 390px、768px、桌面宽度各检查一次。
10. 确认权限、验证码、安全审计、备份导入、历史清空逻辑不受影响。

---

## 2026-06-22 系统美化一期 B：趣味视觉强化 + clue-detail 加载体验修复

### 功能名称

系统美化一期 B：趣味视觉强化 + clue-detail 加载体验修复。

### 用户反馈

1. 系统美化一期 A 后页面确实更好看了一点，但整体变化不够明显。
2. 希望系统更像一个有趣、有城市生活感、有真实产品感的生活信息系统。
3. 希望加入更多轻量、有趣的视觉元素。
4. `clue-detail.html` 在没有 `?id=xxx` 参数时会停留在加载状态，尤其是反馈区域一直显示“正在加载反馈...”，缺少明确提示。

### 本次完成内容

1. 增强首页、外部采集页、线索详情页、历史页的页面 hero 区，加入校园生活、城市生活、探店线索、合规采集、更新轨迹等产品化提示。
2. 增加全局视觉强化样式，包括渐变 hero、生活化标签、卡片左侧色条、空状态图标、按钮 hover 层级、提示框层级。
3. 修复 `clue-detail.html` 缺少线索 ID 时的加载体验：
   - 详情区显示“缺少线索 ID，请从线索列表或审核中心进入详情页。”；
   - 提供“返回审核中心”和“返回首页”入口；
   - 反馈区不再停留在“正在加载反馈...”；
   - 缺少 ID 时反馈区显示“暂无反馈，或请先选择一条线索。”。
4. 优化反馈接口失败时的页面可见错误提示。
5. 正常携带 `?id=xxx` 的详情页仍沿用原有接口和反馈逻辑。

### 修改文件

```text
02_前端页面/style.css
02_前端页面/index.html
02_前端页面/crawler.html
02_前端页面/clue-detail.html
02_前端页面/history.html
02_前端页面/detail.js
02_前端页面/feedback.js
项目交接与续接总文档.md
```

### 前端变化

1. 新增视觉元素均为静态 HTML / CSS 或详情页空状态处理，不新增外部图片、字体、CDN 或复杂动画。
2. 未修改已有接口路径。
3. 未删除已有按钮、表单字段、危险操作字段。
4. 未修改权限、验证码、安全审计、备份导入、历史清空业务逻辑。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### 权限 / 验证码 / 安全审计变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 首页：确认 hero 区、标签、搜索框、分类按钮、真实库和线索库展示正常。
2. 外部采集页：确认 hero 区显示正常，新增采集源、人工导入、采集源列表、采集条目区域仍可展开、收起和操作。
3. 线索详情页无 ID：直接打开 `clue-detail.html`，应显示缺少线索 ID 的明确提示，反馈区不应停留在加载中。
4. 线索详情页有 ID：从审核中心或线索列表进入详情页，确认详情、反馈列表、反馈提交、审核按钮仍正常。
5. 历史页：确认 hero 区、历史筛选、历史列表、危险区日期输入和验证码区域仍正常。
6. 窄屏：约 390px、768px、桌面宽度各检查一次，确认 hero 标签、按钮、卡片没有明显溢出。
7. 确认后端接口、数据库结构、权限、验证码、安全审计、备份导入、历史清空逻辑不受影响。

---

## 2026-06-22 系统美化一期 C：产品级视觉主题 + CSS/SVG 微动效

### 功能名称

系统美化一期 C：产品级视觉主题 + CSS/SVG 微动效。

### 用户反馈

1. 一期 A/B 后页面比之前更好看，但整体仍然不够产品化。
2. 希望系统更像一个有城市生活感、校园生活感、探店系统感的真实产品。
3. 希望加入更有趣的视觉元素，但不希望简单粗暴塞 GIF 动图。
4. 本轮采用 CSS / SVG 微动效和更统一的产品化视觉主题。

### 本次完成内容

1. 在首页加入产品化地图场景：SVG 路线虚线、地图 pin 点、今日线索、待核验、生活地图概念卡。
2. 在外部采集页加入“公开 URL -> 采集条目 -> 人工转线索 -> 真实库”的工作流视觉。
3. 在外部采集页补充合规 badge，包括公开可访问、限速采集、人工审核、不绕反爬。
4. 在线索详情页加入线索生命周期视觉：发现、补充、核验、沉淀。
5. 在历史页加入系统操作时间线预览：线索更新、反馈补充、危险操作。
6. 在全局 CSS 中新增产品化视觉组件：地图路线、pin pulse、信号卡、workflow step、lifecycle step、timeline preview、status pill、skeleton shimmer、hover 上浮、`prefers-reduced-motion` 动效降级。
7. 保留上一轮 `clue-detail.html` 无 ID 时的明确提示与反馈区空状态。

### 修改文件

```text
02_前端页面/style.css
02_前端页面/index.html
02_前端页面/crawler.html
02_前端页面/clue-detail.html
02_前端页面/history.html
项目交接与续接总文档.md
```

说明：`detail.js` / `feedback.js` 保留上一轮 clue-detail 显示状态修复，本轮未继续扩大 JS 逻辑。

### 前端变化

1. 重点页面更贴近“校园生活地图 + 探店线索流 + 城市信息共建”的产品定位。
2. 使用内联 SVG、CSS 伪元素、emoji、badge、卡片和轻量动效建立统一视觉主题。
3. 未引入外部 CDN、外部图片、GIF 动图、新字体文件或动画库。
4. 未修改接口路径、表单字段、按钮事件、危险操作字段。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### 权限 / 验证码 / 安全审计变化

无。

### 是否引入外部资源

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 首页：确认地图路线、pin 点、概念卡、搜索框、分类按钮、真实库和线索库展示正常。
2. 外部采集页：确认工作流视觉、合规 badge、采集源、人工导入、采集条目、运行日志区域正常。
3. 线索详情页：确认生命周期视觉显示正常；无 ID 时仍显示明确提示；有 ID 时详情和反馈功能正常。
4. 历史页：确认时间线预览、历史筛选、历史列表、危险区日期输入和验证码区域正常。
5. 动效：确认 hover 上浮、pin pulse、路线虚线、skeleton shimmer 不影响阅读；系统开启减少动态效果时动效应降级。
6. 窄屏：约 390px、768px、桌面宽度各检查一次，确认视觉组件不溢出、不遮挡表单和按钮。
7. 确认后端接口、数据库结构、权限、验证码、安全审计、备份导入、历史清空逻辑不受影响。

---

## 2026-06-22 系统美化一期 D：采集管理页整页简洁专业化

### 功能名称

系统美化一期 D：采集管理页整页简洁专业化。

### 用户反馈

1. 顶部 hero、地图路线、标签、概念卡已有变化，但整体美化仍集中在页面上方。
2. 页面下半部分的表单、输入框、采集源列表、筛选区、统计卡片、按钮组、折叠区、区块标题和说明文字仍偏旧风格。
3. 希望整个页面都统一美化，而不是只改顶部。
4. 本轮风格要求简洁、专业、清爽、统一，不要花哨。

### 本次完成内容

1. 将 `crawler.html` 重点区域统一调整为“采集工作台”视觉风格。
2. 统一采集页大区块卡片，包括新增采集源、推荐公开源模板、人工导入、采集源列表、预览结果、采集条目、运行日志。
3. 统一区块标题、说明文字、收起按钮和折叠区视觉。
4. 统一表单区域，包括 label、input、textarea、select、URL 行、人工导入表单。
5. 统一筛选区，包括搜索框、下拉框、筛选按钮、清除按钮和筛选摘要。
6. 统一统计卡片，使采集源健康统计更接近 dashboard 风格。
7. 统一动态生成的采集源卡片、采集条目卡片、预览结果、运行日志和状态标签。
8. 统一提示、空状态、加载状态、失败状态的边框、背景和文字层级。
9. 保留上一轮 hero 和轻量视觉元素，但减少新增花哨元素，重点放在整页一致性。

### 修改文件

```text
02_前端页面/style.css
02_前端页面/crawler.html
项目交接与续接总文档.md
```

### 前端变化

1. 主要通过 `style.css` 覆盖 `collector-page` 下的现有结构和动态生成元素。
2. 未修改 `crawler-collector.js`。
3. 未修改已有 id、onclick、data-action、接口路径、表单字段。
4. 未删除任何采集源、人工导入、采集条目、运行日志功能区。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### JS 业务逻辑变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 采集页整体：确认不只是顶部 hero，页面下半部分也统一为简洁专业的工作台风格。
2. 新增采集源区：确认表单、URL 行、智能识别按钮、新增按钮、运行全部按钮、提示信息显示正常。
3. 推荐公开源模板：确认模板卡片、按钮、展开/收起正常。
4. 人工导入区：确认输入框、textarea、智能识别、保存到采集条目按钮正常。
5. 采集源列表区：确认筛选栏、健康统计卡、采集源卡片、状态标签、操作按钮、展开详情正常。
6. 预览结果区：确认空状态和预览结果展示正常。
7. 采集条目区：确认筛选栏、条目卡片、转入线索库、忽略等操作按钮正常。
8. 运行日志区：确认运行日志列表、状态标签、失败信息展示正常。
9. 窄屏：约 390px、768px、桌面宽度各检查一次，确认表单、筛选、按钮组和列表不拥挤、不溢出。
10. 确认后端接口、采集业务逻辑、权限、验证码、安全审计、数据库结构不受影响。

---

## 2026-06-22 系统美化一期 E：采集工作台主体版式重构

### 功能名称

系统美化一期 E：采集工作台主体版式重构。

### 用户反馈

1. D 轮后顶部和部分样式有所改善，但主体区域仍然太像旧表单页。
2. 新增采集源区、人工导入区仍像“大白框 + 输入框堆叠”。
3. 表单区域缺少产品化信息层级，下半部分仍不够像专业工作台。
4. 用户要求整个页面主体继续改，风格简洁专业，不花哨。

### 本次完成内容

1. 在 `crawler.html` hero 下方新增“主工作区 + 辅助区”的工作台版式：
   - 左侧主工作区承载新增采集源、人工导入、采集源列表、预览结果、采集条目、运行日志；
   - 右侧辅助区承载工作流、合规边界、今日检查和下一阶段提示。
2. 新增采集源区改成“采集源配置卡”：
   - 增加 1 填来源、2 设规则、3 保存到采集篮的步骤提示；
   - 保留名称、平台、类型、启用状态、URL、关键词、备注等原有字段；
   - 底部按钮统一为操作栏。
3. 人工导入区改成“人工录入工作台”：
   - 增加适用场景说明；
   - 保留来源平台、标题、URL、摘要、公开文本、备注、智能识别和保存按钮。
4. 采集源列表区进一步工作台化：
   - 搜索和筛选区保持独立 filter panel；
   - 健康统计卡保持 dashboard 风格；
   - 动态生成的采集源卡片、状态标签、详情区、操作按钮通过 CSS 统一。
5. 采集条目区改成“线索篮”视觉：
   - 动态条目列表统一为卡片栅格；
   - 转入线索库、忽略等操作按钮保持原逻辑。
6. 运行日志区改成“运行轨迹”视觉：
   - 日志列表通过 CSS 呈现时间线感；
   - 长错误信息继续允许换行，不撑破布局。
7. 窄屏时工作台左右分栏自动变为上下堆叠。

### 修改文件

```text
02_前端页面/crawler.html
02_前端页面/style.css
项目交接与续接总文档.md
```

### 前端变化

1. 本轮调整了 `crawler.html` 主体信息架构和布局，但只新增 wrapper、class、说明文案、辅助区。
2. 未修改已有 id、onclick、data-action、接口路径、表单字段。
3. 未删除按钮、输入框或任何功能区。
4. 未改变展开 / 收起、保存、识别、运行、筛选、转线索、忽略等业务逻辑。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### JS 业务逻辑变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 整体版式：确认 `crawler.html` 已形成主工作区 + 辅助区，主体不再只是表单堆叠。
2. 新增采集源区：确认步骤条、来源配置、URL 输入、关键词、备注、智能识别、新增采集源、运行全部启用源都正常。
3. 人工导入区：确认说明条、来源平台、标题、URL、摘要、公开文本、备注、智能识别、保存到采集条目都正常。
4. 推荐公开源模板：确认模板填充逻辑、模板按钮和收起/展开正常。
5. 采集源列表区：确认筛选、异常源、清除筛选、健康统计卡、采集源卡片、预览、运行、展开详情正常。
6. 采集条目区：确认筛选、条目卡片、转入线索库、忽略逻辑正常。
7. 运行日志区：确认运行日志像时间线展示，成功/失败信息和长错误文本不撑破页面。
8. 右侧辅助区：确认工作流、合规边界、今日检查、下一阶段提示显示正常且不影响主功能。
9. 窄屏：约 390px、768px、桌面宽度各检查一次，确认主工作区和辅助区可正常堆叠。
10. 确认后端接口、采集业务逻辑、权限、验证码、安全审计、数据库结构不受影响。

---

## 2026-06-22 系统美化一期 F：采集工作台高级质感打磨

### 功能名称

系统美化一期 F：采集工作台高级质感打磨。

### 用户反馈

1. E 轮已经形成主工作区 + 右侧辅助区，但视觉质感仍不够高级。
2. 采集源列表卡片仍偏平，像普通后台列表。
3. 右侧辅助栏、筛选区、统计卡、采集源列表之间的质感不够统一。
4. 需要继续打磨采集源列表、右侧辅助区、筛选区、统计卡和表单区。
5. 风格要求简洁专业，不花哨，不堆 emoji，不做大动画。

### 本次完成内容

1. 采集源卡片高级化：
   - 将动态生成的采集源卡片打磨为“数据源资产卡”；
   - 增加克制的数据源标识；
   - 强化标题、平台、URL、状态标签和操作按钮的层级；
   - URL 使用次级弱化样式并防止撑破布局；
   - 操作按钮组更规整，“运行单个源”改为更协调的青绿色系。
2. 右侧辅助区高级化：
   - 工作流、合规边界、今日检查、下一阶段提示卡片更紧凑；
   - 步骤数字呈现轻量 timeline；
   - checklist 视觉更像产品组件。
3. 筛选区高级化：
   - 搜索框、下拉框、按钮继续统一为轻量 filter panel；
   - 按钮配色更克制，异常筛选保留警示但不刺眼。
4. 统计卡片高级化：
   - 统计卡增加轻微渐变、柔和阴影和右上角弱装饰；
   - 数字更突出，label 更清晰。
5. 表单区高级化：
   - 输入框、textarea、URL 输入条进一步统一；
   - 大块 textarea 高度更合理；
   - URL 输入采用等宽字体，突出其作为数据源地址的属性。
6. 采集条目和运行日志继续打磨：
   - 条目卡片、摘要块、运行日志表格边界更清楚；
   - 长错误信息允许换行，不撑破页面。

### 修改文件

```text
02_前端页面/style.css
项目交接与续接总文档.md
```

### 前端变化

1. 本轮主要通过 CSS 覆盖现有 `crawler.html` 结构和动态生成元素。
2. 未修改 `crawler.html` 的 id、onclick、data-action、接口路径、表单字段。
3. 未修改 `crawler-collector.js`。
4. 未新增外部资源、GIF、图片、CDN 或动画库。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### JS 业务逻辑变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 采集源列表：确认卡片更像数据源资产卡，标题、平台、URL、状态标签、操作按钮层级清楚。
2. 操作按钮：确认展开、预览、运行单个源、启用/停用仍可正常点击，按钮配色不突兀。
3. 右侧辅助栏：确认工作流 timeline、合规 checklist、今日检查和下一阶段提示更精致且不遮挡主内容。
4. 筛选区：确认搜索、启用状态、运行状态、类型、异常源、清除筛选均正常。
5. 统计卡：确认数字、说明、卡片间距和窄屏换行正常。
6. 表单区：确认新增采集源和人工导入的输入框、textarea、URL 输入条、按钮组仍正常。
7. 采集条目和运行日志：确认条目卡片、转线索/忽略、日志表格、长错误信息显示正常。
8. 窄屏：约 390px、768px、桌面宽度各检查一次，确认主工作区和右侧辅助区不拥挤、不溢出。
9. 确认后端接口、采集业务逻辑、权限、验证码、安全审计、数据库结构不受影响。

---

## 2026-06-22 系统美化一期 F 小修：修复 crawler 统计卡片被右侧辅助栏遮挡

### 功能名称

系统美化一期 F 小修：修复 crawler 统计卡片被右侧辅助栏遮挡。

### 用户反馈

采集源列表区域的统计卡片横向超出左侧主工作区，视觉上被右侧辅助栏遮挡；其他区域目前可以接受。

### 修复内容

1. 为 `collector-workbench`、左侧主区、右侧辅助区、采集页卡片、统计网格、筛选栏、采集源卡片补充 `min-width: 0`、`max-width: 100%` 和 `box-sizing: border-box`。
2. 调整工作台左右分栏为 `minmax(0, 1fr) + minmax(280px, 320px)`，避免左侧主区被内容最小宽度撑破。
3. 统计卡片网格改为更小的自适应最小宽度，确保自动换行。
4. 采集源筛选栏和采集条目筛选栏增加可收缩列宽，并在较窄桌面宽度下自动换行。
5. 长 URL、长文字、错误信息增加 `overflow-wrap: anywhere` 和 `word-break: break-word`，避免撑破布局。
6. 1100px 以下继续让右侧辅助栏下移，避免遮挡左侧主工作区。

### 修改文件

```text
02_前端页面/style.css
项目交接与续接总文档.md
```

### 后端变化

无。

### JS 业务逻辑变化

无。

### 数据库结构变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 桌面宽度：采集源列表统计卡片不应再被右侧辅助栏遮挡。
2. 768px：右侧辅助栏应下移，统计卡片和筛选栏应自动换行。
3. 390px：统计卡片、筛选栏、长 URL、采集源卡片不应横向溢出。
4. 确认采集源筛选、异常源筛选、清除筛选、采集源展开、预览、运行单个源功能不受影响。

---

## 2026-06-22 系统美化一期 F 二次小修：crawler 左右分栏改为安全不遮挡布局

### 功能名称

系统美化一期 F 二次小修：crawler 左右分栏改为安全不遮挡布局。

### 用户反馈

采集源列表统计卡片仍被右侧辅助栏遮挡；上一轮补充内部 `min-width: 0` 和统计卡片换行后仍未彻底解决。

### 原因判断

问题不只是统计卡片内部宽度，而是左右分栏断点过晚。中等桌面宽度下，左侧主工作区和右侧辅助栏仍并排硬挤，复杂筛选栏、统计卡片和采集源卡片会让主区视觉上伸到右侧栏下面。

### 修复内容

1. 将 `collector-workbench` 的单栏断点提前到 `1500px`。
2. 在 `1500px` 以下让右侧辅助栏提前下移，主工作区完整占据上方宽度。
3. 保留主工作区、统计卡片、筛选栏和列表区域的 `max-width: 100%` 与防横向溢出规则。
4. `760px` 以下右侧辅助栏继续变为单列。
5. 不再继续改视觉风格，只修遮挡 / 溢出问题。

### 修改文件

```text
02_前端页面/style.css
项目交接与续接总文档.md
```

### 后端变化

无。

### JS 业务逻辑变化

无。

### 数据库结构变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 普通桌面宽度：采集源列表统计卡片不应再被右侧辅助栏遮挡。
2. 768px：右侧辅助栏应下移，统计卡片、筛选栏、采集源卡片完整显示。
3. 390px：页面不应出现横向滚动，长 URL / 长文本不应撑破布局。
4. 确认采集源筛选、异常源筛选、展开、预览、运行单个源功能不受影响。

---

## 2026-06-22 系统美化一期 F 三次小修：修复 crawler 采集源卡片内部布局

### 功能名称

系统美化一期 F 三次小修：修复 crawler 采集源卡片内部布局。

### 用户反馈

采集源卡片不重叠后，内部布局变得过空，`启用中`、`启用但无条目` 等标签被挤成竖排，平台、状态、标题、URL、运行状态之间距离太散，视觉不像紧凑的数据源资产卡。

### 原因判断

采集源卡片内部使用 grid 分列后，在可用宽度变化时状态标签所在列被压窄；标签没有强制保持横排，中文短标签会被拆成竖排。左侧信息区的标题、标签、平台和 URL 层级也过于松散。

### 修复内容

1. 将采集源卡片左侧信息区改为紧凑 flex wrap 信息流。
2. 标题独占一行，标签、运行状态和诊断标签横向排列并自动换行。
3. 标签增加 `white-space: nowrap`、`word-break: keep-all`、`writing-mode: horizontal-tb`，避免竖排。
4. URL 改为单行省略显示，避免撑破布局。
5. 右侧按钮组保持横向整齐，中小宽度下自动换行。
6. 压缩卡片 padding、图标尺寸和详情区缩进，减少大面积空白。

### 修改文件

```text
02_前端页面/style.css
项目交接与续接总文档.md
```

### 后端变化

无。

### JS 业务逻辑变化

无。

### 数据库结构变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 采集源卡片高度应更紧凑，内部不应有大面积空白。
2. `启用中`、`启用但无条目`、`成功 / 新增 0` 等标签必须横排显示。
3. 标题、平台、URL、状态标签层级应清楚。
4. URL 不应撑破布局。
5. 展开、预览、运行单个源、启用 / 停用按钮功能不受影响。

---

## 2026-06-22 系统美化一期 A-F：产品化视觉升级与 crawler 工作台化

### 功能名称

系统美化一期 A-F：产品化视觉升级与 crawler 工作台化。

### 本轮包含内容

1. 全局视觉基础统一。
2. 产品级视觉主题。
3. 首页 hero / 标签 / 生活信息概念卡。
4. `crawler.html` 采集页工作台化。
5. `crawler.html` 主工作区 + 辅助区。
6. `crawler.html` 表单区、筛选区、统计区、采集源列表、采集条目、运行日志视觉统一。
7. `crawler.html` 采集源卡片内部布局修复。
8. `crawler.html` 统计卡片遮挡修复。
9. `clue-detail.html` 无 id 加载体验修复。
10. `history.html` 页面视觉增强。

### 修改文件

```text
02_前端页面/style.css
02_前端页面/index.html
02_前端页面/crawler.html
02_前端页面/clue-detail.html
02_前端页面/detail.js
02_前端页面/feedback.js
02_前端页面/history.html
项目交接与续接总文档.md
```

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### 权限 / 验证码 / 安全审计变化

无。

### 外部资源

未引入 CDN、外部图片、GIF、字体文件或动画库。

### 当前测试状态

人工测试阶段性通过，已准备提交；提交后下一阶段继续按页面逐步优化。

### 下次建议

1. 可以继续做 `index/admin/history/detail` 其它页面整页统一。
2. 或开始合法真实探店数据采集 / 导入 MVP。

---

## 2026-06-22 系统美化一期 G：审核中心整页工作台化

### 功能名称

系统美化一期 G：审核中心整页工作台化。

### 本轮目标

将 `admin.html` 从普通后台列表页升级为线索审核工作台，为后续合法真实探店数据进入线索库后的人工审核流程做准备。

### 修改文件

```text
02_前端页面/admin.html
02_前端页面/style.css
项目交接与续接总文档.md
```

### 前端变化

1. 审核中心新增产品化 `page-hero`，明确“线索审核、人工确认、真实库沉淀”的页面定位。
2. 审核说明、用户管理、搜索区、真实库参考区、线索审核列表统一为工作台视觉。
3. 搜索和筛选区升级为清爽筛选面板，状态 / 分类 / 来源 / 外部线索等筛选控件保持原逻辑。
4. 动态线索卡片通过 CSS 工作台化：
   - 标题更突出；
   - 分类、状态、来源平台、外部线索标签更清晰；
   - 来源链接和摘要层级更明确；
   - 用户核验、管理员审核、删除等操作按钮区域更规整。
5. 空状态、加载状态和错误提示继续沿用统一视觉。
6. 窄屏下筛选区和线索卡片自动堆叠，避免横向溢出。

### 后端变化

无。

### 数据库结构变化

无。

### 顶部导航变化

无。

### public-nav.js

未新增。

### 权限 / 验证码 / 安全审计变化

无。

### 当前测试状态

待人工测试。

### 人工测试重点

1. 审核中心顶部 hero、badge 和说明文案显示是否正常。
2. 搜索框、待核验 / 已转入真实库 / 全部线索快捷筛选是否正常。
3. 状态、分类、来源、外部线索、有来源链接、清除筛选是否正常。
4. 线索卡片标题、标签、来源平台、来源链接、摘要、审核提示是否清晰。
5. 用户核验按钮、审核通过按钮、删除线索按钮功能是否不受影响。
6. 真实库参考区域是否正常加载。
7. 无数据、加载失败、窄屏布局是否正常。
