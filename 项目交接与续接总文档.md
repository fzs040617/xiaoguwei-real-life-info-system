# 项目交接与续接总文档

> 适用对象：ChatGPT、Codex、其他后续接手开发者。  
> 项目：小谷围岛广州大学城真实生活信息共建系统。  
> 规则：任何新对话、新账号、新 Codex 任务开始前，都必须先读本文件，再读当前项目代码。不要依赖旧聊天记忆，以本文件和当前代码为准继续开发。

---

## 一、给 ChatGPT / Codex 的开场说明

这是“小谷围岛广州大学城真实生活信息共建系统”的续接文档，也是后续开发的唯一交接依据。

无论是切换 ChatGPT 账号、开启新对话、启动新的 Codex 任务，还是交给其他开发者继续，都必须先阅读本文件。不要依赖旧聊天记录、旧账号记忆或口头印象。实际开发时，以本文件、原续接文档和当前项目代码共同为准；如果文档和代码不一致，先读代码确认现状，再把确认结果补回文档。

后续每次完成修改，都要在本文档末尾追加“本次修改记录”，写清楚修改文件、修改内容、接口变化、测试步骤和当前测试状态。

---

## 二、项目基本信息

1. 项目名称：小谷围岛广州大学城真实生活信息共建系统。
2. 项目根目录：`C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统`
3. 后端目录：`C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务`
4. 前端目录：`C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\02_前端页面`
5. 后端启动命令：

```bat
cd /d "C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\01_后端服务"
py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
```

也可以在项目根目录双击或运行：

```bat
打开后端.bat
```

6. 前端首页路径：

```text
C:\Users\86138\Desktop\小谷围岛广州大学城真实生活信息共建系统\02_前端页面\index.html
```

7. 接口文档地址：

```text
http://127.0.0.1:8000/docs
```

8. 当前 Python 依赖情况：
   - 当前没有在项目中扫描到 `requirements.txt`、`pyproject.toml`、`Pipfile` 或 `poetry.lock`。
   - 从代码导入可以看到至少使用：`fastapi`、`uvicorn`、`pydantic`、`sqlalchemy`、`requests`、`beautifulsoup4`。
   - 后端本地数据库为 SQLite，文件：`01_后端服务\xiaoguwei.db`。

---

## 三、开发原则

1. 不要重构整个项目。
2. 不要删除已有功能。
3. 每次只做一个明确功能包。
4. 修改前必须先阅读本续接文档、原续接文档和现有代码。
5. 优先做最小范围修改，避免无关改动。
6. 优先复用已有脚本、已有接口、已有样式和已有权限逻辑。
7. 如果需要新增全局 JS，必须加入 `02_前端页面\global-loader.js`，并更新 `GLOBAL_SCRIPT_VERSION`。
8. 如果统一修改所有 HTML 的 `global-loader.js?v=xx`，优先使用已有脚本 `02_前端页面\tools\cleanup-global-scripts.py`。
9. 修改完成后必须写清楚修改文件、修改内容和测试步骤。
10. 修改完成后必须更新本交接文档。
11. 管理类、危险类操作不能只靠前端隐藏，后端也必须校验权限。
12. 如果发现文档和代码不一致，不要直接猜；先检查代码，确认后更新文档说明。

---

## 四、当前系统结构

### 4.1 后端主要文件

后端目录：`01_后端服务`

```text
app.py              FastAPI 主程序，包含大部分接口、权限校验、备份导入导出、更新历史、用户系统等。
models.py           SQLAlchemy 数据模型。
database.py         SQLite 数据库连接配置，当前使用 sqlite:///./xiaoguwei.db。
crawler_service.py  公开网页采集相关服务。
scheduler.py        定时任务启动和调度。
xiaoguwei.db        本地 SQLite 数据库文件。
.venv               本地 Python 虚拟环境目录。
```

### 4.2 前端主要文件

前端目录：`02_前端页面`

```text
index.html                 首页。
submit.html                提交线索。
admin.html                 审核中心。
manage.html                数据管理。
dashboard.html             数据看板。
crawler.html               采集管理。
feedback-admin.html        反馈中心。
map.html                   地图中心。
route.html                 路线中心。
history.html               更新历史中心。
backup.html                备份导出/导入。
clue-detail.html           线索详情页。
item-detail.html           真实库详情页。
map-detail.html            地图点详情页。
route-detail.html          路线详情页。
style.css                  全站主要样式。
global-loader.js           全局脚本加载器。
admin-permission-guard.js  管理员权限守卫。
user-widget.js             用户入口旧版脚本。
user-widget-v2.js          用户入口新版脚本。
history-danger-zone.js     更新历史危险操作区。
history.js                 更新历史列表基础逻辑。
history-filter-v2.js       更新历史筛选逻辑。
history-auto.js            自动记录更新历史。
history-badge.js           卡片/列表历史标记。
history-inline.js          详情页内嵌历史。
backup.js                  备份导出与预览。
backup-import.js           备份导入逻辑。
backup-import-layout.js    备份导入布局增强。
dashboard-v2.js            综合数据看板 V2。
feedback-history-link.js   反馈提交后联动更新历史。
generic-smart-fill.js      通用智能识别填表。
route-smart-fill.js        路线智能识别填表。
map-smart-fill.js          地图点智能识别填表。
```

### 4.3 当前数据库模型

当前 `models.py` 中主要模型：

```text
Clue           线索表，对应 clues。
VerifiedItem   真实库表，对应 verified_items。
CrawlTarget    采集目标表，对应 crawl_targets。
Feedback       反馈表，对应 feedbacks。
MapPoint       地图点表，对应 map_points。
RoutePlan      路线表，对应 route_plans。
UpdateHistory  更新历史表，对应 update_histories。
AuthUser       用户表，对应 auth_users。
```

重要字段概览：

```text
UpdateHistory:
id, target_type, target_id, action, title, detail, operator, created_at

AuthUser:
id, account, nickname, password_salt, password_hash, role, avatar_base64, session_token, created_at, last_login_at
```

注意：续接旧文档中有时写“routes”，但当前模型表名是 `route_plans`。备份导出字段里使用 `routes` 作为 JSON 键名，对应数据库模型 `RoutePlan` / 表 `route_plans`。

### 4.4 当前主要接口

基础与状态：

```text
GET  /
GET  /health
GET  /db/status
GET  /scheduler/status
POST /scheduler/test-run
```

线索与真实库：

```text
POST   /clues
GET    /clues
GET    /clues/{clue_id}
PATCH  /clues/{clue_id}
PATCH  /clues/{clue_id}/status
POST   /clues/{clue_id}/archive
POST   /clues/{clue_id}/restore
DELETE /clues/{clue_id}

POST   /verified-items
GET    /verified-items
GET    /verified-items/{item_id}
PATCH  /verified-items/{item_id}
POST   /verified-items/{item_id}/archive
POST   /verified-items/{item_id}/restore
DELETE /verified-items/{item_id}

POST   /admin/clues/{clue_id}/approve
GET    /search
```

采集：

```text
POST   /crawler/targets
GET    /crawler/targets
PATCH  /crawler/targets/{target_id}
POST   /crawler/targets/{target_id}/toggle
DELETE /crawler/targets/{target_id}
POST   /crawler/public-url
```

反馈：

```text
POST   /feedbacks
POST   /feedbacks-v2
GET    /feedbacks
GET    /feedbacks/stats
DELETE /feedbacks/{feedback_id}
```

地图点：

```text
POST   /map-points
GET    /map-points
GET    /map-points/{point_id}
PATCH  /map-points/{point_id}
POST   /map-points/{point_id}/archive
POST   /map-points/{point_id}/restore
DELETE /map-points/{point_id}
```

路线：

```text
POST   /routes
GET    /routes
GET    /routes/{route_id}
PATCH  /routes/{route_id}
POST   /routes/{route_id}/archive
POST   /routes/{route_id}/restore
DELETE /routes/{route_id}
```

更新历史：

```text
POST   /update-history
GET    /update-history
DELETE /update-history/clear-all
DELETE /update-history/clear-all-admin
DELETE /update-history/clear-all-admin-v2
DELETE /update-history/clear-range-admin
```

备份：

```text
GET  /backup/export
POST /backup/import
GET  /backup/import-code
POST /backup/import-v2
POST /backup/import-v3
```

用户：

```text
POST  /auth/register
POST  /auth/login
POST  /auth/me
PATCH /auth/profile
POST  /auth/logout
POST  /auth/register-v2
PATCH /auth/profile-v2
```

### 4.5 全局加载器机制

当前所有主要 HTML 页面末尾统一引用：

```html
<script src="global-loader.js?v=52"></script>
```

`global-loader.js` 当前版本：

```javascript
const GLOBAL_SCRIPT_VERSION = "52";
```

当前全局加载脚本：

```text
page-scope-cleanup.js
user-widget.js
user-widget-v2.js
modal-shortcuts.js
password-eye.js
user-message-clear.js
user-avatar-click-upload.js
danger-message-clear.js
admin-permission-guard.js
```

以后新增全局功能时：

1. 新建独立 JS 文件。
2. 加入 `global-loader.js` 的 `globalScripts`。
3. 更新 `GLOBAL_SCRIPT_VERSION`。
4. 如需统一 HTML 引用版本，在 `02_前端页面` 目录运行 `py tools\cleanup-global-scripts.py`。

### 4.6 用户系统和管理员权限机制

用户表：`auth_users`。

角色：

```text
admin  管理员
user   普通用户
```

用户密码不是明文保存，而是 `password_salt` + `password_hash`。当前 hash 逻辑在 `app.py` 中。

登录后前端把 token 保存在本地，常见键名：

```text
xgw_user_token
```

管理员权限：

1. 前端通过 `admin-permission-guard.js` 隐藏或拦截管理入口。
2. 危险接口应通过后端 `require_admin_user(db, token)` 校验 token 对应用户是否为管理员。
3. 危险操作还要校验系统密码 `xgw2026`。

### 4.7 当前危险操作规则

危险操作至少包括：

```text
清空更新历史
按时间区间清空更新历史
备份导入
未来所有批量删除、批量覆盖、权限修改、用户禁用等管理操作
```

危险操作必须满足：

1. 已登录管理员账号。
2. 输入系统密码：`xgw2026`。
3. 输入确认文字：`清空历史` 或对应功能规定的确认文字。
4. 后端必须校验管理员身份，不能只靠前端隐藏按钮。
5. 前端应明确提示“只删除哪些数据、不删除哪些正式数据”。

---

## 五、已完成功能

当前系统已完成或基本完成以下功能：

1. 首页：入口导航、模块跳转、用户入口。
2. 提交线索：用户提交线索，支持智能识别填表。
3. 审核中心：管理员审核线索、归入真实库、归档/恢复等。
4. 数据管理：线索和真实库信息管理。
5. 数据看板：综合统计线索、真实库、地图点、路线、反馈、历史、采集目标等。
6. 采集管理：维护采集目标，支持公开网页采集相关功能。
7. 反馈中心：管理线索、真实库、地图点、路线的反馈。
8. 地图中心：新增、搜索、管理地图点，支持跳转高德/百度地图。
9. 路线中心：新增、搜索、管理路线，可选地图点加入路线草稿。
10. 更新历史中心：记录新增、编辑、删除、归档、恢复、审核、反馈等操作。
11. 备份导出/导入：支持导出 JSON，导入时要求管理员、系统密码和验证码。
12. 注册登录系统：支持注册、登录、退出、编辑资料、头像上传、角色显示。
13. 管理员权限守卫：普通用户不能进入或使用管理功能，管理员可正常操作。
14. 全局脚本加载器：统一加载用户组件、权限守卫、快捷键、密码眼睛等全局脚本。
15. 智能识别填表：规则版智能识别，把用户输入填入原表单，由用户再校正提交。
16. 反馈联动更新历史：反馈提交成功后写入“收到反馈”历史并刷新详情页。
17. 详情页编辑：线索、真实库、地图点、路线详情页支持编辑并记录历史。
18. 错误提示自动清除：登录、修改密码、危险操作等错误提示会在重新输入后清除。
19. 弹窗快捷键：支持 Enter、Y、N 等快捷操作，并避免输入框里误触发。
20. 密码眼睛：密码输入框支持显示/隐藏密码。

---

## 六、重要规则和密码

1. 系统密码：`xgw2026`。
2. 普通用户可以提交、查看、反馈、参与共建。
3. 管理员可以审核、采集、管理数据、导入备份、执行危险操作。
4. 危险操作必须满足：管理员登录 + 系统密码 + 确认文字。
5. 后端权限校验不能只靠前端隐藏，必须在接口里验证 token 和角色。
6. 账号和密码规则：只允许英文字母、数字和常用英文标点；不允许中文、空格、特殊不可见字符。
7. 注册管理员账号时，需要选择管理员角色并输入系统密码 `xgw2026`。
8. 修改密码必须输入原正确密码，新密码通常需要输入两遍。
9. 以后新增管理类接口时，后端也要校验管理员身份。
10. 系统密码不是用户账号密码，不应用用户密码替代系统密码。

---

## 七、当前待做清单

保留并整理之前的待做事项：

1. 按时间区间选择性清空历史。
2. 后端接口真正权限校验。
3. 可选绑定邮箱。
4. 管理员用户管理页。
5. 正式数据库迁移。
6. AI 智能识别升级。
7. 系统设置页。

补充说明：

当前代码中已经能看到 `/update-history/clear-range-admin` 接口和 `history-danger-zone.js` 中的区间清空前端区域，但本次没有做功能开发，也没有完整测试。下一次如果正式开发“按时间区间选择性清空历史”，应先核对这部分现有代码是否能正常运行、中文提示是否有编码问题、前后端请求体是否一致、管理员权限是否确实生效。

---

## 八、本次接下来要做的功能

当前优先继续做：

```text
按时间区间选择性清空更新历史
```

功能要求：

1. 只删除 `update_histories` 表中的记录。
2. 不删除 `clues`、`verified_items`、`map_points`、`route_plans`、`feedbacks`、`crawl_targets`、`auth_users` 等正式数据。
3. 管理员可以选择开始日期和结束日期。
4. 必须输入系统密码 `xgw2026`。
5. 必须输入确认文字：`清空历史`。
6. 普通用户不应该看到或不能使用这个危险操作。
7. 后端接口也必须校验管理员身份。
8. 删除完成后返回删除数量。
9. 删除完成后前端刷新历史列表或刷新页面。
10. 日期格式建议使用 `YYYY-MM-DD`，按 `created_at` 字段筛选。

现有代码线索：

```text
后端已有疑似接口：DELETE /update-history/clear-range-admin
前端已有疑似脚本：02_前端页面\history-danger-zone.js
页面引用位置：02_前端页面\history.html
```

下一次正式开发时不要从零重写，先检查这些现有实现，再按最小范围修复、完善和测试。

---

## 九、每次修改完成后的记录模板

以后每次完成任务，都按下面模板追加到本文档末尾。

```markdown
## YYYY-MM-DD 本次修改记录

### 本次完成

写清楚完成了什么功能。

### 修改文件

逐个列出文件路径和修改内容。

### 新增接口

列出接口路径、请求方式、请求体、返回值、权限要求。

### 前端变化

说明新增了哪些按钮、表单、交互、提示。

### 后端变化

说明新增了哪些模型、接口、校验、数据库操作。

### 测试步骤

1. 如何启动后端。
2. 打开哪个页面。
3. 登录什么角色。
4. 输入什么测试数据。
5. 点击什么按钮。
6. 预期结果是什么。
7. 失败时应该把什么报错发回来。

### 当前测试状态

已测试通过 / 未测试 / 部分通过 / 有遗留问题。

### 下次继续建议

写清楚下一次应该从哪里继续。
```

---

## 十、给未来新 ChatGPT / Codex 的第一句话

可以复制下面这段话给新对话、新账号或新的 Codex 任务：

```text
请先阅读项目根目录里的《项目交接与续接总文档.md》或《小谷围岛广州大学城真实生活信息共建系统_续接文档.md》。这是项目的唯一交接依据。请不要依赖旧聊天记忆。阅读后先总结当前系统状态，再根据我提出的新需求做最小范围修改。
```

---

## 2026-06-06 本次修改记录

### 本次完成

整理并新建 ChatGPT 与 Codex 通用的唯一交接文档，保留原续接文档不覆盖，并在原续接文档顶部追加优先阅读提示。

### 修改文件

```text
项目交接与续接总文档.md
新增总交接文档，整理项目基本信息、结构、接口、权限规则、已完成功能、待做清单、下一步功能要求和后续记录模板。

小谷围岛广州大学城真实生活信息共建系统_续接文档.md
仅在文档开头追加提示：以后优先阅读《项目交接与续接总文档.md》。
```

### 新增接口

无。本次只整理文档，没有新增或修改接口。

### 前端变化

无。本次没有修改前端功能代码。

### 后端变化

无。本次没有修改后端功能代码。

### 测试步骤

1. 打开项目根目录。
2. 确认存在 `项目交接与续接总文档.md`。
3. 打开原续接文档 `小谷围岛广州大学城真实生活信息共建系统_续接文档.md`。
4. 确认原文档开头有“以后优先阅读《项目交接与续接总文档.md》”提示。
5. 确认没有启动后端、没有修改功能代码。

### 当前测试状态

未测试功能；已完成文档整理。

### 下次继续建议

下一次正式开发“按时间区间选择性清空更新历史”时，先核对现有 `/update-history/clear-range-admin` 和 `history-danger-zone.js`，再做最小范围修复和测试。

## 2026-06-06 本次修改记录

### 本次完成

修复项目根目录已有的 `打开后端.bat`。该脚本用于 Windows 双击启动：先打开前端首页 `02_前端页面\index.html`，再进入后端目录启动 FastAPI 服务。

### 修改文件

```text
打开后端.bat
修复为纯英文 echo、相对路径定位、无完整中文绝对路径的启动脚本；使用 %~dp0 作为项目根目录，自动查找 01_* 下的 app.py 和 02_* 下的 index.html。

项目交接与续接总文档.md
追加本次启动 bat 修复记录、启动步骤、失败排查信息和下次继续建议。
```

### 新增接口

无。本次只修复 Windows 启动脚本，没有修改后端接口。

### 前端变化

无。本次没有修改前端业务文件。bat 只会在启动时打开前端首页 `index.html`。

### 后端变化

无。本次没有修改 `app.py`、`models.py`、`database.py` 或其他后端业务代码。bat 启动命令为：

```bat
py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
```

如果 `py` 命令失败，会继续尝试：

```bat
python -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
```

### 启动步骤

1. 在项目根目录双击 `打开后端.bat`。
2. 脚本先以自身所在目录 `%~dp0` 作为项目根目录。
3. 脚本查找 `01_*` 目录下的 `app.py`，作为后端目录。
4. 脚本查找 `02_*` 目录下的 `index.html`，找到则自动打开前端首页。
5. 脚本进入后端目录，优先用 `py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000` 启动。
6. 如果 `py` 启动失败，脚本自动尝试 `python -m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
7. 后端启动后可打开 `http://127.0.0.1:8000/docs` 检查接口文档。

### 测试步骤

1. 确认 `打开后端.bat` 位于项目根目录。
2. 双击 `打开后端.bat`。
3. 观察是否自动打开 `02_前端页面\index.html`。
4. 观察命令行窗口是否显示 `Uvicorn running on http://127.0.0.1:8000` 或类似启动成功信息。
5. 浏览器打开 `http://127.0.0.1:8000/docs`，确认能看到 FastAPI 接口文档。

### 启动失败时应查看的报错

1. 如果提示 `Cannot find backend app.py`，检查 bat 是否在项目根目录，以及 `01_后端服务\app.py` 是否存在。
2. 如果提示 `Cannot find frontend index.html`，检查 `02_前端页面\index.html` 是否存在；该 warning 不会中断后端启动。
3. 如果 `py` 和 `python` 都失败，查看是否安装 Python、是否安装 `uvicorn` 和 `fastapi`。
4. 如果出现 `ModuleNotFoundError`，把缺少的模块名发回来。
5. 如果出现 `Address already in use` 或端口占用，说明 `127.0.0.1:8000` 已被其他进程占用。
6. 如果出现中文乱码或类似 `'锘緻echo' 不是内部或外部命令`，说明 bat 编码或文件开头仍有异常，需要把命令行完整报错发回来。

### 当前测试状态

已完成文件检查和脚本语法检查；未在本次任务中长期启动后端服务，避免留下运行中的服务进程。

### 下次继续建议

下次继续功能开发前，先双击 `打开后端.bat` 验证后端可启动，再打开 `http://127.0.0.1:8000/docs` 和前端首页确认基础运行状态。后续功能仍建议按总文档待办，从“按时间区间选择性清空更新历史”或“后端接口真正权限校验”中选择一个做最小范围开发。

## 2026-06-06 本次修改记录

### 本次完成

完成“按时间区间选择性清空更新历史”功能包的核对和修复。复用现有 `/update-history/clear-range-admin` 接口和 `history-danger-zone.js` 危险操作区，没有重复新增一套功能。

该功能允许管理员在更新历史中心选择开始日期和结束日期，只删除 `update_histories` 表中 `created_at` 位于该日期范围内的记录，不删除 `clues`、`verified_items`、`map_points`、`route_plans`、`feedbacks`、`crawl_targets`、`auth_users` 等正式数据。

### 修改文件

```text
01_后端服务/app.py
修复 DELETE /update-history/clear-range-admin：请求字段提供默认值以便返回更明确错误；强制 start_date/end_date 使用 YYYY-MM-DD；将开始日期转换为当天 00:00:00，将结束日期转换为当天 23:59:59.999999；继续复用 require_admin_user 和 verify_system_password。

02_前端页面/history-danger-zone.js
完善“按时间区间清空更新历史”交互：日期、系统密码、确认文字输入变化时清除提示；前端空日期、日期倒置、空密码、确认文字错误给出明确提示；后端错误优先显示 detail；成功后提示“已清空 X 条更新历史”，清空系统密码和确认文字，并刷新历史列表。

02_前端页面/history.html
将 history-danger-zone.js 引用版本从 v=47 更新为 v=48，避免浏览器继续使用旧缓存。
```

### 新增/修复接口

修复现有接口，没有新增新路径：

```text
DELETE /update-history/clear-range-admin
```

请求体：

```json
{
  "token": "登录后的用户 token",
  "system_password": "xgw2026",
  "confirm_text": "清空历史",
  "start_date": "YYYY-MM-DD",
  "end_date": "YYYY-MM-DD"
}
```

权限和校验：

```text
必须有有效 token。
token 对应用户必须存在且 role 为 admin。
system_password 必须等于 xgw2026。
confirm_text 必须等于 清空历史。
start_date/end_date 必须非空且格式为 YYYY-MM-DD。
开始日期不能晚于结束日期。
```

返回：

```json
{
  "message": "指定时间区间内的更新历史已清空",
  "deleted_count": 0,
  "operator": "管理员账号",
  "start_date": "YYYY-MM-DD",
  "end_date": "YYYY-MM-DD"
}
```

### 前端变化

更新历史中心的管理员危险操作区已有“按时间区间清空更新历史”区域，本次补齐交互：

1. 开始日期 `input type="date"`。
2. 结束日期 `input type="date"`。
3. 系统密码 `input type="password"`。
4. 确认文字输入框，提示必须输入：`清空历史`。
5. 按钮：`清空所选区间历史`。
6. 结果提示区域显示明确错误或成功信息。
7. 提交前进行两次 `confirm`。
8. 成功后清空系统密码和确认文字，并刷新历史列表。

普通用户不应看到该区域；若普通用户绕过前端直接请求接口，后端也会拒绝。

### 后端变化

没有新增数据库表、没有修改模型、没有修改正式数据表。后端只修复区间清空接口：

1. 使用 `require_admin_user(db, token)` 校验管理员。
2. 使用 `verify_system_password(system_password)` 校验系统密码。
3. 校验确认文字必须为 `清空历史`。
4. 严格校验日期格式为 `YYYY-MM-DD`。
5. `start_date` 按当天 `00:00:00` 处理。
6. `end_date` 按当天 `23:59:59.999999` 处理。
7. 只对 `UpdateHistory` / `update_histories` 执行删除。
8. 返回 `deleted_count`。

### 测试步骤

1. 启动后端：在项目根目录双击 `打开后端.bat`，或进入 `01_后端服务` 后运行 `py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
2. 打开前端页面：`02_前端页面/history.html`。
3. 管理员测试：登录管理员账号，进入更新历史中心，确认能看到“管理员危险操作”和“按时间区间清空更新历史”。
4. 正常区间测试：选择有历史记录的开始日期和结束日期，输入系统密码 `xgw2026`，确认文字输入 `清空历史`，点击“清空所选区间历史”，两次确认后应提示“已清空 X 条更新历史”，历史列表刷新。
5. 普通用户测试：退出管理员，登录普通用户，打开 `history.html`，不应看到危险操作区；若用接口工具直接请求 `/update-history/clear-range-admin`，应返回非管理员拒绝。
6. 输错密码测试：管理员登录后输入错误系统密码，应提示“系统密码错误，禁止执行危险操作”。
7. 确认文字错误测试：确认文字不等于 `清空历史`，前端应提示“确认文字不正确，请输入：清空历史”。
8. 日期为空测试：不选开始日期或结束日期，应提示“请选择开始日期和结束日期”。
9. 日期范围错误测试：开始日期晚于结束日期，应提示“开始日期不能晚于结束日期”。
10. 日期格式错误测试：如果绕过前端直接传非 `YYYY-MM-DD` 格式，应返回“日期格式错误，请使用 YYYY-MM-DD”。

### 当前测试状态

已测试通过。

代码检查：

```text
C:\Users\86138\miniconda3\python.exe -m py_compile app.py
通过。

Get-Content -Raw history-danger-zone.js | node --check -
通过。
```

用户已完成手动功能测试，结果如下：

1. 后端可以正常启动。
2. `/docs` 中能看到 `DELETE /update-history/clear-range-admin`。
3. 管理员使用未来日期 `2099-01-01` 到 `2099-01-02` 测试，成功返回“已清空 0 条更新历史”。
4. 密码错误、确认文字错误、日期倒置等错误提示正常。
5. 普通用户权限测试正常。
6. 没有发现正式数据被误删。

### 下次继续建议

下次如果该功能测试通过，可继续做“后端接口真正权限校验”，优先核对审核、采集、数据管理等管理类接口是否仍只依赖前端隐藏。继续保持最小范围修改，不要重构项目。

## 2026-06-06 本次修改记录

### 本次功能名称

后端接口真正权限校验（第一期）

### 本次完成

完成第一期后端权限加固。重点保护管理类、危险类、修改类接口，复用现有 `AuthUser.session_token`、`role == "admin"`、`get_user_by_token`、`require_admin_user`、`verify_system_password` 等机制。

本期不改数据库结构，不删除功能，不做真实删除、真实清空、真实导入测试。公开浏览、提交线索、提交反馈、登录注册、用户资料编辑继续保持可用。

### 只读审计分类

公开接口，普通用户或未登录用户可以使用：

```text
GET  /
GET  /health
GET  /db/status
GET  /scheduler/status
GET  /clues
GET  /clues/{clue_id}
POST /clues
GET  /verified-items
GET  /verified-items/{item_id}
GET  /search
GET  /feedbacks
GET  /feedbacks/stats
POST /feedbacks
POST /feedbacks-v2
GET  /map-points
GET  /map-points/{point_id}
GET  /routes
GET  /routes/{route_id}
POST /update-history
GET  /update-history
POST /auth/register
POST /auth/register-v2
POST /auth/login
POST /auth/me
POST /auth/logout
```

登录用户接口，普通用户登录后可以使用：

```text
PATCH /auth/profile
PATCH /auth/profile-v2
```

管理员接口，本期已加后端 admin 校验：

```text
POST   /scheduler/test-run
POST   /crawler/targets
PATCH  /crawler/targets/{target_id}
POST   /crawler/targets/{target_id}/toggle
DELETE /crawler/targets/{target_id}
POST   /crawler/public-url
POST   /verified-items
POST   /admin/clues/{clue_id}/approve
PATCH  /clues/{clue_id}/status
PATCH  /clues/{clue_id}
DELETE /clues/{clue_id}
POST   /clues/{clue_id}/archive
POST   /clues/{clue_id}/restore
PATCH  /verified-items/{item_id}
DELETE /verified-items/{item_id}
POST   /verified-items/{item_id}/archive
POST   /verified-items/{item_id}/restore
DELETE /feedbacks/{feedback_id}
POST   /map-points
PATCH  /map-points/{point_id}
POST   /map-points/{point_id}/archive
POST   /map-points/{point_id}/restore
DELETE /map-points/{point_id}
POST   /routes
PATCH  /routes/{route_id}
POST   /routes/{route_id}/archive
POST   /routes/{route_id}/restore
DELETE /routes/{route_id}
PATCH  /detail-edit/clues/{clue_id}
PATCH  /detail-edit/verified-items/{item_id}
DELETE /update-history/clear-all
DELETE /update-history/clear-all-admin
DELETE /update-history/clear-all-admin-v2
DELETE /update-history/clear-range-admin
GET    /backup/export
POST   /backup/import
GET    /backup/import-code
POST   /backup/import-v2
POST   /backup/import-v3
```

### 修改文件

```text
01_后端服务/app.py
新增 AdminTokenRequest 和 require_admin_token 小 helper；复用 require_admin_user 校验管理员。为第一期高风险接口补 token 字段或 token query，并在接口入口调用管理员校验。优化未登录/非管理员错误提示。

02_前端页面/app.js
审核中心、采集管理相关写请求补 token，包括审核通过、线索状态修改、删除线索/真实库、采集目标新增/切换/删除、手动触发采集。

02_前端页面/manage.js
数据管理页编辑、归档、恢复、删除线索和真实库时补 token。

02_前端页面/map.js
地图中心新增、编辑、归档、恢复、删除地图点时补 token。

02_前端页面/route.js
路线中心新增、编辑、归档、恢复、删除路线时补 token。

02_前端页面/detail.js
线索/真实库详情页审核、状态修改、归档、恢复、删除时补 token。

02_前端页面/map-route-detail.js
地图点/路线详情页归档、恢复、删除时补 token。

02_前端页面/map-route-detail-edit.js
地图点/路线详情页保存编辑时补 token。

02_前端页面/clue-item-detail-edit.js
线索/真实库详情页保存编辑时补 token。

02_前端页面/backup.js
备份导出请求补 token。

02_前端页面/backup-import.js
备份导入验证码请求补 token；导入 v3 已有 token，继续保留。

02_前端页面/feedback-admin-v2.js
反馈中心 V2 删除反馈请求补 token，避免加载时序导致旧请求绕过。

02_前端页面/feedback-admin-final.js
反馈中心最终版删除反馈请求补 token。

02_前端页面/feedback.js
线索/真实库详情页删除反馈请求补 token。

02_前端页面/map-route-feedback.js
地图点/路线详情页删除反馈请求补 token。

02_前端页面/submit-action-fix.js
该脚本会覆盖采集目标、地图点、路线创建函数，本次补 token，避免覆盖其他脚本中的 token 修改。

多个 HTML 页面
更新直接引用脚本的版本号，避免浏览器缓存旧 JS。
```

未修改：

```text
01_后端服务/models.py
01_后端服务/database.py
02_前端页面/global-loader.js
```

### 新增或复用的权限 helper

复用：

```text
get_user_by_token(db, token)
require_admin_user(db, token)
verify_system_password(system_password)
AuthUser.session_token
AuthUser.role == "admin"
```

新增小 helper：

```python
class AdminTokenRequest(BaseModel):
    token: str = ""

def require_admin_token(db: Session, token: Optional[str]):
    return require_admin_user(db, token or "")
```

错误提示：

```text
未登录或 token 无效：请先登录 / 请先登录，登录状态无效或已过期
非管理员：无权限操作，只有管理员可以执行此操作
```

### 接口权限表

```text
GET  /clues                         公开       不需要系统密码
GET  /clues/{id}                    公开       不需要系统密码
POST /clues                         公开       不需要系统密码
GET  /verified-items                公开       不需要系统密码
GET  /verified-items/{id}           公开       不需要系统密码
GET  /map-points                    公开       不需要系统密码
GET  /map-points/{id}               公开       不需要系统密码
GET  /routes                        公开       不需要系统密码
GET  /routes/{id}                   公开       不需要系统密码
POST /feedbacks                     公开       不需要系统密码
POST /feedbacks-v2                  公开       不需要系统密码
PATCH /auth/profile                 登录用户   不需要系统密码
PATCH /auth/profile-v2              登录用户   不需要系统密码

POST   /scheduler/test-run                  管理员   不需要系统密码
POST   /crawler/targets                     管理员   不需要系统密码
PATCH  /crawler/targets/{id}                管理员   不需要系统密码
POST   /crawler/targets/{id}/toggle         管理员   不需要系统密码
DELETE /crawler/targets/{id}                管理员   不需要系统密码
POST   /crawler/public-url                  管理员   不需要系统密码
POST   /verified-items                      管理员   不需要系统密码
POST   /admin/clues/{id}/approve            管理员   不需要系统密码
PATCH  /clues/{id}/status                   管理员   不需要系统密码
PATCH  /clues/{id}                          管理员   不需要系统密码
DELETE /clues/{id}                          管理员   不需要系统密码
POST   /clues/{id}/archive                  管理员   不需要系统密码
POST   /clues/{id}/restore                  管理员   不需要系统密码
PATCH  /verified-items/{id}                 管理员   不需要系统密码
DELETE /verified-items/{id}                 管理员   不需要系统密码
POST   /verified-items/{id}/archive         管理员   不需要系统密码
POST   /verified-items/{id}/restore         管理员   不需要系统密码
DELETE /feedbacks/{id}                      管理员   不需要系统密码
POST   /map-points                          管理员   不需要系统密码
PATCH  /map-points/{id}                     管理员   不需要系统密码
POST   /map-points/{id}/archive             管理员   不需要系统密码
POST   /map-points/{id}/restore             管理员   不需要系统密码
DELETE /map-points/{id}                     管理员   不需要系统密码
POST   /routes                              管理员   不需要系统密码
PATCH  /routes/{id}                         管理员   不需要系统密码
POST   /routes/{id}/archive                 管理员   不需要系统密码
POST   /routes/{id}/restore                 管理员   不需要系统密码
DELETE /routes/{id}                         管理员   不需要系统密码
PATCH  /detail-edit/clues/{id}              管理员   不需要系统密码
PATCH  /detail-edit/verified-items/{id}     管理员   不需要系统密码
GET    /backup/export                       管理员   不需要系统密码
GET    /backup/import-code                  管理员   不需要系统密码
POST   /backup/import                       管理员   旧接口仍需要 password 或 system_password
POST   /backup/import-v2                    管理员   旧接口仍需要旧 password + 验证码
POST   /backup/import-v3                    管理员   需要系统密码 + 验证码
DELETE /update-history/clear-all            管理员   旧接口仍需要 password + 确认文字
DELETE /update-history/clear-all-admin      管理员   需要管理员账号密码 + 确认文字
DELETE /update-history/clear-all-admin-v2   管理员   需要系统密码 + 确认文字
DELETE /update-history/clear-range-admin    管理员   需要系统密码 + 确认文字
```

### 前端变化

补 token 的页面/脚本：

```text
app.js
manage.js
map.js
route.js
detail.js
map-route-detail.js
map-route-detail-edit.js
clue-item-detail-edit.js
backup.js
backup-import.js
feedback-admin-v2.js
feedback-admin-final.js
submit-action-fix.js
```

统一使用已有 token key：

```text
xgw_user_token
```

更新版本号：

```text
admin.html: app.js?v=9
crawler.html: app.js?v=9, submit-action-fix.js?v=10
manage.html: manage.js?v=2
map.html: map.js?v=9, submit-action-fix.js?v=10
route.html: route.js?v=10, submit-action-fix.js?v=10
clue-detail.html / item-detail.html: detail.js?v=9, clue-item-detail-edit.js?v=23, submit-action-fix.js?v=10
map-detail.html / route-detail.html: map-route-detail.js?v=21, map-route-detail-edit.js?v=22
backup.html: backup.js?v=32, backup-import.js?v=51
feedback-admin.html: feedback-admin-v2.js?v=24, feedback-admin-final.js?v=25
submit.html: submit-action-fix.js?v=10
clue-detail.html / item-detail.html: feedback.js?v=9
map-detail.html / route-detail.html: map-route-feedback.js?v=24
```

没有新增全局 JS，没有修改 `global-loader.js`。

### 测试步骤

1. 启动后端：双击项目根目录 `打开后端.bat`，或在 `01_后端服务` 运行 `py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000`。
2. 打开 `http://127.0.0.1:8000/docs`，确认接口仍存在。
3. 管理员测试：登录管理员账号，分别打开审核中心、数据管理、采集管理、备份、反馈中心、地图中心、路线中心，验证页面加载正常。
4. 管理员安全测试：不要真实删除数据；可用错误密码、空 token、未来日期 0 条影响等方式测试拒绝和 0 影响路径。
5. 普通用户测试：登录普通用户，确认管理页仍被前端权限守卫拦截；如果直接请求本期加固接口，应返回 401 或 403。
6. 错误 token 测试：在接口工具中传空 token 或伪造 token 请求 `GET /backup/export?token=bad`、`DELETE /feedbacks/1?token=bad`，应返回“请先登录”类错误，不应执行操作。
7. 公开功能回归：未登录或普通用户打开首页、列表、详情页，提交线索、提交反馈，应继续可用。

安全测试提醒：

```text
不要真实删除线索、真实库、地图点、路线、反馈、用户。
不要真实清空历史。
不要真实导入备份。
不要修改真实用户密码。
```

### 当前测试状态

已完成静态检查：

```text
C:\Users\86138\miniconda3\python.exe -m py_compile app.py
通过。

Get-Content -Raw <修改过的 JS> | node --check -
全部通过。
```

未完成后端导入/启动检查：

```text
C:\Users\86138\miniconda3\python.exe -c "import app"
失败原因：当前 Miniconda 环境缺少 fastapi。
```

说明：这不是代码语法错误；实际项目后端可能使用另一个已安装依赖的 Python 环境启动。待人工用 `打开后端.bat` 或原后端环境做浏览器测试。

### 第二期待做

1. 核对 `detail.js` 中“用户核验”按钮是否应继续允许普通用户修改线索状态。本期将 `PATCH /clues/{id}/status` 归为管理员写接口；如果要保留普通用户核验，应下一期新增独立、安全的普通用户核验接口，而不是复用管理状态修改接口。
2. 核对未被当前主要页面引用的旧脚本：`feedback-admin.js`、`feedback.js`、`map-route-feedback.js`、`detail.js` 中旧管理入口是否仍有页面可触达；本期已加固后端，即使旧脚本不传 token 也会被拒绝。
3. 数据看板统计接口目前仍复用公开 GET 数据源，前端已有管理员权限守卫；如需进一步加固，可新增专用 dashboard 后端接口并要求 admin。
4. `/update-history` 的创建接口仍公开，用于前端自动记录历史；后续如要防滥用，可设计服务端内部记录机制或登录用户限制。
5. 继续梳理审核、采集、数据管理页面的普通用户体验，避免按钮闪现后再被后端拒绝。

## 2026-06-06 本次修改记录

### 本次功能名称

后端接口真正权限校验（第一期）非破坏性自检 + 测试准备

### 本次完成

本次没有继续开发新功能，没有修改业务逻辑。已确认上一任务“后端接口真正权限校验（第一期）”已经记录到本文档中，且记录包含：

```text
修改文件清单
加固接口清单 / 接口权限表
测试状态
第二期待做
手动测试步骤
```

本次新增人工测试清单和安全测试 bat，方便后续人工测试时按步骤执行。

### 修改文件

```text
权限加固第一期_人工测试清单.md
新增人工测试清单，包含测试前准备、公开功能测试、管理员页面测试、管理员低风险操作测试、普通用户权限测试、bad token 测试、失败信息收集和禁止测试事项。

权限加固_安全测试命令.bat
新增非破坏性 CMD 测试脚本，只使用 bad token 和未来日期测试权限拒绝；不写真实管理员 token，不删除数据，不导入备份，不修改数据库。

项目交接与续接总文档.md
追加本次非破坏性自检和测试准备记录。
```

### 业务代码变化

无。本次没有修改：

```text
01_后端服务/app.py
01_后端服务/models.py
01_后端服务/database.py
02_前端页面/*.js
02_前端页面/*.html
02_前端页面/global-loader.js
```

### 安全测试 bat 内容说明

`权限加固_安全测试命令.bat` 只测试：

```text
GET    /backup/export?token=bad
DELETE /update-history/clear-range-admin
```

其中区间清空请求使用：

```text
token=bad
system_password=xgw2026
confirm_text=清空历史
start_date=2099-01-01
end_date=2099-01-02
```

预期返回：

```text
401 或 403
```

不应返回备份 JSON，也不应删除任何更新历史。

### 测试准备状态

已完成测试准备，待人工浏览器测试。

下一次继续时，优先执行：

```text
权限加固第一期_人工测试清单.md
权限加固_安全测试命令.bat
```

不要直接继续新功能；先确认“后端接口真正权限校验（第一期）”人工测试通过。

### 下次继续建议

1. 先关闭旧后端。
2. 双击 `打开后端.bat`。
3. 打开 `http://127.0.0.1:8000/docs`。
4. 按 `权限加固第一期_人工测试清单.md` 执行测试。
5. 可先运行 `权限加固_安全测试命令.bat` 做 bad token 非破坏性测试。
6. 测试期间不要真实删除、真实清空、真实导入、修改密码或改数据库。

## 2026-06-06 本次修改记录

### 本次功能名称

权限加固安全测试 bat 修复

### 本次完成

只修复 `权限加固_安全测试命令.bat` 的第二条安全测试命令，没有修改后端代码、前端业务 JS 或任何功能逻辑。

### 问题记录

运行原 `权限加固_安全测试命令.bat` 时：

```text
GET /backup/export?token=bad
返回 401，通过。

DELETE /update-history/clear-range-admin with bad token
原 bat 中 PowerShell 构造 confirm_text 失败，报错：
值不能为 null。参数名: argument
导致接口返回 422。
```

用户手动运行修正版命令后，`DELETE /update-history/clear-range-admin` 使用 `token=bad` 和未来日期返回：

```text
STATUS: 401
```

说明后端权限拦截通过。

### 修改文件

```text
权限加固_安全测试命令.bat
将第二条测试里的 confirm_text 构造方式改为稳定写法：
$confirm=([string][char]28165)+([string][char]31354)+([string][char]21382)+([string][char]21490)
```

### 当前测试状态

待重新运行 `权限加固_安全测试命令.bat` 确认脚本整体输出。已知用户手动修正版命令验证过后端返回 401。

### 下次继续建议

下次继续时先运行修复后的 `权限加固_安全测试命令.bat`。预期两条测试都返回 401 或 403，且不返回备份 JSON、不删除任何更新历史。

## 2026-06-06 本次修改记录

### 本次功能名称

普通用户无权限页请求日志清理 + 上下箭头切换选项快捷键

### 本次完成

修复普通用户访问管理页面时，“无权限访问”页面上方反复显示 `/auth/me` 请求日志的问题。请求诊断信息默认只保留在浏览器 Console，不再追加到页面 DOM；管理员权限守卫在 token 未变化且角色已确认时复用已知角色，避免无权限页面持续重复请求 `/auth/me`。

完善全局快捷键：当焦点在 `select` 或 `radio` 上时，按 `ArrowUp` / `ArrowDown` 可以切换上一个或下一个选项，并触发 `change` 事件。普通文本输入框和 `textarea` 不拦截上下箭头，原有 Enter / Y / N 弹窗快捷键逻辑保持不变。

### 修改文件清单

```text
02_前端页面/request-timeout-fix.js
默认不再把请求诊断日志写入页面 DOM；如确实需要页面内诊断框，可手动设置 window.XGW_SHOW_REQUEST_DEBUG_BOX = true。

02_前端页面/admin-permission-guard.js
token 未变化且角色已确认时不再重复请求 /auth/me，只重新应用当前权限状态。

02_前端页面/modal-shortcuts.js
升级为 V3，新增 select/radio 的 ArrowUp / ArrowDown 选项切换逻辑。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 52 更新到 53。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=52 同步为 v=53，确保浏览器加载最新全局脚本。
```

### 当前测试状态

已完成 JS 语法检查：

```text
node --check 02_前端页面/request-timeout-fix.js
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查结果通过。本次没有修改后端代码、数据库结构或后端权限逻辑。

### 手动测试建议

1. 关闭旧后端后双击 `打开后端.bat`。
2. 使用普通用户登录，依次打开 `admin.html`、`manage.html`、`crawler.html`、`backup.html`、`feedback-admin.html`、`dashboard.html`。
3. 预期只看到干净的“无权限访问”提示，页面正文不再出现“正在请求 / 请求完成 / auth/me”日志。
4. 使用管理员登录，确认上述管理页面仍能正常进入。
5. 在任意包含 `select` 的页面中聚焦下拉框，按上下箭头确认选项会切换并触发页面原有联动。
6. 在任意同组 `radio` 上聚焦，按上下箭头确认可切换同组单选项。
7. 在普通文本输入框和 `textarea` 中按上下箭头，预期仍是默认光标或滚动行为，不应强制切换选项。

### 下次继续建议

继续完成“后端接口真正权限校验（第一期）”人工测试。若发现某个页面仍有请求日志残留，请记录页面名称、当前登录角色、浏览器 Console、页面正文截图和具体请求 URL。

## 2026-06-06 本次修改记录

### 本次功能名称

初始化本地 Git 仓库，建立安全版本回退点

### 本次完成

已在项目根目录初始化本地 Git 仓库，用于本机版本回退和后续变更追踪。本次不上传 GitHub，不移动文件，不重构目录，不删除文件，不修改业务代码。

### .gitignore 重要规则

已新增项目根目录 `.gitignore`，重点忽略：

```text
Python 缓存：__pycache__/、*.pyc、*.pyo
虚拟环境：.venv/、venv/、env/
数据库和本地运行数据：*.db、*.sqlite、*.sqlite3、xiaoguwei.db
备份和压缩包：*.zip、xgw_backup_*.json、xgw_before_auth_test_*.zip
环境密钥：.env、.env.*
系统文件：.DS_Store、Thumbs.db
编辑器个人状态：.idea/workspace.xml、.idea/tasks.xml、.idea/usage.statistics.xml、.idea/shelf/
```

数据库文件 `01_后端服务/xiaoguwei.db` 不纳入 Git，避免提交本地测试数据、用户数据或运行态数据。

### 当前提交状态

```text
chore: initialize project git checkpoint after permission hardening
```

当前提交尚未完成。原因是本机 Git 未配置 `user.name` 和 `user.email`，Git 拒绝创建提交。当前项目文件已执行 `git add .` 暂存，待用户配置真实提交身份后，可继续执行同一条 `git commit` 命令完成本地回退点。

### 后续 Git 建议

1. 以后每次 Codex 修改前，建议先运行 `git status` 查看当前工作区状态。
2. 修改完成并测试后，建议及时 `git add .` 和 `git commit` 建立回退点。
3. 如果要做大规模文件夹整理、批量重命名或跨模块重构，应先新建分支，或至少先提交当前稳定状态。
4. 数据库、备份包、虚拟环境、缓存和环境密钥继续保持不提交。

## 2026-06-06 项目目录细分整理方案（暂未执行）

### 当前状态

本次只做方案记录，没有移动文件，没有修改代码，没有修改 HTML 引用。

当前前端目录 `02_前端页面` 采用平铺结构：HTML、CSS、页面脚本、全局脚本、智能填充脚本、工具脚本都放在同一层。随着功能增加，文件数量已经较多，后续适合逐步细分。

当前后端目录 `01_后端服务` 主要文件：

```text
app.py
models.py
database.py
crawler_service.py
scheduler.py
xiaoguwei.db
.venv/
__pycache__/
```

其中 `xiaoguwei.db`、`.venv/`、`__pycache__/` 已由 `.gitignore` 忽略，不应纳入 Git。

### 建议目标目录结构

前端建议目标结构：

```text
02_前端页面/
  pages/
    index.html
    submit.html
    admin.html
    manage.html
    crawler.html
    backup.html
    dashboard.html
    feedback-admin.html
    history.html
    map.html
    route.html
    clue-detail.html
    item-detail.html
    map-detail.html
    route-detail.html

  css/
    style.css

  js/
    global/
      global-loader.js
      page-scope-cleanup.js
      user-widget.js
      user-widget-v2.js
      modal-shortcuts.js
      password-eye.js
      user-message-clear.js
      user-avatar-click-upload.js
      danger-message-clear.js
      admin-permission-guard.js

    common/
      app.js
      archive-filter.js
      detail-link.js
      detail.js
      feedback.js
      request-timeout-fix.js

    history/
      history.js
      history-auto.js
      history-badge.js
      history-danger-zone.js
      history-filter-v2.js
      history-inline.js

    admin/
      manage.js
      manage-filter.js
      dashboard.js
      dashboard-v2.js
      dashboard-feedback.js

    backup/
      backup.js
      backup-import.js
      backup-import-layout.js

    feedback/
      feedback-admin.js
      feedback-admin-v2.js
      feedback-admin-final.js
      feedback-history-link.js

    map-route/
      map.js
      route.js
      map-route-detail.js
      map-route-detail-edit.js
      map-route-detail-link.js
      map-route-feedback.js
      map-smart-fill.js
      route-smart-fill.js
      route-smart-fix.js
      route-load-fix.js
      route-ui-refactor.js

    smart-fill/
      smart-core.js
      generic-smart-fill.js
      generic-smart-fix.js
      smart-progress.js
      smart-progress-cleanup.js
      smart-edit-panel.js
      smart-edit-hook.js
      smart-form-mode.js
      submit-action-fix.js
      submit-success-reload.js
      clue-item-detail-edit.js

  tools/
    add-global-loader-to-html.py
    cleanup-global-scripts.py

  assets/
    （后续图片、图标、导入模板等静态资源放这里）
```

后端建议目标结构，放在后续阶段，不建议现在立即执行：

```text
01_后端服务/
  app.py                  （第一阶段暂时保留，避免破坏 uvicorn app:app 启动命令）
  database.py             （第一阶段暂时保留）
  models.py               （第一阶段暂时保留）
  crawler_service.py      （后续可迁入 app/services/）
  scheduler.py            （后续可迁入 app/services/ 或 app/jobs/）

  app/
    routers/
      auth.py
      clues.py
      admin.py
      history.py
      backup.py
      feedback.py
      map_route.py
      crawler.py
    services/
      crawler_service.py
      backup_service.py
      permission_service.py
      history_service.py
    schemas/
      auth.py
      admin.py
      history.py
      backup.py
    models/
      （仅当决定拆分 SQLAlchemy 模型时再启用）

  data/
    （本地数据库或运行态数据，仅本地使用，不提交 Git）

  scripts/
    （维护脚本、初始化脚本、非破坏性测试脚本）
```

后端当前 `app.py` 较大，但它直接承载 FastAPI 实例和大量接口。后端拆分风险高于前端静态文件整理，建议等权限加固第一期人工测试通过并完成 Git 提交后，再单独开任务处理。

### 全局脚本识别

当前由 `global-loader.js` 自动加载的全局脚本：

```text
page-scope-cleanup.js
user-widget.js
user-widget-v2.js
modal-shortcuts.js
password-eye.js
user-message-clear.js
user-avatar-click-upload.js
danger-message-clear.js
admin-permission-guard.js
```

这些脚本适合迁入：

```text
02_前端页面/js/global/
```

移动后必须同步修改 `global-loader.js` 中的加载路径。例如：

```text
原：page-scope-cleanup.js
新：js/global/page-scope-cleanup.js
```

如果 `global-loader.js` 自身也移动到 `js/global/global-loader.js`，所有 HTML 中的引用也要从：

```text
global-loader.js?v=53
```

改为：

```text
js/global/global-loader.js?v=新版本号
```

并继续更新 `GLOBAL_SCRIPT_VERSION`，避免浏览器缓存旧脚本。

### 页面专属脚本识别

按当前 HTML 直接引用关系，页面专属或页面组脚本建议归类如下。

首页、审核、采集、提交相关：

```text
index.html:
app.js, archive-filter.js, detail-link.js, history-badge.js, history-auto.js, global-loader.js

admin.html:
app.js, archive-filter.js, detail-link.js, history-auto.js, history-badge.js, global-loader.js

crawler.html:
app.js, generic-smart-fill.js, smart-progress.js, generic-smart-fix.js, submit-action-fix.js,
request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js, submit-success-reload.js,
smart-form-mode.js, smart-progress-cleanup.js, history-auto.js, global-loader.js

submit.html:
app.js, generic-smart-fill.js, smart-progress.js, generic-smart-fix.js, submit-action-fix.js,
request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js, submit-success-reload.js,
smart-form-mode.js, smart-progress-cleanup.js, history-auto.js, global-loader.js
```

备份页：

```text
backup.html:
backup.js, backup-import.js, backup-import-layout.js, global-loader.js
```

历史页：

```text
history.html:
history.js, history-filter-v2.js, history-danger-zone.js, global-loader.js
```

管理和看板页：

```text
manage.html:
manage.js, manage-filter.js, detail-link.js, history-auto.js, history-badge.js, global-loader.js

dashboard.html:
history-auto.js, dashboard-v2.js, global-loader.js
```

反馈管理页：

```text
feedback-admin.html:
feedback-admin.js, history-auto.js, history-badge.js, feedback-admin-v2.js,
feedback-admin-final.js, global-loader.js
```

线索/真实库详情页：

```text
clue-detail.html / item-detail.html:
detail.js, feedback.js, generic-smart-fill.js, smart-progress.js, generic-smart-fix.js,
submit-action-fix.js, request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js,
submit-success-reload.js, smart-form-mode.js, smart-progress-cleanup.js, history-auto.js,
history-badge.js, history-inline.js, clue-item-detail-edit.js, feedback-history-link.js,
global-loader.js
```

地图和路线页：

```text
map.html:
map.js, smart-core.js, map-smart-fill.js, smart-progress.js, submit-action-fix.js,
request-timeout-fix.js, smart-edit-panel.js, smart-edit-hook.js, submit-success-reload.js,
smart-form-mode.js, smart-progress-cleanup.js, history-auto.js, history-badge.js,
map-route-detail-link.js, global-loader.js

route.html:
route.js, route-smart-fill.js, smart-progress.js, route-smart-fix.js, submit-action-fix.js,
request-timeout-fix.js, route-load-fix.js, smart-edit-panel.js, smart-edit-hook.js,
submit-success-reload.js, route-ui-refactor.js, smart-form-mode.js,
smart-progress-cleanup.js, history-auto.js, history-badge.js, map-route-detail-link.js,
global-loader.js

map-detail.html / route-detail.html:
map-route-detail.js, history-auto.js, map-route-detail-edit.js,
map-route-feedback.js, feedback-history-link.js, global-loader.js
```

### 移动后需要同步修改的引用

前端每移动一类文件，都必须同步修改：

```text
1. 对应 HTML 的 <script src="..."> 路径。
2. 对应 HTML 的 <link rel="stylesheet" href="..."> 路径。
3. global-loader.js 内 globalScripts 的路径。
4. global-loader.js 的 GLOBAL_SCRIPT_VERSION。
5. 所有 HTML 中 global-loader.js 的版本号。
6. 如果脚本内部有动态加载、跳转或资源路径，也要逐项搜索确认。
```

当前 HTML 全部直接引用 `style.css`，如果移动到 `css/style.css`，所有 HTML 都要改为：

```text
css/style.css
```

如果 HTML 自身移动到 `pages/`，路径会反过来变成：

```text
../css/style.css
../js/...
```

因此第一阶段不建议移动 HTML 页面，只先移动少量 JS 或工具脚本，降低路径改动范围。

### 暂时不建议移动的文件

第一阶段不建议移动：

```text
02_前端页面/*.html
02_前端页面/style.css
02_前端页面/global-loader.js
01_后端服务/app.py
01_后端服务/models.py
01_后端服务/database.py
打开后端.bat
权限加固_安全测试命令.bat
权限加固第一期_人工测试清单.md
项目交接与续接总文档.md
小谷围岛广州大学城真实生活信息共建系统_续接文档.md
```

原因：

```text
HTML 和 style.css 路径影响所有页面。
global-loader.js 是全局脚本入口，移动它会影响所有 HTML。
app.py 是 uvicorn app:app 的启动入口，移动或拆分会影响后端启动方式。
models.py / database.py 被 app.py 直接使用，后端拆分需要单独测试。
bat 和文档位于根目录，便于非技术使用者双击启动和续接。
```

### 分阶段整理方案

第 0 阶段：准备回退点

```text
1. 先配置 Git user.name / user.email。
2. 完成当前首次 commit。
3. 运行 git status，确认工作区干净。
4. 新建分支，例如 git switch -c chore/frontend-folder-split-phase-1。
```

如果工作区不干净，不开始移动文件。

第 1 阶段：只移动工具脚本，风险最低

建议移动：

```text
02_前端页面/add-global-loader-to-html.py -> 02_前端页面/tools/add-global-loader-to-html.py
02_前端页面/cleanup-global-scripts.py -> 02_前端页面/tools/cleanup-global-scripts.py
```

这两个脚本不是页面运行时引用，移动后不需要修改 HTML。测试只需确认文件存在、后续维护脚本路径记录清楚。

第 2 阶段：移动全局脚本，但暂不移动 global-loader.js

建议移动：

```text
page-scope-cleanup.js
user-widget.js
user-widget-v2.js
modal-shortcuts.js
password-eye.js
user-message-clear.js
user-avatar-click-upload.js
danger-message-clear.js
admin-permission-guard.js
```

迁入：

```text
02_前端页面/js/global/
```

同步修改：

```text
global-loader.js 内 globalScripts 路径改为 js/global/xxx.js。
GLOBAL_SCRIPT_VERSION +1。
所有 HTML 中 global-loader.js?v=版本号同步更新。
```

测试重点：

```text
用户登录/退出/头像/用户菜单正常。
普通用户访问管理页仍显示无权限。
管理员访问管理页正常。
弹窗 Enter/Y/N 和 select/radio 上下键正常。
密码眼睛、消息清理、危险操作提示清理正常。
```

第 3 阶段：移动历史、备份、反馈等边界清晰的页面组脚本

优先移动：

```text
js/history/
js/backup/
js/feedback/
```

对应更新：

```text
history.html
backup.html
feedback-admin.html
clue-detail.html
item-detail.html
map-detail.html
route-detail.html
```

测试重点：

```text
history.html 历史列表、筛选、危险操作区。
backup.html 导出、导入区域显示和 bad token 安全测试。
feedback-admin.html 反馈列表、管理员处理状态。
详情页反馈提交和反馈历史入口。
```

第 4 阶段：移动地图/路线和智能填充脚本

建议迁移：

```text
js/map-route/
js/smart-fill/
```

这组脚本互相引用关系和页面交互较多，风险高于历史/备份/反馈。必须逐页测试：

```text
map.html
route.html
map-detail.html
route-detail.html
submit.html
crawler.html
clue-detail.html
item-detail.html
```

第 5 阶段：移动 HTML 到 pages/，移动 CSS 到 css/

这是影响最大的阶段，建议最后执行。移动后几乎所有相对路径都要调整：

```text
pages/index.html 引用 ../css/style.css
pages/index.html 引用 ../js/...
页面之间 location.href / a href 也可能要从 index.html 改为 ../pages/index.html 或同目录相对路径。
打开后端.bat 自动打开首页路径也要从 02_前端页面/index.html 改为 02_前端页面/pages/index.html。
```

不建议在权限加固人工测试完成前执行。

第 6 阶段：后端拆分

后端拆分应单独开任务。建议先从服务层开始，不先改启动入口：

```text
先保留 01_后端服务/app.py。
逐步把 crawler_service.py、scheduler.py 或备份/历史 helper 抽到 app/services/。
最后再把路由拆到 app/routers/。
```

每拆一个接口组，都要跑：

```text
python -m py_compile app.py models.py database.py
py -m uvicorn app:app --reload --host 127.0.0.1 --port 8000
打开 /docs
做对应页面的非破坏性测试
```

### 风险点

```text
1. 静态 HTML 使用相对路径，移动 HTML 会导致 CSS/JS 路径失效。
2. global-loader.js 负责全局脚本加载，路径改错会影响所有页面的登录组件、权限守卫和快捷键。
3. 多个页面复用同一脚本，例如 app.js、history-auto.js、history-badge.js、feedback-history-link.js，不能按单一页面理解。
4. 部分脚本有版本号参数，迁移时必须同步更新版本号避免缓存。
5. 后端 app.py 是启动入口，贸然拆分会影响 `py -m uvicorn app:app`。
6. 当前 Git 首次 commit 尚未完成，未建立可用 commit hash 前不适合开始大规模移动。
```

### 测试步骤

每一阶段移动后至少测试：

```text
1. git status，确认只出现本阶段计划内文件移动和引用修改。
2. 打开后端.bat 启动后端。
3. 打开 http://127.0.0.1:8000/docs，确认后端可用。
4. 打开 02_前端页面/index.html，确认首页加载 CSS 和 JS。
5. 浏览器 Network 确认没有 404 的 JS/CSS。
6. 普通用户登录、退出、提交线索、提交反馈。
7. 管理员打开 admin/manage/crawler/backup/feedback-admin/dashboard/history/map/route。
8. 普通用户访问管理页确认仍被拦截。
9. 运行权限加固安全测试 bat，只做 bad token / 未来日期非破坏性测试。
```

### Git 回退方式

开始整理前必须先完成 Git 提交。当前文档记录显示首次 commit 仍因未配置 `user.name` / `user.email` 未完成，因此下一步应先完成：

```text
git config user.name "你的名字"
git config user.email "你的邮箱"
git commit -m "chore: initialize project git checkpoint after permission hardening"
git status
```

如果已经提交并新建分支，移动失败时可用：

```text
git status
git restore .
```

如果某些移动已经暂存：

```text
git restore --staged .
git restore .
```

如果已经产生了一次错误提交，但还没有继续开发，可先看日志：

```text
git log --oneline -5
```

再决定是否用 `git revert <commit>` 做反向提交。除非非常明确当前分支只有本地错误整理提交，否则不要轻易使用 `git reset --hard`。

### 第一阶段建议结论

最安全的第一阶段只移动：

```text
02_前端页面/tools/add-global-loader-to-html.py
02_前端页面/tools/cleanup-global-scripts.py
```

其次再移动全局脚本到 `02_前端页面/js/global/`，但必须同步修改 `global-loader.js` 路径和版本号。HTML、CSS、后端入口文件和 bat 暂时不要移动。

## 2026-06-06 本次修改记录

### 本次功能名称

完成本地 Git 首次提交

### 本次完成

本次只处理本地 Git 首次提交，不做功能开发，不移动文件，不删除文件，不上传 GitHub。

已在当前项目仓库内设置本地 Git 用户信息：

```text
user.name = 86138
user.email = 86138@example.local
```

该邮箱仅作为本地 Git 占位邮箱使用；如果后续要上传 GitHub，可再改为 GitHub 邮箱或 noreply 邮箱。

### .gitignore 检查

已确认 `.gitignore` 包含本地运行数据和敏感/临时文件忽略规则：

```text
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.db
*.sqlite
*.sqlite3
xiaoguwei.db
*.zip
xgw_backup_*.json
xgw_before_auth_test_*.zip
.env
.env.*
```

数据库 `01_后端服务/xiaoguwei.db` 不纳入 Git。

### 提交信息

```text
chore: initialize project checkpoint before directory refactor
```

### 后续建议

后续每次修改前先运行 `git status`，确认工作区状态；修改后完成测试再提交。若要执行目录整理，应先确认当前提交存在，再新建分支或至少保持当前稳定提交可回退。

## 2026-06-06 本次修改记录

### 本次功能名称

表单字段上下箭头键盘切换增强

### 本次完成

本次只修改前端键盘交互，没有修改后端、数据库或业务接口。继续复用已有 `modal-shortcuts.js`，未新增全局 JS 文件。

新增表单字段上下箭头切换能力：当焦点在常见单行输入框上时，`ArrowDown` 聚焦下一个可填写字段，`ArrowUp` 聚焦上一个可填写字段。字段顺序按页面 DOM 自然顺序计算，跳过 disabled、readonly、hidden、不可见元素和按钮类输入。

### select 优先级规则

`select` 下拉框优先处理内部选项：

```text
1. select 获得焦点时，默认进入“先切换 option”状态。
2. ArrowUp / ArrowDown 先切换 select 内部 option，并触发 change 事件。
3. 到达边界时不越界，也不会跳到其他字段。
4. 按 Enter 后，给当前 select 设置临时确认状态：data-xgw-select-confirmed="1"。
5. select 被 Enter 确认后，再按 ArrowDown / ArrowUp 才跳到下一个或上一个字段。
6. select blur 或重新 focus 后会清除确认状态，重新回到“先切换 option”的状态。
```

### textarea 规则

`textarea` 普通 `ArrowUp` / `ArrowDown` 保留浏览器原生行为，避免用户编辑多行说明时误跳字段。

如需从 `textarea` 跳字段：

```text
Ctrl + ArrowDown 或 Alt + ArrowDown：跳到下一个可填写字段
Ctrl + ArrowUp 或 Alt + ArrowUp：跳到上一个可填写字段
```

### radio / checkbox 规则

`radio` 仍然优先在同 name 单选组内切换：

```text
ArrowDown：切换到同组下一个 radio
ArrowUp：切换到同组上一个 radio
切换后 checked = true，并触发 change 事件
到边界时不越界，不跳字段
```

`checkbox` 可参与字段上下切换，但不改变勾选状态，不影响点击逻辑。

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V4，新增表单字段 ArrowUp / ArrowDown 导航；保留 Enter / Y / N 弹窗快捷键；保留 select/radio 内部切换优先级。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 53 更新到 54。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=53 同步为 v=54，确保浏览器加载最新全局脚本。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

建议重点测试：

```text
submit.html：单行输入框 ArrowDown 跳到下一个字段，ArrowUp 回到上一个字段。
route.html：路线名称、路线类型/分类、起点/区域等字段按 DOM 顺序切换。
map.html：地图点表单字段可上下切换。
crawler.html：采集目标表单字段可上下切换。
backup.html / history.html / feedback-admin.html / admin.html / manage.html：页面加载不报错。
```

重点验证：

```text
1. select 上 ArrowDown 先切换 option，不直接跳字段。
2. select 按 Enter 后，再 ArrowDown 才跳到下一个字段。
3. textarea 中普通 ArrowUp / ArrowDown 不跳字段。
4. textarea 中 Ctrl+ArrowUp / Ctrl+ArrowDown 可以跳字段。
5. radio 组中 ArrowUp / ArrowDown 只切换同组 radio。
6. button、submit、reset、hidden 不参与字段切换。
7. 弹窗 Enter / Y / N 快捷键仍正常；输入框里的 Y/N 不误触发弹窗。
```

### 当前测试状态

已测试通过。

用户已完成浏览器测试，结果如下：

```text
1. select 按 ArrowDown 可以展开并选择。
2. 按 Enter 后下拉框收起。
3. 再按 ArrowDown 可以跳到下一个填写框，不再继续改动当前 select。
4. textarea 边界跳转没有发现异常。
5. 普通 input 上下切换没有发现异常。
```

## 2026-06-06 本次修改记录

### 本次功能名称

select confirmed 状态改为 Enter + change 双来源

### 本次完成

本次只修复前端 select 键盘交互 bug，没有修改后端、数据库或业务接口。继续复用 `modal-shortcuts.js`，未新增 JS。

### 修复原因

上一版主要依赖 `keydown Enter` 设置 `data-xgw-select-confirmed="1"`。在原生 select 下拉框展开后，部分浏览器会由原生下拉控件处理 Enter，页面脚本不一定稳定收到该 Enter。因此确认状态不能只依赖 Enter。

### 当前 confirmed 设置来源

```text
1. select keydown Enter：
   设置 data-xgw-select-confirmed="1"，并 preventDefault / stopPropagation / stopImmediatePropagation。

2. select change：
   设置 data-xgw-select-confirmed="1"。
   这是原生下拉框展开后，用户用键盘选择 option 并按 Enter 收起时更可靠的确认来源。
```

### ArrowUp / ArrowDown 处理顺序

select 上按 `ArrowUp` / `ArrowDown` 时：

```text
1. 第一优先级检查 data-xgw-select-confirmed 是否为 "1"。
2. 如果已经 confirmed：
   - preventDefault()
   - stopPropagation()
   - stopImmediatePropagation()
   - 直接跳到上一个/下一个可填写字段
   - 不切换 option
   - 不调用 showPicker()
   - 不调用 click()
3. 只有未 confirmed 时，才尝试展开 select 或 fallback 手动切换 option。
```

### showPicker 处理

```text
1. 如果 select.showPicker() 存在并调用成功，本次 ArrowUp / ArrowDown 只负责展开下拉框。
2. showPicker 成功时不再手动修改 selectedIndex。
3. 后续用户通过原生下拉框选择 option 后，由 change 事件设置 confirmed。
4. 如果 showPicker 不支持或调用失败，才进入 fallback：尝试 click()，并手动切换 selectedIndex、触发 change。
```

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V7，select confirmed 状态由 Enter + change 双来源设置；confirmed 状态下箭头事件会被完全消费并直接跳字段；showPicker 成功时不再手动改 option。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 56 更新到 57。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=56 同步为 v=57。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

真实流程重点测试：

```text
1. 打开 submit.html。
2. 聚焦分类 select。
3. 按 ArrowDown，预期下拉框展开或进入 option 选择，不跳字段。
4. 用键盘选择一个 option。
5. 按 Enter，预期下拉框收回，并通过 change 或 Enter 标记 confirmed。
6. 再按 ArrowDown，预期跳到下一个填写框，不再改变当前 select 的 option。
7. 再聚焦这个 select，预期重新进入选择状态，ArrowDown 又先展开/选择 option。
8. route.html、map.html 的 select 重复测试。
9. textarea 首尾边界跳字段、中间行原生上下移动继续正常。
10. 普通 input 上下键切换字段继续正常。
11. radio 上下键继续只切换同组 radio。
12. 弹窗 Enter / Y / N 快捷键继续正常；输入框中的 y/n 不误触发弹窗。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

## 2026-06-06 本次修改记录

### 本次功能名称

修复 select Enter 确认后 ArrowUp / ArrowDown 未跳字段问题

### 本次完成

本次只修复前端键盘交互 bug，没有修改后端、数据库或业务接口。继续复用 `modal-shortcuts.js`，未新增 JS。

### 修复内容

修复前现象：

```text
select 聚焦后 ArrowDown 可以展开并切换 option。
按 Enter 后下拉框收回。
再次按 ArrowDown 时仍然继续改变 select option，没有跳到下一个填写框。
```

修复后逻辑：

```text
1. select 按 Enter 时，设置 data-xgw-select-confirmed="1"。
2. Enter 事件会 preventDefault 和 stopPropagation，避免提交表单或触发弹窗确认。
3. select 处于 confirmed 状态时，ArrowUp / ArrowDown 先判断 confirmed。
4. confirmed 状态下 ArrowUp / ArrowDown 直接跳到上一个/下一个可填写字段。
5. confirmed 状态下不再切换当前 select 的 option。
6. confirmed 状态下不再调用 showPicker() 或 click()。
7. 跳字段时会 preventDefault 和 stopPropagation，避免浏览器继续处理原生 select。
8. select blur 后延迟清理 confirmed 状态；如果只是原生下拉框收起导致临时 blur，但焦点仍在当前 select，则保留 confirmed。
9. select 重新获得焦点时清除旧 confirmed，重新进入“先切换 option”的状态。
```

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V6，调整 select confirmed 判断顺序，确认后箭头直接跳字段，并消费键盘事件。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 55 更新到 56。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=55 同步为 v=56。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

重点测试：

```text
1. submit.html 中聚焦分类/平台等 select。
2. 按 ArrowDown，确认先展开/切换 option，不跳字段。
3. 按 Enter，确认当前 select。
4. 再按 ArrowDown，确认跳到下一个可填写字段，当前 select option 不再变化。
5. 再回到该 select，按 ArrowDown，确认重新进入先切换 option 的状态。
6. route.html 中测试路线类型/分类 select。
7. map.html 中测试分类 select。
8. textarea 第一行开头 ArrowUp、最后一行末尾 ArrowDown、中间行上下移动继续正常。
9. 普通 input ArrowUp / ArrowDown 仍能切换字段。
10. radio 组 ArrowUp / ArrowDown 仍只切换同组 radio。
11. 弹窗 Enter / Y / N 快捷键仍正常；输入框中的 y/n 不误触发弹窗。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

### 下次继续建议

下次优先在 `submit.html`、`route.html`、`map.html`、`crawler.html` 做键盘实测。如果发现某个字段顺序不符合预期，先记录页面、字段名称和 DOM 顺序，再决定是否给该页面做局部 `tabindex` 或更细的字段选择规则。

## 2026-06-06 本次修改记录

### 本次功能名称

表单键盘交互小修补：select 尝试展开 + textarea 边界跳字段

### 本次完成

本次只修改前端键盘交互，没有修改后端、数据库或业务接口。继续复用 `modal-shortcuts.js`，未新增 JS。

### select 展开兼容逻辑

当焦点在 `select` 上，用户按 `ArrowUp` / `ArrowDown` 时，仍然优先切换下拉框内部 option，并触发 `change`。同时新增“尽量展开下拉框”的兼容尝试：

```text
1. 每次 select 获得焦点后，只尝试展开一次，避免反复闪烁或重复报错。
2. 优先调用 select.showPicker()。
3. 如果浏览器不支持 showPicker，则尝试 select.click()。
4. 如果浏览器安全策略不允许脚本展开原生 select，捕获异常，只保留 option 切换逻辑。
5. select blur 或重新 focus 时清除本次尝试状态。
```

兼容性说明：原生 `select` 是否能被 JavaScript 展开取决于浏览器和安全策略。支持时会尽量展开，不支持时不会报错，仍可用上下键切换 option。

### textarea 边界跳字段

`textarea` 普通 `ArrowUp` / `ArrowDown` 默认仍保留原生多行光标移动。

新增边界跳转：

```text
1. selectionStart === 0 且 selectionEnd === 0 时，按 ArrowUp 跳到上一个可填写字段。
2. selectionStart === value.length 且 selectionEnd === value.length 时，按 ArrowDown 跳到下一个可填写字段。
3. 如果用户选中了文字，即 selectionStart !== selectionEnd，不跳字段，保留原生行为。
4. 光标在中间行或非边界位置时，不跳字段。
5. Ctrl+ArrowUp / Ctrl+ArrowDown 或 Alt+ArrowUp / Alt+ArrowDown 仍保留为强制跳字段。
```

跳字段时会 `preventDefault`，避免页面滚动。

### 修改文件清单

```text
02_前端页面/modal-shortcuts.js
升级为 V5，增加 select 尝试展开逻辑，增加 textarea 首尾边界普通上下键跳字段逻辑。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 54 更新到 55。

02_前端页面/*.html
将 global-loader.js 引用版本从 v=54 同步为 v=55。
```

### 检查结果

已完成 JS 语法检查：

```text
node --check 02_前端页面/modal-shortcuts.js
node --check 02_前端页面/global-loader.js
```

检查通过。`git status` 确认本次没有修改 `01_后端服务`，没有修改数据库文件。

### 手动测试步骤

建议测试：

```text
1. submit.html / route.html / map.html / crawler.html 中聚焦 select，按 ArrowDown，确认优先切换 option。
2. 在支持 showPicker 的浏览器中，观察 select 是否尽量展开；不支持时确认没有报错且 option 仍会切换。
3. select 按 Enter 确认后，再按 ArrowDown / ArrowUp，确认可跳到下一个/上一个字段。
4. textarea 光标在中间行时，普通 ArrowUp / ArrowDown 只移动文本光标。
5. textarea 光标在第一行最前时，ArrowUp 跳到上一个字段。
6. textarea 光标在最后一行最后时，ArrowDown 跳到下一个字段。
7. textarea 有选中文本时，ArrowUp / ArrowDown 不跳字段。
8. Ctrl+ArrowUp / Ctrl+ArrowDown 仍可强制跳字段。
9. history.html / backup.html / feedback-admin.html / admin.html / manage.html 页面加载不报错。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

## 2026-06-06 前端权限入口显示最小修复

### 本次完成内容

本次是在回退 public-nav 全局导航方案后采用的最小修复策略。不新增 `public-nav.js`，不全局重建标题栏，不隐藏旧 header 按钮，不大规模修改 HTML 结构。

修复目标：

```text
1. 首页已有的 history.html / 更新历史入口保持公开可见，未登录用户、普通用户、管理员都可以看到。
2. 管理采集目标、采集管理、审核中心、数据管理、数据看板、备份、反馈管理等管理入口只对管理员显示。
3. 后端、数据库、页面结构均未修改。
```

### 前端权限显示规则

公开入口：`history.html` 明确排除在管理员入口隐藏逻辑之外，因此“历史中心 / 更新历史”不会被 `admin-permission-guard.js` 当作管理员入口隐藏。

管理入口：权限守卫现在同时根据按钮目标和按钮文字识别管理入口。目标页面包括：

```text
admin.html
manage.html
crawler.html
dashboard.html
backup.html
feedback-admin.html
```

只要按钮或链接的 `href`、`onclick`、`data-nav-target` 指向以上管理页面，未登录用户和普通用户会隐藏，管理员可见。

### 修改文件清单

```text
02_前端页面/admin-permission-guard.js
最小修复管理入口识别逻辑：显式排除 history.html；新增基于 href / onclick / data-nav-target 的管理页面目标判断；补充管理文字名单。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 57 更新到 58。

02_前端页面/*.html
仅机械同步 global-loader.js 引用版本，从 v=57 更新到 v=58；未改 HTML 结构。

项目交接与续接总文档.md
追加本次记录。
```

### 检查结果

已完成：

```text
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/global-loader.js
```

检查通过。已确认没有新增 `02_前端页面/public-nav.js`。已确认没有修改 `01_后端服务` 和数据库文件。未执行 git commit。

### 人工测试步骤

未登录测试：

```text
1. 打开 index.html。
2. 应能看到“更新历史 / 历史中心”入口。
3. 不应看到“管理采集目标 / 采集管理 / 审核中心 / 数据管理 / 数据看板 / 备份 / 反馈管理”。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 index.html。
3. 应能看到“更新历史 / 历史中心”入口。
4. 不应看到管理入口。
5. 直接打开 crawler.html / manage.html / admin.html / backup.html / dashboard.html / feedback-admin.html，应显示无权限访问。
```

管理员测试：

```text
1. 登录管理员。
2. 打开 index.html。
3. 应能看到历史入口和管理入口。
4. 管理页能正常打开。
```

### 当前测试状态

已完成静态检查和 JS 语法检查，待人工浏览器测试。

## 2026-06-08 头像菜单增加切换账号

### 本次完成

本次只修改前端用户头像菜单和登录状态刷新逻辑，没有修改后端、数据库、顶部标题栏结构，也没有新增 `public-nav.js`。

已登录用户点击右上角头像后，菜单中在“退出登录”上方新增“切换账号”按钮。点击后会清除当前本地登录 token 和用户缓存，触发全局登录状态变化事件 `xgw-auth-changed`，并打开现有登录弹窗。

### 修改文件

```text
02_前端页面/user-widget.js
新增“切换账号”菜单按钮；新增 clearUserWidgetAuthState、notifyUserWidgetAuthChanged、switchAccount；登录、注册、保存资料、退出登录后触发 xgw-auth-changed；组件监听 xgw-auth-changed 后刷新头像、用户名、角色和菜单。

02_前端页面/user-widget-v2.js
V2 注册和保存资料成功后触发 xgw-auth-changed，确保权限入口和用户显示及时刷新。

02_前端页面/admin-permission-guard.js
监听 xgw-auth-changed，登录状态变化后立即重新检查权限；无 token 时仍直接判定 guest，不请求 /auth/me。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 59 更新到 60。

02_前端页面/*.html
仅机械同步 global-loader.js 引用版本，从 v=59 更新到 v=60，未改 HTML 结构。

项目交接与续接总文档.md
追加本次修改记录。
```

### 新增接口

无。本次没有新增或修改后端接口。

### 前端变化

```text
1. 已登录用户菜单保留“退出登录”，新增“切换账号”。
2. 切换账号不会跳转页面，会打开现有登录弹窗。
3. 切换账号会清除 localStorage 中的 xgw_user_token。
4. 切换账号会清空前端 currentUser 和 editingAvatarBase64。
5. 登录状态变化统一触发 xgw-auth-changed。
6. 权限守卫收到 xgw-auth-changed 后立即刷新管理员入口和无权限页。
```

### 后端变化

无。没有修改 `01_后端服务`，没有修改数据库文件。

### 测试步骤

```text
1. 登录管理员，点击头像，应看到“切换账号”和“退出登录”。
2. 点击“切换账号”，应清除当前账号并打开登录入口。
3. 登录普通用户后，头像和权限显示应立即变成普通用户。
4. 普通用户不应看到管理入口。
5. 再切换回管理员后，管理员入口应恢复。
6. 未登录状态打开管理页，应显示未登录用户无权限。
7. 普通用户打开管理页，应显示普通用户无权限。
8. 不应出现 /auth/me 刷屏；无 token 状态下不应反复请求 /auth/me。
```

### 当前测试状态

已完成 JS 语法检查：

```text
node --check 02_前端页面/user-widget.js
node --check 02_前端页面/user-widget-v2.js
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/global-loader.js
```

检查通过。已确认没有新增 `02_前端页面/public-nav.js`，没有修改后端和数据库。

人工浏览器测试已通过，结果如下：

```text
1. 管理员点击头像后，可以看到“切换账号”和“退出登录”。
2. 点击“切换账号”后，会清除当前账号并打开登录入口。
3. 登录普通用户后，头像、用户名、角色和权限显示能立即更新，不需要手动刷新。
4. 普通用户看不到管理入口。
5. 再切换回管理员后，管理员入口能恢复。
6. 未登录访问管理页显示未登录用户无权限。
7. 普通用户访问管理页显示普通用户无权限。
8. 没有出现 /auth/me 刷屏。
```

当前测试状态：已测试通过。

### 下次继续建议

本功能已通过人工测试，可提交本次前端改动。后续如果继续完善用户系统，可考虑把登录态事件、用户缓存和权限守卫抽成更明确的前端小工具，但本次不做导航重构和新功能包。
## 2026-06-06 修复无权限页未登录/普通用户身份提示不一致

### 本次完成内容

本次只修复前端无权限页身份判断和文案显示，不修改后端、不修改数据库、不新增 `public-nav.js`，不重建顶部导航，不提交 Git。

修复点：

```text
1. 没有 token 时，admin-permission-guard.js 直接判定为 guest，不请求 /auth/me。
2. token 存在但 /auth/me 返回 401/403 时，清除 xgw_user_token，触发 xgw-auth-changed，并按未登录用户显示。
3. 只有 /auth/me 成功返回用户且 role 不是 admin 时，才按普通用户显示无权限页。
4. 管理员 role === admin 时正常进入管理页。
5. 无权限页增加 permissionRole 标记，身份变化时允许重绘，避免旧的“普通用户”文案覆盖“未登录用户”。
6. dashboard-v2.js 的页面专属权限提示也改为 guest/user/admin 三态判断，避免未登录访问 dashboard.html 后被写成普通用户提示。
```

### 修改文件清单

```text
02_前端页面/admin-permission-guard.js
修复 token 无效时清理登录状态；无权限页按当前 role 重绘；触发 xgw-auth-changed 让顶部头像状态保持一致。

02_前端页面/dashboard-v2.js
将数据看板权限判断从 boolean 改为 guest/user/admin；未登录不请求 /auth/me；401/403 清 token；无权限文案显示当前身份。

02_前端页面/global-loader.js
GLOBAL_SCRIPT_VERSION 从 58 更新到 59。

02_前端页面/*.html
仅机械同步 global-loader.js 引用版本，从 v=58 更新到 v=59；dashboard.html 中 dashboard-v2.js 从 v=53 更新到 v=54。

项目交接与续接总文档.md
追加本次记录。
```

### 测试步骤

未登录访问管理页：

```text
1. 退出登录或清除 xgw_user_token。
2. 打开 dashboard.html / admin.html / manage.html。
3. 应一直显示“当前身份是：未登录用户”。
4. 不应一闪后变成“普通用户”。
5. 顶部头像显示未登录。
6. 不应反复请求 /auth/me。
```

普通用户访问管理页：

```text
1. 登录普通用户。
2. 打开 dashboard.html / admin.html / manage.html。
3. 应显示“当前身份是：普通用户”。
4. 顶部头像显示普通用户。
5. 不显示管理员内容。
```

管理员访问管理页：

```text
1. 登录管理员。
2. 打开 dashboard.html / admin.html / manage.html。
3. 页面正常进入，不显示无权限页。
```

切换账号测试：

```text
1. 管理员切换到普通用户，再打开管理页，应显示普通用户无权限。
2. 退出登录后打开管理页，应显示未登录用户无权限。
3. 不需要手动刷新才能修正身份。
```

### 当前测试状态

已完成 JS 语法检查：

```text
node --check 02_前端页面/admin-permission-guard.js
node --check 02_前端页面/dashboard-v2.js
node --check 02_前端页面/global-loader.js
```

检查通过。已确认没有新增 `public-nav.js`，没有修改后端和数据库，未执行 git commit。当前待人工浏览器测试。

## 2026-06-08 权限加固第一期收尾测试记录

### 本次完成

本次只更新文档测试状态，没有修改业务代码、后端代码、数据库或文件位置。当前最新稳定提交点为：

```text
5ada1141a0eaa0a0a7062465818f341d61926850
```

“后端接口真正权限校验（第一期）”已完成一轮人工收尾测试，测试结论为：已测试通过。

### 修改文件

```text
项目交接与续接总文档.md
补充权限加固第一期人工收尾测试范围、测试通过结果、禁止事项和下一步建议。
```

### 本次测试范围

```text
1. 未登录用户打开公开页面：
   index.html
   submit.html
   map.html
   route.html
   history.html

2. 普通用户使用公开功能：
   查看首页
   查看地图中心
   查看路线中心
   查看历史中心
   提交线索
   提交反馈

3. 普通用户访问管理页面：
   admin.html
   manage.html
   crawler.html
   backup.html
   dashboard.html
   feedback-admin.html

4. 未登录用户访问管理页面。

5. 管理员访问管理页面：
   admin.html
   manage.html
   crawler.html
   backup.html
   dashboard.html
   feedback-admin.html
   history.html

6. bad token 非破坏性安全测试：
   GET /backup/export?token=bad
   DELETE /update-history/clear-range-admin 使用 bad token 和未来日期

7. 普通用户无权限页请求日志检查。

8. 右上角头像菜单切换账号功能回归。
```

### 测试通过结果

```text
1. 未登录用户可以正常打开公开页面。
2. 普通用户可以正常使用公开功能。
3. 普通用户打开管理页面时显示无权限访问，不显示管理员内容。
4. 未登录用户打开管理页面时显示未登录用户无权限。
5. 管理员可以正常打开管理页面。
6. GET /backup/export?token=bad 返回 401。
7. DELETE /update-history/clear-range-admin 使用 bad token 和未来日期返回 401。
8. 普通用户无权限页不再刷 /auth/me 日志。
9. 右上角头像菜单切换账号功能正常。
```

### 禁止事项

权限加固第一期测试通过后，后续继续保持非破坏性测试原则：

```text
1. 禁止用真实数据做真实删除测试。
2. 禁止做真实备份导入测试。
3. 禁止真实清空更新历史或正式数据。
4. 禁止为验证权限而修改数据库。
```

### 当前测试状态

已测试通过。权限加固第一期可以作为稳定基础进入下一阶段。

### 下次继续建议

下一步可以开始考虑小范围目录整理第一阶段，或继续做管理员用户管理页。若做目录整理，必须继续保持小步、可回退、先提交稳定点；若做管理员用户管理页，应优先明确后端权限校验和禁止越权修改规则。

## 2026-06-08 项目目录整理第一阶段

### 本次完成

本次完成“项目目录整理第一阶段：只移动低风险工具脚本”。没有修改后端、数据库、HTML 页面结构、业务 JS、CSS、`global-loader.js` 或 `GLOBAL_SCRIPT_VERSION`，没有新增 `public-nav.js`，没有做新功能。

本次只处理两个前端维护工具脚本，并创建工具目录：

```text
02_前端页面/tools/
```

### 修改文件

```text
02_前端页面/add-global-loader-to-html.py
通过 git mv 移动到 02_前端页面/tools/add-global-loader-to-html.py。

02_前端页面/cleanup-global-scripts.py
通过 git mv 移动到 02_前端页面/tools/cleanup-global-scripts.py。

02_前端页面/tools/add-global-loader-to-html.py
移动后将 HTML 扫描目录适配为父目录 02_前端页面，避免脚本只扫描 tools 目录；同步更新脚本顶部用法注释。

02_前端页面/tools/cleanup-global-scripts.py
移动后将 HTML 扫描目录适配为父目录 02_前端页面，避免脚本只扫描 tools 目录。

项目交接与续接总文档.md
更新工具脚本新路径，追加本次整理记录。

小谷围岛广州大学城真实生活信息共建系统_续接文档.md
更新工具脚本新路径和运行命令。
```

### 前端变化

无页面功能变化。没有移动 HTML、业务 JS、CSS；没有修改任何 HTML 的 `<script>` 引用；没有修改 `global-loader.js`。

### 后端变化

无。没有修改 `01_后端服务`，没有修改数据库文件。

### 测试步骤

```text
1. 运行 git status，确认仅出现两个工具脚本移动、两个工具脚本路径适配和必要文档更新。
2. 确认 02_前端页面/tools/add-global-loader-to-html.py 存在。
3. 确认 02_前端页面/tools/cleanup-global-scripts.py 存在。
4. 全局搜索 add-global-loader-to-html.py 和 cleanup-global-scripts.py，确认旧路径说明已更新为 tools/ 下的新路径。
5. 确认没有修改 01_后端服务。
6. 确认没有修改数据库文件。
7. 确认没有修改 HTML 页面结构和 HTML script 引用。
8. 确认没有修改业务 JS、CSS、global-loader.js 或 GLOBAL_SCRIPT_VERSION。
```

### 当前测试状态

已完成文件移动、路径引用检查和静态状态检查；待人工确认目录整理结果。未执行 Git 提交。

### 下次继续建议

测试确认无误后提交 Git。第二阶段再考虑整理更多前端脚本，但必须另开任务，不能一次性大搬家；移动任何业务 JS 前必须先明确页面引用、`global-loader.js` 加载路径和回退方案。

## 2026-06-08 统一顶部标题栏按钮

### 本次完成

本次统一各页面顶部标题栏按钮命名和显示顺序，并压缩顶部导航按钮样式，使桌面宽度下尽量一行展示。

本次没有使用 `public-nav.js`，没有新增 `public-nav.js`，没有通过 JS 重建导航栏，没有隐藏旧 header 再生成新导航，没有拦截导航点击，也没有使用 `preventDefault` 阻止跳转。所有顶部按钮继续使用页面内原有的 `button onclick="location.href='...'"` 方式跳转。

### 顶部入口顺序

公开入口固定顺序：

```text
首页 -> index.html
提交线索 -> submit.html
地图中心 -> map.html
路线中心 -> route.html
历史中心 -> history.html
```

管理员入口固定顺序：

```text
审核中心 -> admin.html
数据管理 -> manage.html
采集管理 -> crawler.html
数据看板 -> dashboard.html
备份 -> backup.html
反馈管理 -> feedback-admin.html
```

权限显示继续复用现有 `admin-permission-guard.js`。未登录用户和普通用户不应看到管理员入口，管理员可见。

### 修改文件

```text
02_前端页面/index.html
02_前端页面/submit.html
02_前端页面/map.html
02_前端页面/route.html
02_前端页面/history.html
02_前端页面/admin.html
02_前端页面/manage.html
02_前端页面/crawler.html
02_前端页面/dashboard.html
02_前端页面/backup.html
02_前端页面/feedback-admin.html
02_前端页面/clue-detail.html
02_前端页面/item-detail.html
02_前端页面/map-detail.html
02_前端页面/route-detail.html
统一顶部 header-actions 内按钮名称和顺序。同步将历史页标题改为“历史中心”，备份页标题改为“备份”，管理员反馈页标题改为“反馈管理”，提交页标题改为“提交线索”。首页中原导航性质的“备份导出”入口改为“备份”。

02_前端页面/style.css
只调整顶部标题栏相关样式：缩小 header padding、标题字号、顶部按钮字号、按钮 padding 和按钮间距；header-actions 保持 flex 布局，桌面宽度下尽量不换行。

项目交接与续接总文档.md
追加本次记录。
```

### 未修改内容

```text
没有修改后端。
没有修改数据库。
没有修改 global-loader.js。
没有修改 GLOBAL_SCRIPT_VERSION。
没有修改 admin-permission-guard.js。
没有修改头像菜单、切换账号功能、无权限页逻辑或表单快捷键。
没有移动文件。
没有新增 public-nav.js。
没有做管理员用户管理页。
```

### 检查结果

已完成：

```text
1. 检查 15 个重点页面 header-actions 中的按钮 href 顺序一致。
2. 搜索确认顶部按钮中不再出现“返回首页”“更新历史”“管理采集目标”“提交一条线索”“备份导出”“反馈中心”等旧导航名称。
3. 确认没有修改 01_后端服务。
4. 确认没有修改数据库文件。
5. 确认没有修改 global-loader.js 或其他全局 JS。
6. 确认没有新增 02_前端页面/public-nav.js。
```

本次没有修改 JS，因此没有运行 JS 语法检查。

### 人工测试步骤

未登录测试：

```text
1. 打开 index.html。
2. 顶部公共入口名称应统一：首页、提交线索、地图中心、路线中心、历史中心。
3. 不应看到管理员入口。
4. 不应出现“返回首页”“更新历史”等不统一名称。
5. 顶部按钮应比之前小，尽量一行。
```

普通用户测试：

```text
1. 登录普通用户。
2. 打开 index.html、submit.html、map.html、route.html、history.html。
3. 顶部公共入口名称和顺序应基本一致。
4. 不应看到管理员入口。
5. 点击每个顶部按钮都能正常跳转。
6. 不应出现乱码。
```

管理员测试：

```text
1. 登录管理员。
2. 打开 index.html、admin.html、manage.html、crawler.html、dashboard.html、backup.html、feedback-admin.html。
3. 管理员应能看到公共入口和管理员入口。
4. 管理员入口名称应统一：审核中心、数据管理、采集管理、数据看板、备份、反馈管理。
5. 点击顶部按钮应能正常跳转。
6. 页面不应乱码。
```

不应影响：

```text
提交按钮、保存按钮、搜索按钮、筛选按钮、删除按钮、清空按钮、用户头像菜单、切换账号功能、无权限页判断、表单上下箭头快捷键。
```

### 当前测试状态

已测试通过。用户已完成人工浏览器测试，确认：

```text
1. 首页、提交线索、地图中心、路线中心、历史中心 5 个公共按钮在各页面顺序一致。
2. 5 个公共按钮的横向起点和相对位置稳定。
3. clue-detail.html、item-detail.html、map-detail.html、route-detail.html 已和普通页面保持同一套顶部导航结构。
4. 用户头像固定在右侧，不再挤压公共按钮。
5. 管理员入口在公共入口后方，顺序正确。
6. 未登录用户和普通用户看不到管理员入口，管理员可以看到管理员入口。
7. 顶部按钮点击能正常跳转。
8. 中文没有乱码，没有 ??。
9. 没有新增 public-nav.js。
10. 没有修改 global-loader.js。
11. 没有修改后端、数据库、业务 JS。
```

### 下次继续建议

先完成人工测试并确认未登录、普通用户、管理员三种状态下顶部导航显示和跳转都正常；测试通过后再提交 Git。不要回到 `public-nav.js` 或全局 JS 重建导航方案。

## 2026-06-08 顶部标题栏布局稳定性修复

### 本次完成

本次在上一轮“统一顶部标题栏按钮”未提交改动基础上继续修复，只处理标题栏布局稳定性，没有做新功能，没有提交 Git。

修复重点：

```text
1. 顶部标题栏固定为三块区域：左侧标题与副标题、中间固定导航、右侧用户头像/登录状态。
2. 公共入口固定为前 5 个按钮，顺序和相对位置不随页面标题长短变化。
3. 管理员入口固定排在公共入口后面；管理员入口隐藏或显示时，不改变公共入口顺序和起点。
4. 详情页 clue-detail.html、item-detail.html、map-detail.html、route-detail.html 继续使用同一套顶部标题栏结构。
```

### 修改文件

```text
02_前端页面/style.css
将 .header-inner 调整为三列 grid：
左列为稳定标题区，中列为 header-actions 导航区，右列为用户头像组件区。
header-actions 使用 justify-content:flex-start，避免因居中或标题长度导致公共按钮漂移。
桌面宽度下尽量不换行，窄屏允许改为单列堆叠。

项目交接与续接总文档.md
追加本次布局稳定性修复记录。
```

### 未修改内容

```text
没有修改后端。
没有修改数据库。
没有新增 public-nav.js。
没有修改 global-loader.js。
没有修改 admin-permission-guard.js。
没有修改业务 JS。
没有修改头像菜单、切换账号功能、无权限页逻辑或表单快捷键。
没有提交 Git。
```

### 检查结果

已完成：

```text
1. 检查 15 个页面 header-actions 按钮 href 顺序完全一致。
2. 搜索确认没有 ??、冲突标记、返回首页、管理采集目标、提交一条线索、更新历史按钮、备份导出按钮、反馈中心按钮等异常残留。
3. 确认没有新增 02_前端页面/public-nav.js。
4. 确认没有修改 global-loader.js。
5. 确认没有修改 01_后端服务。
6. 确认没有修改数据库。
7. 确认没有修改业务 JS。
```

### 人工测试步骤

```text
1. 未登录打开 index.html、submit.html、map.html、route.html、history.html。
2. 确认公共入口始终为：首页、提交线索、地图中心、路线中心、历史中心。
3. 确认公共入口在不同页面之间起点和相对位置稳定，不随页面标题长短漂移。
4. 登录普通用户后重复打开公开页面，确认公共入口位置不变，管理员入口不可见。
5. 登录管理员后打开 index.html、admin.html、manage.html、crawler.html、dashboard.html、backup.html、feedback-admin.html。
6. 确认管理员入口固定排在公共入口后面：审核中心、数据管理、采集管理、数据看板、备份、反馈管理。
7. 打开 clue-detail.html、item-detail.html、map-detail.html、route-detail.html，确认详情页顶部结构一致。
8. 点击顶部按钮确认正常跳转，不出现乱码。
9. 确认页面主体提交、保存、搜索、筛选、删除、清空、头像菜单、切换账号、无权限页、表单上下箭头快捷键不受影响。
```

### 当前测试状态

已完成静态检查，待人工浏览器测试。未执行 Git 提交。

### 下次继续建议

先完成浏览器测试，重点看不同页面之间公共按钮是否仍有水平漂移。测试通过后再提交 Git；不要新增 `public-nav.js`，不要改用全局 JS 重建导航。

## 2026-06-08 顶部公共按钮和用户头像固定布局补充修复

### 本次完成

本次继续在未提交的顶部标题栏统一改动基础上修复布局稳定性，只修改 `style.css` 和本文档，没有提交 Git。

修复点：

```text
1. 标题区、公共导航区、用户头像区改为更明确的三列固定布局。
2. 标题区固定在第一列，标题长短不再推动导航起点。
3. header-actions 固定在第二列并左对齐，公共入口前 5 个按钮起点固定。
4. userWidget 固定在第三列并右对齐，用户头像不再挤压或改变导航起点。
5. 管理员入口继续排在公共入口之后；管理员入口隐藏或显示不改变前 5 个公共按钮的顺序和相对起点。
6. 详情页继续沿用同一套 header-actions 顺序和布局。
```

### 修改文件

```text
02_前端页面/style.css
进一步固定 header 三列布局：
grid-template-columns: 320px minmax(0, 1fr) 170px；
标题区 grid-column:1；
header-actions grid-column:2 且 justify-self:start；
#userWidget grid-column:3 且 justify-self:end；
窄屏时三块区域回到单列排列。

项目交接与续接总文档.md
追加本次补充修复记录。
```

### 检查结果

已完成：

```text
1. 检查 15 个页面 header-actions 按钮 href 顺序完全一致。
2. 检查没有 ??、冲突标记、返回首页、管理采集目标、提交一条线索、更新历史按钮、备份导出按钮、反馈中心按钮等异常残留。
3. 确认没有新增 public-nav.js。
4. 确认没有修改 global-loader.js。
5. 确认没有修改后端。
6. 确认没有修改数据库。
7. 确认没有修改业务 JS。
```

### 测试步骤

```text
1. 分别打开 index.html、submit.html、map.html、route.html、history.html。
2. 对比公共入口：首页、提交线索、地图中心、路线中心、历史中心，确认横向起点一致。
3. 打开 clue-detail.html、item-detail.html、map-detail.html、route-detail.html，确认详情页公共入口起点一致。
4. 未登录和普通用户状态下，确认管理员入口隐藏后公共入口仍保持同一位置。
5. 管理员状态下，确认管理员入口出现在公共入口之后，且公共入口前 5 个按钮位置不变。
6. 确认右上角用户头像固定在右侧，头像菜单和切换账号功能正常。
7. 确认顶部按钮正常跳转，页面无乱码。
```

### 当前测试状态

已完成静态检查，待人工浏览器测试。未执行 Git 提交。

### 下次继续建议

人工测试时重点对比不同页面公共按钮左边缘是否一致。如果仍有轻微漂移，下一步优先考虑给 header 内部标题、导航、用户区增加显式 class，但仍不要新增 `public-nav.js` 或用 JS 重建导航。
